# Changelog

## [6.0.0-preview.6.1.5] — 2026-06-25
- **Stream-to-device: playing an un-prepped file no longer gets stuck "Reconnecting…" once the background prep finishes.** When you play a file that isn't fully prepped, playback runs over a short-lived **on-demand (JIT)** session while the full bundle builds in the background. The player buffers far ahead and — while paused or fully buffered — could go longer than the server's 90 s idle window without fetching a segment, so the reaper tore the session (and its segment dir) down mid-watch. The next fetch then `410`'d and the generic reconnect loop just retried the **dead** session URL forever ("stuck reconnecting"), never noticing the now-ready full bundle. Two fixes in [static/index.html](static/index.html):
  - **Keepalive.** While on-demand playback is active, a lightweight ping (`_lpOdKeepAlive`, throttled ~30 s, driven by the existing stall-watch tick) touches the session so its `last_access` stays fresh even when no segments are being fetched — the reaper no longer deletes a session that's still being watched.
  - **Recover + switch over.** A `410` (session reaped/expired) now triggers a full **re-prepare at the current position** (`_lpReloadOnDemand`) instead of the dead-session retry loop: if the background full prep has finished it seamlessly **promotes playback to the rich ABR bundle**, otherwise it spins up a fresh on-demand session and resumes. Wired for both the hls.js path (detects the `410` directly) and the Safari-native path (escalates after the reconnect backoff grows). See [docs/STREAMING.md](docs/STREAMING.md).
  - *Host-served web change: reload the dashboard. No native rebuild or server restart needed.*

## [6.0.0-preview.6.1.4] — 2026-06-25
- **iOS: hide the developer/test affordances from the app UI.** Two test-only entry points are no longer shown: the **Localhost HLS self-test (Gate 1b)** link on the app's connect screen ([ios-app/www/index.html](ios-app/www/index.html)), and the **Offline Sync → "Sync Now & Show Status"** diagnostics row in profile settings ([static/index.html](static/index.html)). The underlying code is left fully in place — `localtest.html` stays in the bundle, and the `psOfflineSyncRow` markup, `_appSyncDebug()`, and the `syncDebugModal` are untouched; each is re-enabled by un-commenting a single line if needed again. *Host-served web + bundled app asset change: reload the dashboard; the self-test link removal ships on the next `./build-ipa.sh` rebuild. Server change is version-only.*

## [6.0.0-preview.6.1.3] — 2026-06-24
- **iOS: a brief connectivity loss no longer wedges or drops bulk downloads.** Losing the connection even momentarily during a bulk download left episodes stuck at "Queued…" (or silently dropped) and they never resumed. Two failure points, both now resilient:
  - **JS orchestration could hang or give up.** The per-episode manifest/prep `fetch`es had no timeout, so a WKWebView request caught mid-blip could hang indefinitely and **wedge the whole download pipeline** — every episode queued behind it stuck forever. And when a fetch did fail, the episode was deleted outright. Added an `AbortController` timeout wrapper (`_appFetchT`) to every download-path fetch, a network-vs-server error classifier (`_appIsNetErr`), a **bounded retry-with-backoff** around the manifest/prep/handoff so a brief outage is absorbed and the episode is *kept* (shown as "Waiting for connection…") instead of dropped, and a prep-poll that keeps polling through a blip rather than aborting. ([static/index.html](static/index.html))
  - **Native transfers were cancelled on any blip.** `BundleDownloader`'s `URLSession` delegate dropped the entire bundle on *any* transport error (and on a 5xx/429 from a flaky tunnel). It now **re-enqueues the failed file** with a capped, backed-off retry (`retryOrFail`, shared by the transport-error and HTTP-status paths); because the session is a background `URLSession`, the re-created task **waits for connectivity** and resumes on its own when the link returns. Only a persistent failure (retries exhausted) or a genuine permanent error surfaces. ([BundleDownloader.swift](ios-app/ios/App/App/BundleDownloader.swift)) See [docs/GOTCHAS.md](docs/GOTCHAS.md).
  - *Requires a `./build-ipa.sh` rebuild for the native retry; the JS resilience is host-served (reload the dashboard). Server change is version-only.*

## [6.0.0-preview.6.1.2] — 2026-06-24
- **iOS: bulk offline downloads start much faster — no more 20-30 s "Queued…" lag.** Two compounding bottlenecks made each episode in a batch wait many seconds before transferring, even with nothing actively downloading:
  - **The host re-fetched the same series poster from TMDb for every episode.** `GET /bundle-manifest` inlines the series poster as a base64 `data:` URL so the offline Downloads picker can show it with no network — but it did a fresh TMDb download (up to a 12 s timeout) on *every* per-episode manifest fetch. A whole-series bulk download therefore paid that round-trip N times. `_tmdb_image_data_url` now **memoizes** results by path+size (immutable posters, so no invalidation needed) behind a lock that also dedups concurrent identical fetches — the poster downloads once per series, the rest are instant. ([main.py](main.py))
  - **The app downloaded the batch strictly one episode at a time.** Both bulk paths (*Download all* on a card, multi-select *Download (N)* in the picker) ran `await appDownloadBundle` serially, so every episode's host round-trip stacked up and later episodes sat at "Queued…". A new bounded-concurrency runner `_appRunPooled` now pipelines a few manifest fetches at once (the native background `URLSession` already runs the actual transfers concurrently, and host-side HLS prep is still serialized server-side, so a small limit never floods the host). ([static/index.html](static/index.html))
  - *Host-served web + server change: reload the dashboard — no native rebuild needed.*

## [6.0.0-preview.6.1.1] — 2026-06-24
- **iOS: fix bulk offline downloads finishing but never appearing in Downloads.** When saving many episodes at once, files that completed while the app was backgrounded (or after iOS killed and headlessly relaunched it to deliver the background `URLSession` events) were moved to disk but **never flagged `complete` in `index.json`** — the in-memory `Job` that calls `markComplete` is only created by `startDownload`, so on a relaunch `didFinishDownloadingTo`'s `guard let job = self.jobs[sha]` bailed before finalizing. Since `downloads.html` lists only `complete` bundles, fully-downloaded episodes silently vanished from the Downloads section even though every segment was on disk. Added a **disk-truth reconcile pass** (`reconcileIndexLocked`) that flags a bundle complete whenever all its expected files are present at their expected size, run whenever the index is read for display (`list()` / `getLocal()`) and after the background session flushes its events (`urlSessionDidFinishEvents`, which also emits the deferred `bundleComplete` so the dashboard/Live Activity update). Completion no longer depends on the in-memory job surviving the download. ([BundleDownloader.swift](ios-app/ios/App/App/BundleDownloader.swift)) *Requires a `./build-ipa.sh` rebuild for the app; existing stuck downloads self-heal on the next Downloads refresh — no re-download needed. Server change is version-only.*

## [6.0.0-preview.6.1.0] — 2026-06-24
- **iOS: choose the download quality when saving offline.** Previously every offline download silently grabbed the source (highest) resolution. Now, when a download starts and the host bundle has more than one rung (e.g. 1080p source + 720p + 480p down-rungs), the app shows a **Download Quality** chooser listing only the qualities that actually exist, each with its **download size** (`1080p (Source) — 1.4 GB`, `720p — 600 MB`, …). For a bulk download — *Download all* on a card, or multi-select *Download (N)* in the episode picker — the chooser appears **once** and applies to the whole batch; a single-rung bundle skips the prompt and downloads as before. `GET /bundle-manifest` now returns the full ABR ladder annotated with per-rung download bytes (`videos[]`) and accepts `?quality=<height>|original` to ship just that rung's `files[]` + rewritten `master_m3u8`. New `_bundle_rung_info` / `_bundle_select_rung` helpers (generalising `_bundle_highest_only`) in [main.py](main.py); `_appChooseQuality` / `_appBatchQuality` + reworked `appDownloadBundle` in [static/index.html](static/index.html). *Host-served web change: reload the dashboard — no native rebuild needed (`BundleDownloader` already writes the inline trimmed master).* See [docs/STREAMING.md](docs/STREAMING.md) and [docs/API.md](docs/API.md).

## [6.0.0-preview.6.0.0] — 2026-06-24
- **iOS: background downloads + lock-screen/Dynamic Island Live Activities, and a Dynamic Island TV remote.** Two new capabilities, both built on ActivityKit Live Activities via a new **`StreamLinkLiveActivities`** widget-extension target (App Group `group.com.streamlink.client`, deployment iOS 17; everything availability-gated so iOS <17 devices are byte-for-byte unchanged).
  - **Downloads keep running when the app is minimized, with a progress Live Activity.** `BundleDownloader` switched from a foreground `URLSession` (which died ~30 s after backgrounding) to a **background `URLSession`** that genuinely continues and completes transfers while suspended, with a new `AppDelegate.application(_:handleEventsForBackgroundURLSession:)` hook so the session flushes on wake. A new `DownloadLiveActivity` controller shows an aggregate download-progress activity on the lock screen / Dynamic Island (title + progress bar + files-done), updated live while foreground and per-file while suspended, ending with a brief complete/failed frame. ([BundleDownloader.swift](ios-app/ios/App/App/BundleDownloader.swift), [AppDelegate.swift](ios-app/ios/App/App/AppDelegate.swift), [DownloadLiveActivity.swift](ios-app/ios/App/App/DownloadLiveActivity.swift), [DownloadActivityWidget.swift](ios-app/ios/App/StreamLinkLiveActivities/DownloadActivityWidget.swift))
  - **Dynamic Island TV remote when something is playing on the TV.** When a TV session is active (host VLC or YouTube kiosk) **and** the fullscreen controls are open, the new **`TVRemote`** Capacitor plugin starts a Live Activity; once the app is minimized iOS surfaces a Dynamic Island / lock-screen remote with **Pause/Play, Vol−, Vol+** buttons. The buttons are `LiveActivityIntent`s (run in the app process) that POST the same control endpoints the dashboard uses — `/api/vlc/pause` · `/api/vlc/volume/up|down` for VLC, `/api/youtube/control` for YouTube — reading the host URL + token from the shared App Group. ([TVRemote.swift](ios-app/ios/App/App/TVRemote.swift), [TVRemoteIntents.swift](ios-app/ios/App/Shared/TVRemoteIntents.swift), [TVRemoteWidget.swift](ios-app/ios/App/StreamLinkLiveActivities/TVRemoteWidget.swift), [static/index.html](static/index.html))
  - *Requires a `./build-ipa.sh` rebuild for the app, and a one-time Xcode pass to confirm automatic signing provisions both targets with the App Group capability. The web glue (`_tvRemoteSync`) is host-served and inert outside the app. Server change is version-only (`UI_VERSION`).* See [docs/IOS_APP_PLAN.md](docs/IOS_APP_PLAN.md) and [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [6.0.0-preview.5.3.0] — 2026-06-24
- **iOS: new in-app Dashboard in the ☰ App menu — server status, live ongoing downloads, and storage-by-title.** The app menu previously only had *Downloads* and *Change Server / Re-pair*. Added a **Dashboard** entry that opens a full overlay **on the host page**, so the live SSE link stays connected the whole time it's open (unlike the offline `downloads.html`, which has no server connection). It shows three things at a glance, refreshing on a 1 s tick while open (and tearing the tick down on close):
  - **Server connection status** — a live green/red dot + label driven by the dashboard's existing SSE `app._connected` flag (Connected / Reconnecting / Offline), plus the saved host address.
  - **Ongoing downloads** — every in-flight offline bundle with its live percentage and byte counts, sourced from the same `offlineBundles` map the native `BundleDownloader` progress events already keep current (host prep 0–50 %, device transfer 50–100 %). Bundle entries now also carry their display `name`/`meta` so the list can label episodes (`Series — S01E02 Title`) even mid-prep.
  - **Local storage used, grouped by series / movie** — completed bundles grouped by title with a per-title size bar and the device total, so it's clear what's taking up space. ([static/index.html](static/index.html))
  - *Host-served web change: reload the dashboard — no native rebuild needed (the native plugins are unchanged).*

## [6.0.0-preview.5.2.1] — 2026-06-24
- **iOS: offline downloads now save only the highest (source) resolution, not the whole ABR ladder.** A downloaded bundle was fetching *every* video rung the host had prepped — the source-resolution original **plus** the 720p and 480p ABR down-rungs — so each saved episode used far more space than needed (the device only ever plays the top rung from a single downloaded file). `GET /bundle-manifest` now filters its `files[]` to the original (idx 0) rung, drops every down-rung's playlist + init + segments, and returns the **rewritten `master.m3u8` inline** (`master_m3u8`) referencing only the kept rung — a non-destructive, per-download equivalent of the admin "Drop HLS Resolutions" trim (the shared on-disk bundle still serves all rungs to streaming clients). The app passes `masterContent` to the native `BundleDownloader.download`, which writes the trimmed master verbatim before its resume scan so the dropped rungs are never fetched or referenced. New `_bundle_highest_only` helper in [main.py](main.py); [static/index.html](static/index.html); [BundleDownloader.swift](ios-app/ios/App/App/BundleDownloader.swift). *Requires a `./build-ipa.sh` rebuild for the app; reload the dashboard for the browser. Existing oversized downloads are unaffected — remove and re-download to reclaim the space.*

## [6.0.0-preview.5.2.0] — 2026-06-24
- **iOS: instant download feedback, no more thumbnail flashing, and app-native download on the library card.** Builds on 5.1.0's bulk app download:
  - **Bulk "Download (N)" now paints every selected episode's spinner instantly.** Previously each episode only showed an in-progress icon after `appDownloadBundle`'s per-file manifest fetch resolved (a few seconds' lag). Selected episodes are now pre-marked **`queued`** and their per-row button is repainted up front, before any network call. New `queued` state in `_appDlBtnHTML`. ([static/index.html](static/index.html))
  - **Episode thumbnails no longer flash during downloads.** Progress events used to call full `renderEpList()` (throttled), which rebuilt every `<img>` still on each tick → visible flashing. Replaced with a **targeted `_appRefreshDlBtn(itemId, filePath)`** that updates only the one episode's download button slot (`.ep-appdl-slot`); while actively transferring it touches just the `%` text so even the spinner animation isn't restarted. `appDownloadBundle`, the host-prep poll, and the `bundleProgress`/`bundleComplete`/`bundleError` listeners all use it now; the old `_appThrottledEpRefresh` full-list refresh is gone. ([static/index.html](static/index.html))
  - **The library/series card download button now uses the app download in the app** (web ZIP/direct-link in a browser, unchanged). Single-file cards save the one file as an offline bundle (`appDownloadBundle`); multi-file cards save **all** on-disk episodes as bundles via new `appDownloadAllBundles()`. Falls back to the web path when the host can't build HLS bundles. ([static/index.html](static/index.html))
  - *Web change: requires a `./build-ipa.sh` rebuild for the app; reload the dashboard for the browser.*

## [6.0.0-preview.5.1.0] — 2026-06-24
- **iOS: episode download UX cleanup + season-scoped bulk select.** Three changes, all `isApp`-gated so the browser dashboard is unaffected:
  - **In the app, the per-row "Download to device" (web/host) button is now hidden** — only the app's offline-bundle download button shows. Previously both rendered with the same icon, which was confusing. The web download link still shows in the plain browser. ([static/index.html](static/index.html))
  - **Bulk "Download (N)" now saves offline bundles on the device when in the app** (the same path as the per-row app download button), instead of triggering a host ZIP download. It skips episodes already saved/in-flight, and requires HLS prep on the host. The browser keeps the single-file/ZIP behaviour. ([static/index.html](static/index.html))
  - **New "± Season" bulk chip** selects or clears every episode in the currently-visible season at once (a toggle — clears if all are already selected). Shown only for season-split items; other seasons keep their selection. ([static/index.html](static/index.html))
  - *Web change: requires a `./build-ipa.sh` rebuild for the app; reload the dashboard for the browser.*

## [6.0.0-preview.5.0.4] — 2026-06-24
- **iOS: fixed the status bar / Dynamic Island overlapping the top of the fullscreen remote (the "Close / Now Playing / Night" header) and the top app bar.** Although 5.0.3 made the CSS `env(safe-area-inset-*)` insets the single source of truth, in the Capacitor WKWebView `env(safe-area-inset-top)` resolves to ~0 on the navigated host dashboard, so `.safe-top` chrome got no top padding and the status bar covered it. Added an `.is-app`-scoped fallback — `padding-top: max(env(safe-area-inset-top), 59px)` (and a matching `34px` home-indicator floor for `.safe-bottom`) — so a real env() value still wins when WebKit reports one, but a missing inset no longer leaves the chrome under the Island. **Scoped to `@media (orientation: portrait)`** so landscape (where the top inset is legitimately 0) isn't pushed down by a forced band. ([static/index.html](static/index.html)) *Host-served web change: reload the dashboard — no native rebuild needed.*

## [6.0.0-preview.5.0.3] — 2026-06-24
- **iOS: fixed the white bar at the top + the whole UI shifting down after rotating the device to landscape and back.** The dashboard already provides its own safe-area insets in CSS (`.safe-top`/`.safe-bottom` = `env(safe-area-inset-*)`), but Capacitor was *also* insetting the WebView via **`ios.contentInset: "always"`** — so the top inset was applied twice, and Capacitor's `scrollView.contentInset` **goes stale on orientation change**, leaving the scroll view's (white) background exposed above the content and the layout pushed down until relaunch. Switched to **`contentInset: "never"`** in both [capacitor.config.json](ios-app/capacitor.config.json) files so the web content owns the full `viewport-fit=cover` viewport and the CSS `env()` insets — which WebKit **does** recompute correctly on rotation — are the single source of truth. Also set the WebView `backgroundColor` + `html`/`body` background to `#030712` (gray-950) so any transient gap during a reflow is dark, not white. ([static/index.html](static/index.html)) *Native config change: requires a full `./build-ipa.sh` rebuild.*

## [6.0.0-preview.5.0.2] — 2026-06-24
- **iOS: fixed the dashboard layout intermittently breaking during use (misaligned/overlapping/clipped, buttons dead until app relaunch) — a WKWebView `dvh` viewport bug dating back to M2.** The height-locked app shell (`html,body{overflow:hidden}` + `body{height:100dvh}`) tracks the **dynamic** viewport so a mobile browser's collapsing URL bar doesn't clip it. But inside the Capacitor WebView there is no URL bar, and WKWebView **shrinks the dynamic viewport when the soft keyboard appears** (search box, profile name, episode field…) and frequently **fails to restore it on dismiss** — leaving the locked shell sized wrong until a relaunch. Fix: mark the document `is-app` **before first paint** (an inline `window.Capacitor.isNativePlatform()` check; the runtime is injected at document-start) and, scoped to `.is-app`, pin the shell to the **stable large-viewport `vh`** instead of `dvh` — correct *and* stable in the app since there's no chrome to collapse. The browser keeps `dvh` (byte-for-byte unchanged), and the browser-only "swipe to hide the URL bar" body-growth hack is now excluded in-app (`html:not(.is-app)`). ([static/index.html](static/index.html)) *Web change: requires a `./build-ipa.sh` rebuild.*

## [6.0.0-preview.5.0.1] — 2026-06-23
- **iOS: the always-on `☰ App` menu now hides during on-device playback** so it no longer floats over the local player. Pure CSS — `body:has(#localPlayer.lp-active:not(.lp-tiny)) #appNav { display:none }` ([static/index.html](static/index.html)) — so it auto-tracks the player lifecycle (reappears on close) and stays visible in tiny picture-in-picture mode. *Web change: requires a `./build-ipa.sh` rebuild.*

## [6.0.0-preview.5.0.0] — 2026-06-23
- **M5 of the iOS client app: device pairing / auth, in-app navigation, and downloads management.** Adds safe remote use and always-available access to settings + downloads.
  - **Backend (Windows-correct), plan A4 — device pairing:** new **`POST /api/pair`** ([main.py](main.py)) issues a long-lived bearer token to a client app, with the host&rsquo;s `ADMIN_PASSWORD` as the pairing secret. Tokens are **persisted** to host-local `device_tokens.json` (gitignored) so a paired device survives a host restart (unlike in-memory admin sessions). Companion routes: **`GET /api/pair/status`** (does the host require pairing / is this token valid), **`DELETE /api/pair`** (a device revokes its own token), and admin **`GET/DELETE /api/admin/devices[/{id}]`** to list/revoke paired devices (token prefix only, never the secret). New `_check_device_token` / `_require_device_auth` helpers; the device-facing endpoints **`/api/sync/progress`**, **`/api/sync/pull`**, **`/api/sync/resolve`** and **`/api/library/{id}/bundle-manifest`** now require a valid device **or** admin token — but **only when the new `REQUIRE_DEVICE_AUTH` setting is on**. It defaults **off**, so LAN/browser use and online HLS playback are byte-for-byte unaffected (the no-regression invariant); turn it on for remote exposure. *Known gap (documented):* shared online-playback surfaces (`/api/library/offline-cache/*`, `/offline-prepare`, `/api/library`) stay open so the browser dashboard keeps working — see [docs/IOS_APP_PLAN.md](docs/IOS_APP_PLAN.md).
  - **Native:** `OfflineStore` gains **`setPairingToken` / `getPairingToken`** ([ios-app/ios/App/App/OfflineStore.swift](ios-app/ios/App/App/OfflineStore.swift)). The token must live natively because the connect shell (`capacitor://localhost`) and the host dashboard (`https://host`) are different origins and can&rsquo;t share `localStorage` — same reason the offline progress log is native. (Download management — `list`/`remove`/`bytesUsed` — already existed in `BundleDownloader`.)
  - **App shell** ([ios-app/www/index.html](ios-app/www/index.html)): the Connect screen&rsquo;s old reserved &ldquo;pairing token&rdquo; field is now a working **Host password** field — entering the host&rsquo;s admin password pairs the device (`POST /api/pair`) and stores the token natively before navigating. Shows when a device is already paired.
  - **Dashboard** ([static/index.html](static/index.html)): reads the native token at startup and sends `Authorization: Bearer` on all device-facing fetches (`_appPairingToken`/`_appAuthHeaders`); a 401 on bundle-manifest tells the user to re-pair. New **always-on in-app nav** (app only) — a small `☰ App` menu giving one-tap access to **Downloads** and **Change Server / Re-pair** from anywhere, online or off (the host-served dashboard otherwise has no way back to the app shell). The offline [downloads.html](ios-app/www/downloads.html) gains a matching **Settings** button (it already had per-download delete + storage-used).
  - **All app glue is `isApp`-gated and auth is opt-in** — the plain browser dashboard is unaffected. *Native + web changes: requires a full `./build-ipa.sh` rebuild (no `--fast`/`--no-sync`).* **Docs:** [docs/API.md](docs/API.md), [docs/IOS_APP_PLAN.md](docs/IOS_APP_PLAN.md), [README.md](README.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [6.0.0-preview.4.1.1] — 2026-06-24
- **iOS: fixed offline watch progress never reaching the server — the sync endpoint was being shadowed by a route collision (root cause of "device→server sync doesn't work").** `POST /api/library/sync/progress` was registered *after* `POST /api/library/{item_id}/progress` (`update_progress`), so FastAPI matched the parameterized route first with `item_id="sync"` and ran `update_progress`, which requires a top-level `file_path` — every push therefore failed with **HTTP 422 `body.file_path required`** and was silently swallowed by the flush's empty `catch`. (`sync/pull` and `sync/resolve` were unaffected — no `{item_id}/pull` or `/resolve` siblings exist — which is why offline *resume* tracked the server but offline *history never pushed back*.) Moved all three sync routes out of the `/api/library/{item_id}` namespace to **`/api/sync/progress`**, **`/api/sync/pull`**, **`/api/sync/resolve`** so the collision is structurally impossible, and updated every caller ([static/index.html](static/index.html)) and the docs. The new on-device "Sync Now & Show Status" diagnostic surfaced the exact 422 that pinned this down. ([main.py](main.py)) *Web change: requires a `./build-ipa.sh` rebuild.* **Docs:** [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/STREAMING.md](docs/STREAMING.md), [docs/IOS_APP_PLAN.md](docs/IOS_APP_PLAN.md).

## [6.0.0-preview.4.1.0] — 2026-06-23
- **iOS: added an on-device offline-sync diagnostic to root-cause why device→server progress push isn't landing.** The whole flush path (`_appFlushOfflineProgress` and its `OfflineStore`/`fetch` calls) swallows every error in empty `catch` blocks, so a failed push is silent and indistinguishable from "nothing to push." New **Profile Settings → This Device → Offline Sync → "Sync Now & Show Status"** (app-only; revealed by the Capacitor check) runs the *same* push path as the auto-flush and reports each step in a modal: environment (`isApp`, plugins present, selected profile), the native store's active profile, **every stored record** (position / duration / `completed` / `dirty` / `base_synced_at` / profile), what `pending()` would actually push, and the live **`POST /api/library/sync/progress`** outcome (HTTP status, `applied`/`conflicts` counts, or the thrown fetch error). It marks watermarks on success and opens the M4 conflict UI if the server returns conflicts — so one tap shows exactly where the chain breaks (offline watch never saved, filtered out as trivial, host unreachable/TLS, wrong profile, or a genuine conflict). All `isApp`-gated; the browser dashboard is unaffected. *Web change: requires a `./build-ipa.sh` rebuild.*

## [6.0.0-preview.4.0.0] — 2026-06-23
- **M4 of the iOS client app: sync-conflict resolution.** When the same episode advanced **both** offline (in the app) **and** elsewhere (e.g. on the TV) with positions too far apart to auto-merge, `POST /api/library/sync/progress` already returned those divergences in `conflicts` and wrote nothing — but M3 left them pending. M4 surfaces them: the app now shows a **"keep mine / keep server"** chooser and writes the user's pick. **Backend (Windows-correct):** new **`POST /api/library/sync/resolve`** ([main.py](main.py), plan A3) — body `{profile_id, resolutions:[{item_id, file_path, choice:"client"|"server", …device values}]}`. `choice:"client"` writes the device values reusing the `/sync/progress` merge shape (`completed` stays monotonic, sibling track keys preserved) and bumps `updated_at`; `choice:"server"` (and any unknown value, the safe default) writes nothing. Response carries the authoritative `server` values + `server_updated_at` per file; all writes under one `_lib_lock`. **App:** `_appFlushOfflineProgress` ([static/index.html](static/index.html)) now collects conflicts and opens the new `#syncConflictModal` resolution UI (`_appShowConflicts`/`_appRenderConflicts`/`_appApplyConflictResolutions`) — per-conflict mine-vs-server toggle, titles enriched from each download's persisted `meta`. On Apply it POSTs to `/sync/resolve`, then settles the device's `OfflineStore` record: a "keep mine" win just advances the watermark (`markSynced`); a "keep server" win adopts the server values via a **forced** `seedProgress` (new `force` flag overrides the unsynced-record guard so the device's own pending value is discarded). **All app glue is `isApp`-gated — the browser dashboard is byte-for-byte unaffected.** *Native + web changes: requires a full `./build-ipa.sh` rebuild (no `--fast`/`--no-sync`).* **Docs:** [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/STREAMING.md](docs/STREAMING.md), [docs/IOS_APP_PLAN.md](docs/IOS_APP_PLAN.md).

## [6.0.0-preview.3.1.3] — 2026-06-23
- **iOS: fixed offline watch progress never reaching the server on reconnect.** The native `OfflineStore` decided whether a record still needed pushing by comparing the device-clock `clientUpdatedAt` against the server-clock `baseSyncedAt` — so any clock skew (the device even slightly behind the host) made a freshly-watched offline event look already-synced, and `pending()` dropped it, so it was never POSTed to `/api/library/sync/progress`. (Pulling the server's state *into* the device was unaffected, which is why offline kept up with the server but never pushed back.) Replaced the cross-clock comparison with an explicit **`dirty`** flag: a local watch sets it, `markSynced` clears it on server acknowledgement, and `seedProgress` refuses to overwrite a `dirty` record. `baseSyncedAt` stays a server timestamp purely for the server's conflict detection. ([ios-app/ios/App/App/OfflineStore.swift](ios-app/ios/App/App/OfflineStore.swift)) *Native change: requires a full `./build-ipa.sh` rebuild.*

## [6.0.0-preview.3.1.2] — 2026-06-23
- **iOS: fixed the root cause that made *all* offline progress features inert — the `OfflineStore` native plugin was never registered.** Capacitor 8 does **not** auto-discover app-local plugins; they must be explicitly registered in `MainViewController.capacitorDidLoad()` via `registerPluginInstance` (see the comment there and [docs/GOTCHAS.md](docs/GOTCHAS.md)). Only `LocalMediaServer` and `BundleDownloader` were registered, so every `OfflineStore` call from JS failed and the `offlineStore()`/`_cap.os` helpers silently fell back to null — meaning offline progress was never saved, resumed, synced or seeded (this is why the `preview.3.1.1` resume fix appeared to have no effect: the store it relied on was unreachable). Registered `OfflineStore()` alongside the other two. ([ios-app/ios/App/App/MainViewController.swift](ios-app/ios/App/App/MainViewController.swift)) *Native change: requires a full `./build-ipa.sh` rebuild.*

## [6.0.0-preview.3.1.1] — 2026-06-23
- **iOS: fixed offline playback always restarting from the beginning after closing and reopening a show.** The offline player ([ios-app/www/downloads.html](ios-app/www/downloads.html)) set `<video>.src` (which begins loading immediately) and only *then* `await`ed the saved position before attaching the resume-seek listener — so on a fast local load `loadedmetadata` fired before the listener existed and the seek never happened. Now the saved position is read **first**, the seek listeners (`loadedmetadata`/`durationchange`/`canplay`, retried until `duration` is known) are attached **before** `src` is assigned, and saves are suppressed until the resume seek lands so the pre-seek t≈0 can't overwrite it. Offline resume now works across close/reopen. *Web change: requires a `./build-ipa.sh` rebuild (no `--fast`/`--no-sync`).*

## [6.0.0-preview.3.1.0] — 2026-06-23
- **iOS M3 follow-ups: offline resume now reflects online history, sync is bidirectional, and the offline Downloads picker shows series/episode metadata + posters.** Three gaps from `preview.3.0.0`:
  - **Offline resume didn't know where online viewing left off.** Progress only flowed device→server. Added the reverse: **`POST /api/library/sync/pull`** ([main.py](main.py)) returns the requesting profile's server progress for a list of downloaded files, and the new native **`OfflineStore.seedProgress`** ([ios-app/ios/App/App/OfflineStore.swift](ios-app/ios/App/App/OfflineStore.swift)) adopts it as the local baseline (without clobbering unsynced offline progress). The dashboard seeds at download time (the bundle-manifest now returns the file's `progress`) and again on every reconnect via `_appSeedFromServer`. So a show watched partway online resumes at that spot fully offline.
  - **Going back online didn't reliably push offline history.** Hardened the flush ([static/index.html](static/index.html) `_appFlushOfflineProgress`): it no longer early-returns past the server-seed step, refreshes the library only when the server actually advanced, and always runs the pull-seed afterward. Triggered on profile-select, profile-restore and the window `online` event.
  - **The offline picker was a flat filename list with no artwork.** The **`GET /api/library/{id}/bundle-manifest`** response now carries a `meta` block — series/title, season·episode, episode name, overview, and the **poster inlined as a data URL** (`_tmdb_image_data_url`, so it shows with no network) — plus the profile's `progress`. **`BundleDownloader`** ([ios-app/ios/App/App/BundleDownloader.swift](ios-app/ios/App/App/BundleDownloader.swift)) persists `meta` in its index and returns it from `list()`/`getLocal()`. The offline **Downloads** screen ([ios-app/www/downloads.html](ios-app/www/downloads.html)) now **groups downloads by series** with a poster, episode list (S·E + episode name), overview, per-episode **watch-progress bars** ("Watched" / "Resume hh:mm:ss"), and per-episode Play/Delete. *Native + web changes: requires a full `./build-ipa.sh` rebuild.* **Docs:** [docs/API.md](docs/API.md), [docs/STREAMING.md](docs/STREAMING.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/IOS_APP_PLAN.md](docs/IOS_APP_PLAN.md).

## [6.0.0-preview.3.0.0] — 2026-06-22
- **M3 of the iOS client app: offline watch history is kept and syncs back to the server on reconnect.** Episodes watched fully offline (in the bundled Downloads player) now **resume where you left off offline** and their progress **flows back to the host** the next time the app reconnects — device-only and close-position cases merge silently, and `completed` never regresses. **Backend (Windows-correct):** new **`POST /api/library/sync/progress`** ([main.py](main.py), plan A2) — a batch progress sync with real **conflict detection** using a per-file **`base_synced_at`** watermark the device carries. Per event: if the server hasn't moved since the device last synced that file it **applies** the device event; if both advanced it **auto-resolves** when positions agree within 60 s or either side completed (newest timestamp wins, `completed` monotonic); otherwise it returns a **conflict** (server + client) and writes nothing, deferred to the M4 resolution UI. Everything settled is returned in `applied` with a `server_updated_at` the device records as the file's new watermark. All writes happen under one `_lib_lock` acquisition; reuses the `update_progress` merge shape (`_norm_sub_sel`, preserved track keys). Verified against the full conflict matrix (device-only / auto-resolve / divergent-conflict / completed-monotonic / track-prefs-survive). **App:** new native **`OfflineStore`** plugin ([ios-app/ios/App/App/OfflineStore.swift](ios-app/ios/App/App/OfflineStore.swift)) — a durable, file-backed (Application Support, backup-excluded) progress log keyed by `(profileId, itemId, filePath)`, written by the offline player and drained by the dashboard. The offline Downloads player ([ios-app/www/downloads.html](ios-app/www/downloads.html)) now captures progress (timeupdate/pause/seeked/ended/pagehide) and **resumes from the saved offline position**. The dashboard ([static/index.html](static/index.html)) pushes the active profile to the store (`setProfile`), drains pending progress to `/sync/progress` on profile-select and the `online` event (`_appFlushOfflineProgress`), and falls back to the store when an online `saveProgress` fails. **All app glue is gated behind the Capacitor native-platform check — the browser dashboard is byte-for-byte unaffected.** Conflict-resolution UI is **M4** (next). *Native + web changes: requires a full `./build-ipa.sh` rebuild (no `--fast`/`--no-sync`); the new `OfflineStore.swift` was hand-added to the Xcode project.* **New:** [ios-app/ios/App/App/OfflineStore.swift](ios-app/ios/App/App/OfflineStore.swift). **Docs:** [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/STREAMING.md](docs/STREAMING.md), [docs/IOS_APP_PLAN.md](docs/IOS_APP_PLAN.md).

## [6.0.0-preview.2.1.2] — 2026-06-22
- **iOS: fixed "start() requires `path`" when playing from the offline Downloads screen.** `BundleDownloader.list()` wasn't returning each bundle's on-disk `dir`, so the offline player called `LocalMediaServer.start({ path: undefined })` and the native server rejected it. `list()` now includes `dir` (matching `getLocal()`), so Play works offline. ([ios-app/ios/App/App/BundleDownloader.swift](ios-app/ios/App/App/BundleDownloader.swift)) — *native change: requires a full `./build-ipa.sh` rebuild.*

## [6.0.0-preview.2.1.1] — 2026-06-22
- **iOS: the connect screen can no longer get stuck on "Connecting to…" with no way out.** The reachability probe now uses a hard timeout that **always** resolves within ~2.2 s (a stuck WKWebView TLS handshake could ignore `AbortController`), so an offline launch reliably falls through to the bundled **Downloads** screen instead of hanging. The connecting screen shows a clear status ("Checking connection…" → "Can't reach server — opening downloads…") and a prominent **View offline downloads** button that's tappable the whole time; the Change-server screen keeps its own offline button. ([ios-app/www/index.html](ios-app/www/index.html)) — *Note: web changes only reach the app via `npx cap sync`/`cap copy`; rebuild with `./build-ipa.sh` (no `--fast`/`--no-sync`) for them to take effect.*

## [6.0.0-preview.2.1.0] — 2026-06-22
- **iOS M2 fixes: downloads now show live progress, and downloaded shows actually play offline.** Two on-device bugs from `preview.2.0.0`:
  - **Download froze at 0% until an app restart.** `BundleDownloader` used a *background* `URLSession`, whose delegate callbacks (`didWriteData`/`didFinishDownloadingTo`) were batched by `nsurlsessiond` and not delivered until the next launch — so the UI sat at 0% and the bundle only "appeared" saved after a restart. Switched to a **foreground (default) `URLSession`** that reports progress + completion live, held alive across a brief backgrounding by a `UIApplication` background-task assertion. ([ios-app/ios/App/App/BundleDownloader.swift](ios-app/ios/App/App/BundleDownloader.swift))
  - **Couldn't play downloads offline — stuck on "connecting to server".** The dashboard UI (`static/index.html`) is served *by the host*, so with no connection the WKWebView can't load it at all and every offline feature was unreachable. Added a bundled **offline Downloads screen** ([ios-app/www/downloads.html](ios-app/www/downloads.html)) that lists saved bundles and plays them from the loopback `LocalMediaServer` (native HLS — audio/subtitle tracks in the player menu) with **zero network**. The connect shell ([ios-app/www/index.html](ios-app/www/index.html)) now **probes host reachability** on launch and routes to this screen when offline (and offers a "View downloads (offline)" button always). **App-only; the browser dashboard is unaffected.**

## [6.0.0-preview.2.0.0] — 2026-06-22
- **M2 of the iOS client app: offline download + fully-offline playback (the core feature).** Inside the native app you can now **download an episode to the device** and play it back with no host connection — in Airplane Mode — with working audio, subtitles, ABR and skip-intro, the exact same player UI as online. Each library row in the app gains a **Download** button: it resolves the host bundle (building it first if needed), then the native **`BundleDownloader`** ([ios-app/ios/App/App/BundleDownloader.swift](ios-app/ios/App/App/BundleDownloader.swift)) fetches every file of the `.offline_cache/<sha>/` HLS bundle via a **background URLSession** into a non-evictable Application Support dir (keyed by the cache sha; resumable; durable across an app kill). On play, [_lpLoadIndex](static/index.html) detects the local copy and serves it from the M1 **`LocalMediaServer`** (`http://127.0.0.1:<port>/`), swapping `master_url` and reading `meta.json` straight from the bundle — no host round-trip. **Backend (Windows-correct):** new **`GET /api/library/{item_id}/bundle-manifest`** ([main.py](main.py), plan A1) enumerates a built bundle's files (names + sizes + total) plus `duration_sec`/`audios`/`subtitles`/sidecar `subs`; returns **409 not_ready** (with `ondemand_only`) for un-prepped/JIT-only items so the app triggers a normal `/offline-prepare` + `/offline-job` poll first. Reuses the existing traversal-guarded `/offline-cache/<sha>/<file>` server for the transfers. **All web glue is gated behind a Capacitor native-platform check, so the browser dashboard is byte-for-byte unaffected.** Progress sync of offline watch history is **M3** (next). **New:** [ios-app/ios/App/App/BundleDownloader.swift](ios-app/ios/App/App/BundleDownloader.swift). **UI:** [static/index.html](static/index.html) — `appDownloadBundle`, `_appDlBtnHTML`, `_appFetchManifest`, `_appStartLocalPlayback`, offline `master_url` swap. **Docs:** [docs/API.md](docs/API.md), [docs/STREAMING.md](docs/STREAMING.md), [docs/GOTCHAS.md](docs/GOTCHAS.md), [docs/IOS_APP_PLAN.md](docs/IOS_APP_PLAN.md), [README.md](README.md).

## [6.0.0-preview.1.0.0] — 2026-06-18
- **M1 of the iOS client app: the app shell exists.** First milestone toward the **6.0.0** native iOS client (see [docs/IOS_APP_PLAN.md](docs/IOS_APP_PLAN.md)). A new Capacitor project lives in [ios-app/](ios-app/): a WKWebView shell with a Metro-styled **first-run Connect screen** ([ios-app/www/index.html](ios-app/www/index.html)) that captures the host address (+ an optional pairing token, reserved for M5), persists it, and navigates the webview to the existing dashboard — so online search/library/play/admin are byte-for-byte the browser experience (the Capacitor bridge persists across the navigation, exposing native plugins to the host page). Ships the **Gate 1b** deliverable: a dependency-free native **`LocalMediaServer`** plugin ([ios-app/ios/App/App/LocalMediaServer.swift](ios-app/ios/App/App/LocalMediaServer.swift)) — a `Network.framework` `NWListener` static HLS server (loopback-only, correct HLS MIME mirroring `_HLS_MIME`, byte-`Range` support) that serves a bundle dir at `http://127.0.0.1:<port>/` for native `<video>` HLS. A bundled sample fmp4 bundle + on-device self-test page ([ios-app/www/localtest.html](ios-app/www/localtest.html)) prove localhost playback + the ATS exception. Info.plist opens cleartext **only** for `127.0.0.1`/`localhost`; the remote host stays on HTTPS (install its CA profile on the device). **No host backend behavior changed** — `UI_VERSION` + the page badge carry the preview tag per the plan's [Versioning](docs/IOS_APP_PLAN.md#versioning). **New:** [ios-app/](ios-app/) (Capacitor project, web shell, Swift plugin). **Docs:** [docs/IOS_APP_PLAN.md](docs/IOS_APP_PLAN.md), [README.md](README.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.46.0] — 2026-06-21
- **Regular users can now turn On-Demand Stream Only on/off themselves — and admins can lock it.** Previously the per-show "on-demand only" flag (on-device playback streams just-in-time, no permanent HLS bundle is kept, ~no disk cost) was admin-only. The dashboard episode page now shows an **On-Demand Only: On/Off** toggle next to the title so any viewer can flip it on demand. To keep a show's setting fixed, the admin **Storage tab** gains a per-item **Lock/Unlock** control: when an item is locked, the user toggle still shows the current state but is disabled (lock glyph + tooltip) and the server refuses non-admin changes (HTTP 403). The admin can always change the flag regardless of the lock. The toggle is hidden where HLS isn't available (macOS). **Backend:** [main.py](main.py) — new `POST /api/library/{id}/ondemand-only` (user, lock-aware), `POST /api/admin/ondemand-only-lock`, shared `_apply_ondemand_only` helper, new `item["ondemand_only_locked"]` field surfaced in `/files`, `/api/library` and the admin list. **UI:** [static/index.html](static/index.html) — `#epOndemandBtn`, `toggleEpOndemandOnly()`, `_renderEpOndemandBtn()`; [static/admin.html](static/admin.html) — per-item Lock toggle + `toggleOndemandLock()`. **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/STREAMING.md](docs/STREAMING.md), [docs/FRONTEND.md](docs/FRONTEND.md).

## [5.45.0] — 2026-06-21
- **You can now rename a series (or a movie's title) to fix wrong artwork / episode info.** When a download was named something the metadata matcher can't resolve — e.g. an "AOT" folder that auto-matched the unrelated show *AOTU* — open the item's episode page and use the new **pencil button** next to the title. The name you type is exactly what drives the TMDb lookup (`_search_terms_for_item`), so renaming to "Attack on Titan" re-binds the poster, backdrop, overview and per-episode names to the correct show. For a series the whole group is renamed at once (the library grouping key is `item.series`) and each entry's cached metadata is dropped so it re-matches on next view; remembered per-series subtitle picks are re-keyed so they survive the rename. For a movie/one-off the item's own title is renamed instead. The hero repaints with the corrected art immediately. **Backend:** [main.py](main.py) — new `POST /api/library/{id}/rename` + `RenameReq`. **UI:** [static/index.html](static/index.html) — `#epRenameBtn`, `renameSeries()`. **Docs:** [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/FRONTEND.md](docs/FRONTEND.md).

## [5.44.5] — 2026-06-19
- **Hardened the Windows accept-loop patch so it can never again break server startup.** Belt-and-suspenders after the 5.44.4 regression: `winaccept_patch.py` now installs a thin wrapper around its hardened `_start_serving` that, if the reimplementation ever raises for *any* reason (a future CPython restructure, a renamed internal, an arg-binding mismatch), logs a warning and **falls back to the stock asyncio implementation** instead of letting the exception break startup. Worst case we lose the WinError-64 hardening and the listener behaves as plain asyncio — but the server always *starts*, never crash-loops. The gate is one `try/except` around a once-per-listening-socket startup call; the happy path is behaviourally unchanged. **Backend:** [winaccept_patch.py](winaccept_patch.py).

## [5.44.4] — 2026-06-19
- **Fixed: the v5.44.2 accept-loop patch itself crashed the server on Python 3.13 (the actual cause of the post-update crash loop).** `winaccept_patch.py`'s replacement `_start_serving` ended its signature in `**ssl_kw`, but CPython calls `loop._start_serving(...)` with **all arguments positional** and 3.11+ appends a 7th (`ssl_shutdown_timeout`) — and `**kwargs` doesn't absorb a surplus *positional* arg. So every call raised `TypeError: _start_serving() takes from 3 to 7 positional arguments but 8 were given`; the listener never armed, `http_srv.started` stayed `False`, the daemon raised "HTTP server (port 80) failed to start," and the supervisor crash-looped — the failed-start also leaks the already-bound socket, producing the `[WinError 10048]` "address already in use" on the immediate retry. (The v5.44.2 test only called the inner closure with 2 args, so it never exercised the real 7-arg convention.) The patch now learns the trailing param names from the **original** `_start_serving` signature at apply time, accepts them via `*trailing, **trailing_kw` (so no call can `TypeError` on any version), and forwards to `_make_ssl_transport` only the kwargs it actually accepts. Verified against both the 3.13 (7-positional) and 3.9 (6-positional) calling conventions, plus transient-error recovery and SSL-timeout forwarding. **Backend:** [winaccept_patch.py](winaccept_patch.py).

## [5.44.3] — 2026-06-19
- **Fixed: a second StreamLink launch could crash-loop forever and take the whole service down (reboot to recover).** The service log showed a healthy instance polling qBit/VLC (`200 OK`) interleaved with a *second* process failing to bind — `[Errno 10048] ... bind on ('0.0.0.0', 443): only one usage of each socket address ... permitted` (Windows `WSAEADDRINUSE`) and `HTTP server (port 80) failed to start`. Two instances were running at once and fighting for ports 80/443; Windows can't share a listening socket, so the loser thrashed (5 fast deaths → supervisor gives up → Task Scheduler relaunches the wrapper → repeat). A second launch arises with no malice — the `/SC ONLOGON` scheduled task firing on a fresh logon while the prior session's process is still alive, a manual `python run.py` next to the installed service, or a double `schtasks /Run` — and nothing guarded against it. Added a **single-instance guard**: `run.dashboard_already_serving()` confirms a live instance by hitting the public `/api/version` (not just "is the port open" — a foreign service on :80 is distinguished), and a newcomer **yields cleanly** instead of binding. Wired into `run.py` (refuses to start a duplicate) and the `daemon.py` service-wrapper template (checked at the top of every supervise iteration, so it also resolves the rarer two-wrappers-at-once race — the loser exits before `_MAX_FAST_DEATHS`). A genuine self-restart (our own server crashed, port now free) is unaffected. **After updating, re-run `python run.py --install`** to regenerate `streamlink_service.py` with the guard. **Backend:** [run.py](run.py) — new `dashboard_already_serving`; [daemon.py](daemon.py) — wrapper supervisor guard. **Docs:** [docs/RUNTIME.md](docs/RUNTIME.md), [docs/DAEMON_WATCHDOG.md](docs/DAEMON_WATCHDOG.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.44.2] — 2026-06-18
- **Fixed: the whole server could go permanently unresponsive on Windows after a client dropped mid-connection (reboot was the only recovery).** The logs showed `Accept failed on a socket` on `laddr=('0.0.0.0', 80)` with `[WinError 64] The specified network name is no longer available`, after which nothing could reach the dashboard until the machine was rebooted. Root cause is a long-standing CPython bug in the Windows `ProactorEventLoop`: when a client vanishes between `AcceptEx` completing and asyncio reading the result (a port scan, a half-open probe, a flaky Wi-Fi/VPN client, a NAT dropping the flow), the *per-connection* accept raises `OSError` — and asyncio's `_start_serving` handles that by **closing the listening socket**, so the server stops accepting *all* new connections forever. We can't switch to the SelectorEventLoop (it can't run `asyncio.create_subprocess_exec`, which ffmpeg/whisper/git depend on), so the new [winaccept_patch.py](winaccept_patch.py) reinstalls `_start_serving` with an accept loop that distinguishes a recoverable per-connection failure (log it, re-arm a fresh accept, keep the listener alive) from a genuine listening-socket failure (close, as upstream does). Applied at import in [main.py](main.py) so both `run.py` and the `daemon.py` service path get it; no-op on Linux/macOS. **New:** [winaccept_patch.py](winaccept_patch.py). **Docs:** [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.44.1] — 2026-06-18
- **Fixed: setting a series to "on-demand stream only" no longer freezes the site for everyone.** Enabling the flag triggered a forced full-cache inventory walk plus a bundle purge inline in the request; on a large `.offline_cache/` that walk holds the GIL long enough (even though offloaded to a thread) that the event loop went laggy — other users couldn't load the dashboard or act on it while it ran. Now the request does only the fast work inline (flip the flag, save, cancel in-flight prep jobs) and returns immediately; the heavy inventory walk + bundle deletion runs in a **detached background task** (`_purge_ondemand_bundles` via the new `_spawn_bg` helper), so the toggle takes effect instantly and disk space is reclaimed shortly after without blocking anyone. The response now returns `purging: true` instead of `bytes_freed`/`bundles_purged`, and the admin toast reads "reclaiming disk space in the background." Also re-synced the drifted `UI_VERSION` constant (was `5.16.1`) with the page badge. **Backend:** [main.py](main.py) — `admin_set_ondemand_only`, new `_purge_ondemand_bundles` + `_spawn_bg`. **UI:** [static/admin.html](static/admin.html) — `toggleOndemandOnly` toast.

## [5.44.0] — 2026-06-18
- **Search now has a Categories filter that hides results from unselected content types.** Next to the **Sources** picker, a new **Categories** picker lists the content types the allowed indexers carry (Movies, TV, Music, Books, Games, Apps, XXX, Other). Untick one — e.g. **XXX** — and no indexer returns that type: the selection is translated into a Jackett `Category[]` filter applied to every per-indexer query (and the aggregate fallback), so it's enforced server-side, not just hidden in the UI. The selection is remembered per profile (`localStorage`), defaults to all-on, and only appears when there are 2+ filterable categories. `anime` isn't offered separately — it has no distinct Torznab bucket and is delivered within Movies/TV. The filter intersects with the admin `INDEXER_CATEGORIES` base restriction and the per-profile source allowlist, so a user can only ever narrow within what the admin permits. **Backend:** [main.py](main.py) — `/api/search` gains a `categories` param, new `_CONTENT_TYPE_TO_TORZNAB` map + `_resolve_search_categories` / `_torznab_top_bucket` helpers. **UI:** [static/index.html](static/index.html) — `#catWrap` picker, `rebuildSearchCategories` / `renderCategoryList` / `toggleCategory` / `setAllCategories` / `_selectedCategoriesParam`. **Docs:** [docs/API.md](docs/API.md).

## [5.43.2] — 2026-06-18
- **Fixed: dismissable/collapsable notice banners no longer let taps fall through to the UI underneath.** The server-attention and host-performance banners floated at z-70 with `pointer-events:none` so they couldn't block the on-device player buttons beneath them — but that also meant a tap aimed at the banner (or just near a button under it) passed straight through and could trigger an unintended control. Now that those banners are dismissable (the × on the server-attention banner) or collapsable (the chevron on the host-performance banner), the user always has a way to clear them, so a shown/expanded banner captures clicks instead of leaking them: `#serverAttentionBanner` and the expanded `#perfBanner` get `pointer-events:auto`. The collapsed perf banner stays click-through (it's just a transparent chevron tab), and the auto-dismissing `#globalToast` is unchanged. **UI:** [static/index.html](static/index.html) (banner pointer-events CSS).

## [5.43.1] — 2026-06-18
- **Fixed: "Play All" said "Nothing to play" after finishing an episode without starting the next.** When a viewer completed an episode (e.g. E3) and didn't begin the next, the backend's `find_resume_hint` correctly returned the next un-completed episode (E4) — but at `pct: 0`, so the frontend treated it as "no progress." `resumeLibraryItemWithChooser` only carried the resume `file_path` when `pct > 3`, so the device ("On Device") path lost the target file and fell into a fallback that read `item.files` from `_libCache` — which only carries `first_file` — leaving the list empty → "Nothing to play." Now the chooser always carries the backend's resume hint (the next un-completed episode, or the first file when all are watched) as the starting file, seeking into it only when mid-episode; and the empty-hint fallback fetches the ordered file list from `/api/library/{id}/files` instead of the cache. **UI:** [static/index.html](static/index.html) — `resumeLibraryItemWithChooser`, `pcChoose`.

## [5.43.0] — 2026-06-18
- **The dashboard now shows which profile started the current playback, and progress stays linked to that profile.** Playback on the shared VLC host is owned by whoever pressed Play/Continue — progress, watched-marks, and resume have always been attributed to that profile (`state.library_profile_id`), and the shared fullscreen controls (pause/seek/next/prev) deliberately carry no profile, so a different viewer driving them can never reattribute the session to themselves. What was missing was *visibility*: nobody could tell whose session/progress they were affecting. A **"started by" chip** (colored profile dot + name, "· you" when it's your own) now renders in both the footer status row and the fullscreen-controls overlay whenever a library item is playing, so a second viewer knows their controls feed the starter's progress, not their own. **Backend:** [main.py](main.py) — new `state.library_profile_name`/`library_profile_color`, `_set_playback_owner` helper, owner fields added to `state_snapshot` and cleared on stop/background. **UI:** [static/index.html](static/index.html) — `renderPlaybackOwner`, `#ownerBadge`/`#fcOwner`, optimistic owner seed in `_optimisticBuffering`. **Docs:** [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/BACKEND.md](docs/BACKEND.md), [docs/API.md](docs/API.md), [docs/FRONTEND.md](docs/FRONTEND.md).

## [5.42.1] — 2026-06-18
- **Fixed: indexer names were invisible in the Sources picker on mobile portrait.** Each source row laid the indexer name (`flex-1 min-w-0`) and its content-type chips (`flex-shrink-0`) side by side; on a narrow portrait viewport the non-shrinking chips claimed the full width and squeezed the name to zero, so only the chips showed (desktop and landscape had room for both). The row now stacks vertically below the `sm` breakpoint — checkbox + name on the first line, chips wrapping (indented to align under the name) on the next — and keeps the original single-line side-by-side layout from `sm:` up. **UI:** [static/index.html](static/index.html) (`renderSourceList`).

## [5.42.0] — 2026-06-18
- **iOS now prerolls a much larger forward buffer for on-device playback.** On iPhone/iPad, hls.js can only use ManagedMediaSource, and the OS caps the forward buffer at ~15–30 s (it fires `endstreaming` ≈30 s in, which hls.js answers by pausing the fragment loaders) — so an iPhone on a fast link buffered far less ahead than every other platform, where the full 180 s target is honored. We now override the hls.js instance's `pauseBuffering` **on true iOS devices only** so it ignores that premature OS stop and keeps prerolling until the forward buffer reaches a larger iOS target (`IOS_PREROLL_TARGET_SECS`, 120 s), then honors it; the back-buffer is trimmed to 30 s on iOS to offset the extra retained memory, and hls.js's own `QuotaExceededError` backoff remains the safety valve under real memory pressure. Non-iOS playback is unchanged. **UI:** [static/index.html](static/index.html) — new `_isIOSDevice` / `_lpFwdBuffer` / `_lpInstallIosPreroll` helpers, `IOS_PREROLL_TARGET_SECS`, conditional `backBufferLength`. **Docs:** [docs/GOTCHAS.md](docs/GOTCHAS.md), [docs/STREAMING.md](docs/STREAMING.md).

## [5.41.0] — 2026-06-17
- **New device-local setting to disable the locked progress bar.** Settings → This Device gains a **Locked Progress Bar** toggle. It's **on by default** (the seek bar keeps its hold-to-unlock accidental-click guard). Turning it off makes the bar permanently unlocked on that device — a single tap seeks, no hold gesture. The preference is stored in this browser's `localStorage` (`streamlink_seeklock`), never the server/profile, and applies live. **UI:** [static/index.html](static/index.html) — new `_seekLockEnabled` state honoured by `lockSeekBar`, `toggleSeekLock`, `psSeekLock` checkbox. **Docs:** [docs/FRONTEND.md](docs/FRONTEND.md).

## [5.40.0] — 2026-06-17
- **Search now has a per-user "Sources" picker, and admins can restrict which indexers a profile may search.** Two related additions:
  - **User search source picker.** A collapsible **Sources** control under the search box lists the indexers this profile is allowed to search, each tagged with the content types it carries (Movies / TV / Anime / Music / Books / Games / XXX / …, derived from the indexer's Torznab `caps`). Tick a subset to narrow the search; the choice is remembered per profile in `localStorage`. The picker only appears when there are 2+ sources to choose between. `doSearch` passes the chosen subset as `&indexers=` and the active `&profile_id=`.
  - **Admin per-profile allowlist.** The **Profile PINs** admin tab gains a **Search Sources** row per profile (All / Limited), opening a modal that lists every configured indexer with its content-type chips. Saving an empty/full selection leaves the profile unrestricted; a strict subset limits it. Enforced server-side: `/api/search` intersects the profile's allowlist with the user's selection, so a profile can never search outside what the admin permits.
- **Backend:** [main.py](main.py) — new `_indexer_content_types` / `_configured_indexers_full` / `_profile_allowed_indexers` helpers; `/api/search` gains `indexers` + `profile_id` params and per-indexer filtering; new `GET /api/search/indexers`, `GET /api/admin/indexers/catalog`, `POST /api/profiles/{id}/set-indexers`; `/api/profiles` now returns `allowed_indexers`. **UI:** [static/index.html](static/index.html) (source picker), [static/admin.html](static/admin.html) (per-profile allowlist modal). **Data:** `library.json` profiles gain an optional `allowed_indexers` list. **Docs:** [docs/API.md](docs/API.md), [docs/ADMIN.md](docs/ADMIN.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/FRONTEND.md](docs/FRONTEND.md).

## [5.39.0] — 2026-06-17
- **Every notice banner is now dismissible (the host-performance banner was already collapsible).** The dashboard's **server-attention banner** (missing env keys, in-flight update, degraded indexers) and the admin panel's **indexer-health banner** each gained a `×` dismiss button. Dismissal is keyed to the *current* notice — dismissing "Server needs admin attention: OpenAI Key" hides only that, and the banner re-appears on its own if the condition changes to a different one (e.g. an update starts, or indexer health flips degraded↔healthy). The host-performance banner keeps its existing chevron collapse/expand. **UI:** [static/index.html](static/index.html) (`renderServerAttention`, new `dismissServerAttention`/`serverAttentionDismissedKey`), [static/admin.html](static/admin.html) (`_renderIndexerHealth`, new `dismissIndexerHealthBanner`/`_indexerHealthDismissedKey`).

## [5.38.1] — 2026-06-17
- **The "Open Jackett" admin link now points at the server's primary adapter address — the same IP shown in the Network Adapter menu — instead of whatever hostname the admin happened to connect on.** When the indexer URL is loopback (`http://localhost:9117`), `_jackett_dashboard_url` now swaps in `active_ip` from `_network_status` (the admin-selected primary adapter, with the same offline-fallback resolution), keeping Jackett's scheme + port; it falls back to `request.url.hostname` only if no primary address resolves. The function is now `async` (it reads the library to resolve the adapter) and its three callers (`/api/admin/indexers/health`, `/api/admin/flaresolverr`, indexer test) `await` it. **Backend:** [main.py](main.py) (`_jackett_dashboard_url`).

## [5.38.0] — 2026-06-17
- **"Use My Computer" pause durations are now 30 Min, 2 Hours, and Until Disabled** (previously 60 Sec / 2 Min / Until I Stop) — longer presets better match actually reclaiming the desktop for a while. The countdown label now renders an `h:mm:ss` form once over an hour remains (was always `m:ss`). **UI:** [static/index.html](static/index.html) (`wcButtons`, `renderWindowControl`).

## [5.37.0] — 2026-06-17
- **The episode page now opens on the season you actually left off in, and scrolls the season bar so that season is centred (visible) instead of off-screen.** Two related fixes for shows with many seasons, especially on mobile: **(1)** When nothing is currently playing, `pickDefaultSeason` now lands on the season of the **most-recently-watched** episode (latest `progress.updated_at`) rather than the *first season with any unwatched episode*. An early episode left unwatched on purpose — skipped, or saved for later — no longer drags the picker back to an earlier season. The currently-playing-file rule still wins when something is live, and first-unwatched/first-available remain as fallbacks. **(2)** `renderEpSeasonTabs` now horizontally centres the active season tab in the strip (`_scrollActiveSeasonIntoView`, via `requestAnimationFrame` so layout is settled on first open) — with a dozen seasons the one the viewer is on was otherwise scrolled off-screen and hard to find on a phone. Only the strip's own `scrollLeft` is touched, never the page. **UI:** [static/index.html](static/index.html) (`pickDefaultSeason`, `renderEpSeasonTabs`, `_scrollActiveSeasonIntoView`). **Docs:** [docs/FRONTEND.md](docs/FRONTEND.md).

## [5.36.2] — 2026-06-17
- **Softened the alarming "⚠ Skip partial" library chip — it no longer looks like an error, and now says how many episodes are affected.** Smart Skip legitimately produces no intro/credits points for episodes that simply have no intro or no end-credits sequence, so the amber warning chip overstated a non-problem. The chip is now a neutral grey info pill (`ⓘ No skip on N/M`) whose tooltip explains that this is usually normal, with `N` = episodes lacking skip points and `M` = total analyzable episodes in the item. **Backend:** [main.py](main.py) (`_item_skip_summary` now returns `{status, affected, total}`; `_item_skip_status` kept as a back-compat string accessor; library payload emits `skip_affected`/`skip_total` alongside `skip_status`). **UI:** [static/index.html](static/index.html) (`renderLibrary` partial-skip chip). **Docs:** [docs/ANALYZER.md](docs/ANALYZER.md).

## [5.36.1] — 2026-06-17
- **On-device playback no longer keeps "buffering" forever after a brief connection drop (tunnel, Wi-Fi↔LTE swap).** When the link silently dies mid-segment-fetch, the in-flight fragment XHR *hangs* over the now-dead TCP connection instead of erroring — so hls.js fires **no** fatal `NETWORK_ERROR` (the existing reconnect loop never engages) and just sits on the dead request until its 45 s `fragLoadingTimeOut`, continuing to "buffer" long after the connection is back. Stop+restart cured it instantly because that built a fresh request on a live socket. Two new pieces close the gap (both in [static/index.html](static/index.html)): a **stall watchdog** (`_lpStallWatch`, polled every 3 s) that — once playback has started — aborts the hung fragment (`hls.stopLoad()` + `startLoad(-1)`; Safari reloads at position) and re-requests from the current position after the playhead has been starved past a mode-dependent threshold (9 s bundle, 35 s on-demand — above the server's 30 s `OD_SEG_WAIT_TIMEOUT` so a legitimate just-in-time cold seek is never kicked); and an **`online`-event kick** (`_lpKickIfStalled` via `_lpNetOnline`) that aborts+refetches the moment the OS reports connectivity restored if playback is starved. New `lp.everPlayed` arms the watchdog so initial buffering / a first-segment encode isn't mistaken for a stall. **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.36.0] — 2026-06-16
- **On-demand (just-in-time) playback now carries the source's embedded subtitles.** Previously the JIT path returned `subtitles: []` — only on-disk sidecar `.srt`s showed, so a show with subs baked into the MKV looked subtitle-less whenever playback used on-demand (an un-prepped file, or the new on-demand-only mode). `stream-ondemand` now returns a `subtitles[]` list (`_od_subs_meta` — the same `sub_<i>.vtt` shape the full bundle uses, image subs filtered) and the on-demand file endpoint extracts each embedded text stream to WebVTT **lazily on first fetch** (`_od_extract_sub`, cached in the session dir and reaped with it). The client attaches them with the identical `<track>` machinery as the bundle. **Note:** the HLS *trim* tool does **not** remove subtitles — it only edits video rungs and preserves every `sub_<i>.vtt` + `meta.json subtitles[]`; the subtitle loss was this on-demand gap. **Backend:** [main.py](main.py) (`_od_text_subs`/`_od_subs_meta`/`_od_extract_sub`/`_OD_SUB_RE`, `session["sub_idxs"]`, `sub_<i>.vtt` branch in `ondemand_file`, `subtitles` in `stream_ondemand`). **UI:** [static/index.html](static/index.html) (`_lpLoadIndex` uses `od.subtitles`). **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/ADMIN.md](docs/ADMIN.md).

- **New admin "Storage & Compression" tab with three space-saving tools.** Reclaim disk both by shrinking sources and by slimming the on-device (HLS) bundles. **(1) Default On-Device Resolutions** — checkboxes choose which adaptive-quality down-rungs new stream preps build (the source rung is always included; default unchanged at 720p + 480p). The ladder grew 1080p + 360p options; the selection persists to `library.json → settings.admin_overrides.hls_ladder` and `_hls_video_variants` filters to it. Affects **future preps only**. **(2) Compress Source Files** — re-encodes sources in place to save space, modelled on the File Repair re-encode: pick a scope (whole library or one item), a **codec** (H.264 or HEVC — explained inline), and a **strength** (Light / Balanced / Maximum presets, or an **Advanced** raw-CRF slider). An **Estimate Savings** button projects the reclaimed bytes (source re-encode + the HLS bundle freed on replace) before you commit; a file is replaced **only** when the result decodes cleanly **and** is actually smaller, then its stale bundle is purged so on-device playback re-preps from the smaller source. Like Repair, this is lossy/irreversible and **stops a torrent-backed file seeding** (caveat surfaced in the UI). NVENC-accelerated when present; runs one file at a time at below-normal priority with live progress + Stop. **(3) Drop HLS Resolutions** — trims surplus down-rungs out of **already-prepped** bundles immediately (deletes their playlists/segments, rewrites `master.m3u8` + `meta.json`), with a dry-run **Estimate** of bytes freed — no re-encoding. The Storage & Compression card lives on its **own top-level admin tab** (between Offline Cache and Background), and a running compression shows live on the **Activity** tab (category `Compression`). **Robust playback lock:** because the in-place `os.replace` fails on Windows while anything holds the source open (`WinError 5` — hit when watching an episode mid-compress), a file being compressed is added to `state.compressing_paths` and **every** playback/read path refuses it with HTTP 423 (`_assert_not_compressing` on VLC `/play`, `/offline-prepare`, `/stream-ondemand`, `/clip`, and the start of `_run_offline_job`); the compressor also stops any in-flight playback first (`_free_file_for_compression`: stops VLC, tears down on-demand sessions, terminates+re-queues prep) and retries the replace across handle-release latency, releasing the lock in a `finally`. **Backend:** [main.py](main.py) (`HLS_ABR_LADDER` 1080/720/480/360, `_hls_ladder_heights`, `_hls_video_variants(heights)`, `_build_hls_ffmpeg_args(ladder_heights=…)`; `_compression_params`/`_compress_one_file`/`_run_file_compression`/`_compress_estimate`; `state.compressing_paths` + `_compress_lock_key`/`_is_compressing`/`_assert_not_compressing`/`_free_file_for_compression`; `_trim_one_bundle`/`_hls_trim_bundles`; `CompressFilesReq`/`CompressEstimateReq`/`HlsTrimReq`; `GET`/`POST /api/admin/compress-files`, `/compress-files/stop`, `/compress-estimate`, `/hls-trim`; `hls_ladder` on `/api/admin/settings`; `Compression` row in `_activity_snapshot`). **UI:** [static/admin.html](static/admin.html) (Storage tab + `loadStorageCompression`/`saveHlsLadder`/`estimateCompression`/`startCompression`/`stopCompression`/`estimateHlsTrim`/`runHlsTrim`). **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/STREAMING.md](docs/STREAMING.md), [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).
- **Drop HLS Resolutions got a per-show Scope.** The trim tool now takes a `scope` (whole library or one item) — matched against each bundle's `meta.json` src — so you can slim one show's bundles without touching the rest. **Backend:** `_hls_trim_bundles(scope=…)` / `_trim_one_bundle(allowed_srcs=…)` + `_norm_path`, `HlsTrimReq.scope` ([main.py](main.py)). **UI:** scope dropdown + `_loadStorageItems` populating both compression and trim selects ([static/admin.html](static/admin.html)).
- **New "On-Demand Stream Only" per-show toggle (Storage tab).** Mark a show so on-device (browser) playback **always** uses the just-in-time pipeline and **no permanent HLS bundle is ever built** — segments are transcoded as you watch and reaped after, so it costs ~no disk. Enabling cancels any in-flight prep for the item and **purges its existing bundles** to reclaim space now; the source file is kept (JIT + VLC need it). **Playing on the TV (VLC) is unaffected** — it reads the source directly. Persists as `item["ondemand_only"]`, mirrored into `state.ondemand_only_items` (seeded at lifespan, rebuilt on toggle), and gates every prep-creation chokepoint (`_maybe_start_prep_job`, `_start_admin_prep_job`, the inline `/offline-prepare` job — which returns `{ready:false, ondemand_only:true}` so the client falls to JIT — plus a `_run_offline_job` backstop). **Backend:** [main.py](main.py) (`OndemandOnlyReq`, `_rebuild_ondemand_only_items`, `GET`/`POST /api/admin/ondemand-only`, the gates above). **UI:** [static/admin.html](static/admin.html) (On-Demand Stream Only section + `loadOndemandOnly`/`toggleOndemandOnly`). **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/STREAMING.md](docs/STREAMING.md), [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md).

## [5.35.0] — 2026-06-16
- **The watchdog now monitors FlareSolverr and restarts it if it dies** — previously it was launched once at startup (`run.py`) but never supervised, so a crash left Jackett unable to scrape Cloudflare/DDoS-Guard indexers until a manual restart. FlareSolverr is added to the watchdog's always-kept-alive `plain_specs` **only when it's installed** (`.env` has a `_FLARESOLVERR_BIN` pointing at an existing file); if it was never installed the watchdog ignores it. It's a plain (non-VPN-gated) service with a port check on `FLARESOLVERR_URL` (default `8191`), a 30 s startup timeout (it boots a headless Chrome, slow to bind), and standard exponential back-off. Because FlareSolverr reads its bind `HOST`/`PORT` from the environment rather than argv, `ServiceSpec` gained an optional `launch_env` that `_launch_bg` applies to the child process — host/port derived from `FLARESOLVERR_URL` exactly as `run.py`'s `start_flaresolverr()` does (`localhost`/`::1`/`0.0.0.0` normalised to `127.0.0.1`). This also covers the system-service path, where the watchdog is the only thing launching services at boot. **Watchdog:** [watchdog.py](watchdog.py) (`find_flaresolverr`, `ServiceSpec.launch_env`, `_launch_bg(env=…)`, FlareSolverr spec in `_build_specs`). **Docs:** [docs/DAEMON_WATCHDOG.md](docs/DAEMON_WATCHDOG.md).

## [5.34.1] — 2026-06-16
- **The "Host is overloaded (network)" banner no longer fires during a transfer's speed ramp-up or right after the server boots.** The network-health classifier flagged *degraded* on **any single** packet drop in the 5 s sample window (`d_err > 0`) and *overloaded* at an absolute count of 50 drops — with no regard for how much traffic was flowing. A few drops while TCP probes for bandwidth on a fast transfer, or while the NIC settles after boot, are normal, so the banner appeared constantly. Network status is now classified by **packet-loss rate** (drops ÷ packets this window), only computed once real traffic has flowed (`≥2000` packets and `≥20` drops, so a trickle of drops can't produce a misleading rate), and only surfaced after it **persists for two consecutive samples** (~10 s) — ramp-up/boot spikes clear in one sample and never reach the banner. Thresholds: ≥2% sustained loss → degraded, ≥8% → overloaded; recovery is instant. **Backend:** [main.py](main.py) (`system_monitor_loop` net classification, `_net_bad_streak` + `_NET_MIN_PKTS`/`_NET_MIN_ERRS`/`_NET_LOSS_DEG`/`_NET_LOSS_OVER`/`_NET_CONFIRM`).

## [5.34.0] — 2026-06-15
- **A downloading series now shows the clean episode UI instead of the raw per-file download menu, single-file titles (movies / one-episode series) got a clickable info page, and you can bulk-delete episodes to reclaim disk space.** Three connected Library-tab changes, all routed through the existing full-screen episode page (`#episodePage` / `openEpisodePicker`):
  - **Downloading series → episode UI.** A multi-episode item that's still downloading no longer renders the inline "Files" expander (per-file priority rows). Its card now carries an **☰ Episodes** button that opens the same Netflix-style episode page used for finished items — which already supports in-flight downloads (live per-episode download %, the per-season Download/Prep scheduling bar, ⊘-skip / 🌙-idle states). The card keeps its Idle/Resume + When Ready buttons. The Files expander is retained only for the finished-partial case (`download_partial && torrent_hash`).
  - **Movie / single-episode info page.** The title/meta block of every ready or downloading library card is now a clickable surface (`.lib-tile-open`, wired via data-attrs after render so quoted titles are safe) that opens the info page. For a **single-file** item the page renders in **movie mode** (`epIsMovie`, `_epApplyMovieChrome`, `_epMoviePanel`): the hero shows poster / overview / metadata, and a detail panel shows watch + download status plus the action set (Play/Resume in the sticky bar, Download-to-device, ⚡ Prep for streaming, 🗑 Delete; for a still-downloading movie, the Download-to-host + Now/Idle controls). No episode list / checkboxes.
  - **Bulk delete to free space.** The episode page's bulk-action row gained a red **🗑 Delete (N)** chip; the movie panel has a matching Delete. Deleting removes the files' bytes from disk **and marks them Skip** so qBittorrent won't refetch them — the rows stay re-downloadable (a ⬇ Download button reappears). New endpoint `POST /api/library/{item_id}/delete-files` (`DeleteFilesReq`) sets the files to `skip`, `unlink`s them, purges their cached HLS bundle (same pattern as `/recheck`), reconciles qBit priorities, and reports `freed_bytes`. `get_item_files` now grounds a **skipped** file's completeness in actual disk existence (qBit keeps `progress=1.0` after an out-of-band delete until a recheck), so a freed file immediately shows "not downloaded" rather than masquerading as playable.
  - **Backend:** [main.py](main.py) (`DeleteFilesReq`, `delete_item_files`, skip-file disk-truth branch in `get_item_files`). **UI:** [static/index.html](static/index.html) (`renderLibrary` Episodes button + clickable tile, `_epApplyMovieChrome`/`_epMoviePanel`/`epIsMovie`, `epDeleteFiles`/`epDeleteSelected`, Delete chip + `#epMoviePlayBtn`). **Docs:** [docs/FRONTEND.md](docs/FRONTEND.md), [docs/API.md](docs/API.md), [docs/BACKEND.md](docs/BACKEND.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.33.1] — 2026-06-15
- **Fixed the dashboard freezing when many shows are fingerprinted at once (Smart Skip), even though the host and Remote Desktop stayed responsive.** The analyzer's blocking ffmpeg/fpcalc decodes ran through `asyncio.to_thread`, which shares the event loop's single default thread pool — the **same** pool `get_library()` / `put_library()` use. Each fingerprint parks its worker for the whole decode (head 6 min, tail 10 min of audio), so kicking off Analyze on several series flooded that pool; library reads/writes — which the progress tracker, download monitor, and essentially every HTTP handler depend on — then queued behind the decodes and the UI stalled while the event loop itself was never blocked. Analyzer subprocess work now runs on a **dedicated** thread pool (`analyzer._FP_EXECUTOR` via `_fp_thread`), keeping the default pool free for library I/O. As defence-in-depth, a process-wide `_analysis_gate` semaphore (`ANALYSIS_CONCURRENCY=2`) now caps how many series fingerprint concurrently so a bulk Analyze drains as a queue instead of oversubscribing the host with parallel decodes + match processes. **Analyzer:** [analyzer.py](analyzer.py) (`_FP_EXECUTOR`, `_fp_executor`, `_fp_thread`; fingerprint calls re-routed off `asyncio.to_thread`). **Backend:** [main.py](main.py) (`ANALYSIS_CONCURRENCY`, `_analysis_gate`, `_run_series_analysis` gate). **Docs:** [docs/ANALYZER.md](docs/ANALYZER.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.33.0] — 2026-06-15
- **Added optional FlareSolverr support for Jackett — install it and wire Jackett to it from the admin Indexers tab.** FlareSolverr is a standalone proxy that solves Cloudflare / DDoS-Guard browser challenges so Jackett can scrape indexers that sit behind them. It's fully optional (most indexers don't need it). A new **FlareSolverr** card on the **Indexers** tab shows whether it's *installed*, *running*, or *not installed*; a one-click **Install FlareSolverr** downloads the portable bundle (reusing the Optional Components installer) and auto-launches it; a **Start** button relaunches it if it's stopped. The card displays the **FlareSolverr API URL** with a Copy button and explains that StreamLink **can't** configure Jackett for you — you must paste that URL into Jackett's own *Configure Jackett → FlareSolverr API URL* setting by hand (with an **Open Jackett ↗** link). Install runs on the host with a live progress bar and persists across auto-updates (the binary lands in `tools/`, so `detect_tools()` + `merge_tool_paths()` keep `.env` pointing at it). FlareSolverr also appears in **System → Optional Components**. Prebuilt binaries are **Windows + Linux** only (macOS: run via Docker). `run.py` launches it on startup when installed (binds to the `FLARESOLVERR_URL` host/port via FlareSolverr's `HOST`/`PORT` env vars); a new `FLARESOLVERR_URL` setting (default `http://localhost:8191`) controls the address. **Setup:** [setup.py](setup.py) (`flaresolverr_candidates`, `_resolve_flaresolverr_url`, `FLARESOLVERR_*` constants, `detect_tools`/`write_env`/`merge_tool_paths` → `_FLARESOLVERR_BIN`). **Runtime:** [run.py](run.py) (`start_flaresolverr`, `launch_bg(env=…)`). **Backend:** [main.py](main.py) (`Settings.flaresolverr_url`, `flaresolverr` component install branch + status, `_flaresolverr_bin_path`/`_flaresolverr_running`/`_spawn_flaresolverr`, `GET /api/admin/flaresolverr`, `POST /api/admin/flaresolverr/start`). **UI:** [static/admin.html](static/admin.html) (FlareSolverr card on the Indexers tab, `loadFlaresolverr`/`_renderFlaresolverr`/`installFlaresolverr`/`startFlaresolverr`/`copyFsUrl`). **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/SETUP.md](docs/SETUP.md), [docs/RUNTIME.md](docs/RUNTIME.md), [docs/API.md](docs/API.md).

## [5.32.1] — 2026-06-15
- **The show-card Play button now continues *forward* from the last episode you watched instead of jumping back to one you skipped.** Within a multi-episode item (e.g. a season pack), if you skipped ep5 and watched ep6 partway, Play correctly resumes ep6 — but once ep6 was *finished*, the resume hint walked the file list from the start and offered ep5 (the first not-completed file) again, instead of advancing to ep7. `find_resume_hint` now scans **from the last-watched file onward** first: it resumes an in-progress `last_file`, else returns the first not-completed episode at/after it (skipping `last_file` only when completed), so finishing ep6 advances to ep7. It only falls back to the global "first un-watched anywhere" walk when nothing ahead is left (which still surfaces a genuinely-skipped earlier gap once the rest of the series is done, and remains the cold-start path). **Backend:** [main.py](main.py) (`find_resume_hint`). **Docs:** [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.32.0] — 2026-06-14
- **Smart Skip no longer cuts off the end of a show by guessing at credits.** Credit detection sometimes triggered early — on an episode whose usual end-credits sequence was absent or shorter, the audio-fingerprint matcher latched onto the only *other* recurring audio it could find (a stinger/repeated gag) and auto-skip then advanced there, losing the actual ending. Credits time now comes **only** from a confirmed cross-episode fingerprint match, with **no fabricated fallback**: the old flat-`duration×0.92` guess and the black-frame detector are both **removed**. A matched outro run is accepted only when it **starts late enough** (`≥ duration × MIN_CREDITS_PCT`, 0.75) **and runs to ~the end of the file** (`credits_end ≥ duration − OUTRO_END_MARGIN_SEC`, 120 s) — real credits reach the end, a recurring mid/late cue is followed by more content and is rejected. A new consensus pass (`_filter_cluster_consensus`) prunes a cluster member that matched the season on a *different* region than the real credits (anchor-offset deviation > `CLUSTER_OFFSET_TOL_FRAMES`, ~8 s), applied to both intros and outros. Alternate credits shared by **multiple** episodes are still detected (they form their own cluster); an episode with no genuine match — or a single file with no peer episodes — now gets **no** skip (a `no_skip_points` "Skip unavailable" state) rather than an early guess. `ANALYZER_VERSION` is bumped 3 → 4, so every already-processed series is **re-analyzed automatically** by the idle maintenance loop / next prep (manual admin edits are untouched). **Analyzer:** [analyzer.py](analyzer.py) (`_filter_cluster_consensus`, fingerprint-only credits acceptance in `analyze_series`, removed `_detect_blackframe`/`CREDITS_FALLBACK_PCT`/`ERR_TOO_SHORT`, new `MIN_CREDITS_PCT`/`OUTRO_END_MARGIN_SEC`/`CLUSTER_OFFSET_TOL_FRAMES`, `ANALYZER_VERSION = 4`). **Docs:** [docs/ANALYZER.md](docs/ANALYZER.md), [docs/GOTCHAS.md](docs/GOTCHAS.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md).

## [5.31.1] — 2026-06-14
- **Fixed search still failing (and showing no results) when only *some* indexers are broken.** The 5.31.0 partial-failure handling read Jackett's per-indexer error block but still issued one request to the aggregate `/indexers/all/results` endpoint — and that endpoint waits for the *slowest* indexer, so a single hung/broken one blew our client timeout and the `502` discarded **every** result, including the good ones. `/api/search` now enumerates the configured indexers (`_list_configured_indexers`) and queries each **independently and concurrently** (`/indexers/{id}/results`, 12 s per-indexer timeout); a failing indexer only loses its own results and we merge + return whatever came back. It only surfaces an error when **every** indexer fails. Falls back to the aggregate endpoint if the indexer list can't be fetched. Health is now built from the live per-indexer query outcomes via the shared `_apply_indexer_health`.
- **Fixed the admin "Open Jackett Admin" link pointing at `http://localhost:9117`** — useless from a remote browser, where "localhost" is the admin's own machine. `_jackett_dashboard_url(request)` now swaps the loopback host for the hostname the admin actually connected on (keeping Jackett's scheme + port); a non-loopback `INDEXER_URL` (remote Jackett) is left as-is.
- **Backend:** [main.py](main.py) (`_apply_indexer_health`, `_record_indexer_health`, `_list_configured_indexers`, `_shape_search_results`, rewritten `/api/search`, `_jackett_dashboard_url(request)` + callers). **Docs:** [docs/API.md](docs/API.md).

## [5.31.0] — 2026-06-14
- **Search now survives a single broken indexer — and flags it instead of failing.** Jackett's aggregate `/indexers/all/results` endpoint already returns results from the indexers that *did* respond and reports the ones that errored in its `Indexers` block, but the search endpoint ignored that block, so a degraded fan-out was invisible. `/api/search` now reads it (`_record_indexer_health`): as long as at least one indexer answers, the search bar keeps working, and when *some* fail while others succeed it raises a **non-specific** degraded flag — never naming which indexers are down. The dashboard shows a quiet amber banner ("Some search sources aren't responding — results may be incomplete.") driven by the new `indexers_degraded` / `indexers_total` / `indexers_failing` state fields (`state_snapshot`, surfaced in `renderServerAttention`). The admin **Indexers** tab gains an **Indexer Health** card: a per-indexer Responding/Failing list (with the failure reason), **Test** (per indexer) and **Test All** buttons that probe Jackett's per-indexer test endpoint live, a degraded/all-OK summary banner, and an **Open Jackett Admin ↗** link to the Jackett dashboard so the admin can log in and fix or remove the failing indexers directly. **Backend:** [main.py](main.py) (`AppState.indexers_degraded`/`indexers_total`/`indexers_failing`/`indexer_health`, `state_snapshot`, `_record_indexer_health`, `/api/search`, `GET /api/admin/indexers/health`, `POST /api/admin/indexers/{id}/test`, `POST /api/admin/indexers/test-all`, `_jackett_dashboard_url`). **UI:** [static/index.html](static/index.html) (`renderServerAttention` indexer-degraded branch), [static/admin.html](static/admin.html) (Indexer Health card, `loadIndexerHealth`/`testAllIndexers`/`testOneIndexer`/`_renderIndexerHealth`). **Docs:** [docs/API.md](docs/API.md), [docs/ADMIN.md](docs/ADMIN.md).

## [5.30.1] — 2026-06-13
- **Fixed the indexer (Jackett) intermittently going "unreachable" for searches until you retried several times.** The watchdog declared Jackett down and force-killed it (then sat through a ~40 s mono/.NET cold restart) on a **single** failed HTTP health probe. Jackett's web stack can briefly stop answering `/UI/Login` within the probe timeout while it's busy — fanning a search out to every indexer, or under CPU load from the idle auto-fingerprint/auto-validate maintenance added in 5.21.0 — so a transient slow response was repeatedly cold-restarting the indexer mid-search, surfacing as `502 Indexer unreachable` that "eventually works." The watchdog now tolerates consecutive failed probes before the destructive restart (`ServiceSpec.failure_grace`; Jackett = 2, so ~1 min of *sustained* failure is still recovered, mirroring the in-app `jackett_health_monitor` backstop) and the per-probe timeout is bumped 4 s → 6 s. Port-checked services (VLC, qBit) keep `grace=0` so crash recovery stays immediate. **Watchdog:** [watchdog.py](watchdog.py) (`ServiceSpec.failure_grace`/`_health_misses`, plain-service loop grace logic, `_jackett_alive` timeout). **Docs:** [docs/DAEMON_WATCHDOG.md](docs/DAEMON_WATCHDOG.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.30.0] — 2026-06-13
- **New "Use My Computer" control in Profile Settings — pause the background video and stop VLC grabbing the screen / minimizing your windows so you can use the machine normally.** The host normally runs an idle background video that re-fullscreens VLC and minimizes every other window (`background_video_loop` + `vlc_focus_and_fullscreen`/`_minimize_other_windows_windows`), which fights a user trying to use the same computer. Profile Settings → **Global** now has a **Use My Computer** tile with three buttons — **60 Sec**, **2 Min**, and **Until I Stop** — that pause all of it; while paused the tile shows a live "Paused — resumes in m:ss" (or "until you resume") line with a **Resume** button. A single server-side gate, `window_mgmt_paused()` ([main.py](main.py)), backs it: `vlc_focus_and_fullscreen` returns immediately and `background_video_loop` skips (re)starting the idle video while paused, so every focus/minimize/fullscreen assertion stands down. Pausing also minimizes VLC right away (`vlc_minimize`) so the desktop is usable instantly instead of waiting for the current background video to end; resuming kicks `vlc_focus_and_fullscreen` so the background video returns to the foreground without waiting for the 3 s poll. A timed pause auto-expires server-side on read and the UI counts it down locally between SSE state events. **Backend:** [main.py](main.py) (`AppState.window_mgmt_paused_until`, `window_mgmt_paused()`, guards in `vlc_focus_and_fullscreen`/`background_video_loop`, `state_snapshot` `window_mgmt_paused`/`window_mgmt_pause_remaining`, `WindowControlReq`, `POST /api/window-control`). **UI:** [static/index.html](static/index.html) (`#wcButtons`/`#wcResume` tile, `pauseWindowControl`/`resumeWindowControl`/`renderWindowControl`, SSE `state` + `openProfileSettings`/`closeProfileSettings` hooks). **Docs:** [docs/API.md](docs/API.md), [docs/BACKEND.md](docs/BACKEND.md), [docs/FRONTEND.md](docs/FRONTEND.md).

## [5.29.0] — 2026-06-12
- **New Dev Mode setting — a live diagnostics overlay for on-device streaming.** Profile Settings gains a **This Device** section (settings stored in the browser's localStorage, not on the server — these stats are properties of the viewing device, not the host or profile) with a **Dev Mode** toggle. While on, the on-device player shows a pointer-through monospace HUD (`#lpDevHud`, top-left under the header) repainted at 1 Hz with the streaming internals: playback engine + mode (hls.js/native · bundle/on-demand, plus a RECONNECTING flag while the network-retry loop runs), the **quality rung actually playing** (height + bitrate, auto vs. pinned, from `lp.hls.levels`), the hls.js **bandwidth estimate**, the **last segment's download speed** (size/duration/rate from `FRAG_LOADED` stats, recorded only while Dev Mode is on), the decoded video dimensions, **how far ahead playback has buffered** (seconds past the playhead + buffered-range count), dropped/total frames (`getVideoPlaybackQuality`), and a session stall counter + `readyState`. The HUD deliberately ignores the controls' auto-hide (it exists to be watched), hides in tiny-tile mode, toggles live mid-playback, and costs nothing while off (no interval, no stat collection). Safari-native HLS exposes no level/bandwidth API, so there the HUD shows decode size/buffer/frames only. **UI:** [static/index.html](static/index.html) (`#psDevMode` + This Device section, `toggleDevMode`, `devMode` localStorage flag, `#lpDevHud` + CSS, `_lpDevHudStart`/`_lpDevHudStop`/`_lpDevHudTick`/`_lpDevNoteFrag`/`_lpDevFmtBits`, hooks in `lpPlay`/`lpStop`/`FRAG_LOADED`/`waiting`). **Docs:** [docs/FRONTEND.md](docs/FRONTEND.md).

## [5.28.1] — 2026-06-12
- **Fixed the on-device player's forward buffer being capped at ~30 s on Safari (macOS and iOS) instead of the intended ~3 minutes.** Safari 17.1+ exposes the new ManagedMediaSource API — on macOS *alongside* classic MediaSource — and hls.js 1.5 prefers MMS by default (`preferManagedMediaSource: true`). Under MMS the OS owns the fetch cadence: it stops segment loading once ~30 s is buffered and duty-cycles the radio (buffer drains to ~10–15 s, then refills), silently defeating the `maxBufferLength: 180` outage buffer added in 5.24.0. The player now passes `preferManagedMediaSource: false`, so every browser with classic MSE (macOS Safari, Chrome, Firefox, Edge) gets the full 3-minute buffer again. On iOS classic MSE doesn't exist, so hls.js still falls back to MMS there — the ~30 s ceiling on iPhone is an OS-level battery optimization that cannot be lifted (documented as a platform gap). **UI:** [static/index.html](static/index.html) (hls.js config). **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.28.0] — 2026-06-12
- **On-device player: new orientation-lock button — keep watching in landscape even when the phone auto-rotates to portrait.** A new rotate-lock button (`#lpRotBtn`, touch devices only — hidden on fine pointers) in the bottom control strip toggles `.lp-lock-landscape` on `#localPlayer`. Two mechanisms cooperate: where the Screen Orientation API works (Android, in OS fullscreen) `screen.orientation.lock("landscape")` pins the real viewport; everywhere else — iPhone Safari has no lock API at all — a CSS fallback fires whenever the viewport goes portrait, rotating the entire player 90° (`@media (orientation:portrait)`: sized to the swapped viewport dimensions `100dvh`×`100dvw`, centered, `rotate(90deg)`), so the video **and the custom controls** keep rendering landscape no matter how the phone is held. The two paths can't conflict: when the native lock holds, the portrait media query never matches. Browser hit-testing follows the CSS transform automatically; the one manual fix is the seek bar's scrub math (`_lpSeekPosFromEvent` scrubs along `clientY` while rotated, via the new `_lpRotated()` helper). The lock clears itself on stop and on minimize-to-tile (`_lpClearOrientLock` — a rotated corner tile would be absurd), and the button tints indigo while engaged. **UI:** [static/index.html](static/index.html) (`#lpRotBtn`, `.lp-lock-landscape` CSS, `lpToggleOrientLock`/`_lpClearOrientLock`/`_lpRotated`, `_lpSeekPosFromEvent`, cleanup in `lpStop`/`lpMinimize`). **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.27.0] — 2026-06-12
- **Fixed the mobile UI feeling "loose" — overscrolling in the wrong place could rubber-band, shift, or break the layout — and gave the on-device player true fullscreen on iPhone Safari.** The document body was the page's scroll container while the header was `sticky` and every panel (episode page, players, modals) a `fixed` overlay — so on phones, any overscroll rubber-banded the *entire page*: fixed footers/overlays visibly detached, pull-to-refresh fired mid-scroll, and flicking past the end of a modal list scrolled the page hiding underneath. The dashboard is now a **height-locked app shell**: `html`/`body` are `100dvh` + `overflow:hidden` with `overscroll-behavior:none` (the body intentionally drops `min-h-screen` — `100vh` overshoots `100dvh` while the mobile URL bar is visible), making `<main>` and the overlay lists the only things that can scroll. Every scrollable region gets `overscroll-behavior:contain` so its momentum never chains to whatever is behind it, horizontal strips (season tabs) get `overscroll-behavior-x:contain` so a hard flick can't trigger browser back/forward swipe, and `touch-action:manipulation` on the shell kills accidental double-tap zoom (pinch-zoom still works). No JS read `window.scrollY` or used window scroll listeners, so nothing else changes. **True fullscreen on iPhone:** the player's fullscreen button — previously hidden on iPhone Safari (no element-fullscreen API) — now falls back to the `<video>`'s native `webkitEnterFullscreen()`: genuinely full screen (no browser chrome at all), native transport while fullscreen, in-stream audio/sub tracks still work (they're in the HLS manifest on the Safari-native path), and `webkitendfullscreen` resyncs the custom UI on exit. **Swipe-to-minimize the browser bar** also still works during playback: a locked document means the browser can never auto-hide its URL bar (that only happens on real *root* scrolls), so while `#localPlayer` is open full-screen (not the tiny tile) an `html:has(...)` rule (coarse-pointer only) re-enables root scrolling by making `<body>` 45vh taller than the viewport — the overflow must be the body's own box (an absolutely-positioned spacer below body doesn't reliably extend Safari's root scroll range) and must hang off the root scroller, not `<body>`'s own `overflow` (inner scrolling never collapses the bar). Closing/minimizing the player clamps the offset back to 0 without touching `<main>`'s scroll position. **UI:** [static/index.html](static/index.html) (app-shell CSS block, body class, `lpToggleFullscreen`, fs-button init, `webkitendfullscreen` hook). **Docs:** [docs/FRONTEND.md](docs/FRONTEND.md), [docs/GOTCHAS.md](docs/GOTCHAS.md), [docs/STREAMING.md](docs/STREAMING.md).

## [5.26.1] — 2026-06-12
- **Fixed VLC "unable to open the MRL" on Windows files whose full path is 260+ characters (MAX_PATH).** qBittorrent writes such files fine (it uses `\\?\` extended-length paths internally), but VLC opens inputs with the plain Win32 API, so a long multi-episode filename (e.g. a four-part series finale) produced a file that existed on disk yet could never be played — every other episode in the same folder worked. All VLC input URIs are now built through a single helper, `vlc_file_uri()` ([main.py](main.py)), which behaves exactly like the old `Path.resolve().as_uri()` except on Windows paths over 259 chars, where it substitutes the 8.3 short form (`GetShortPathNameW`) that stays under the limit. Round-tripping is safe — everything that maps VLC's reported URI back to a library path goes through `Path.resolve()`, which expands short names back to the canonical long path. Falls back to the long URI (with a log warning) if 8.3 name generation is disabled on the volume. **Docs:** [docs/GOTCHAS.md](docs/GOTCHAS.md), [docs/BACKEND.md](docs/BACKEND.md).

## [5.26.0] — 2026-06-11
- **On-device player: the video now takes the entire screen, and the track/clip controls moved behind a gear button — fixing the mobile layout overflow.** The 5.25.0 player kept the header and the always-visible track row (quality/audio/sub/AI/clip — up to ~200px tall) as flex siblings of the video, which both shrank the video and overflowed short mobile viewports. Now **everything overlays the video**: `#lpStage` is `absolute inset:0` (the video gets every pixel, nothing can overflow), the header (`#lpHeader`) is an absolute translucent top bar, and the old track row (`#lpTrackRow`) became an **options panel** — hidden until the new **gear button** in the bottom control strip toggles it (`lpToggleOpts`, `.lp-opts` on `#localPlayer`), anchored above the transport, scrollable, max 380px wide. While the panel is open the overlay never auto-hides; a tap on the video closes the panel first. On narrow phones (≤480px) the header buttons collapse to icons (`.lp-btn-label`) so the bar can't overflow either. The bottom strip now carries the safe-area inset itself (the old `safe-bottom` spacer div is gone). **UI:** [static/index.html](static/index.html) (`#lpStage`/`#lpHeader`/`#lpTrackRow` CSS, `#lpOptsBtn`, `lpToggleOpts`/`_lpOptsOpen`, guards in `_lpCtlShow`/`_lpCtlIdle`/`lpStageTap`, cleanup in `lpStop`/`lpMinimize`). **Docs:** [docs/FRONTEND.md](docs/FRONTEND.md), [docs/STREAMING.md](docs/STREAMING.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.25.0] — 2026-06-11
- **The on-device player now draws its own Metro-style controls instead of the browser's native `<video>` chrome — so the player looks and behaves identically on every OS and browser.** Native controls vary wildly across platforms (different seek bars, different/missing subtitle menus, iOS hijacking fullscreen into its own player), so users saw a different UI depending on their device. The `controls` attribute is gone; a custom overlay (`#lpControls`) provides: a **seek bar** with buffered indicator and drag-to-scrub (position previews while dragging, a single seek commits on release — important in on-demand mode where every cold seek restarts the JIT encode), **±10 s tiles**, a center **play/pause tile**, **current/total time**, **mute**, and **fullscreen** — which fullscreens the whole player container (header, transport, audio/sub/quality selectors all stay visible and usable inside OS fullscreen) rather than the bare video, keeping the custom UI on-screen. Mobile-first: 44 px+ touch targets, tap the video to toggle the overlay (auto-hides after 3 s while playing; never while paused, scrubbing, buffering, or reconnecting), square Metro buffering spinner shown on any data stall, all chrome fades opacity-only (no reflow). Desktop additions: mouse-move reveals controls, keyboard shortcuts (Space/K play-pause, ←/→ ±10 s, F fullscreen, M mute). On iPhone Safari (no element-fullscreen API) the fullscreen button hides itself — the player is already a full-viewport overlay. In tiny corner-tile mode the overlay hides and a tap maximizes. **UI:** [static/index.html](static/index.html) (`#lpStage`/`#lpControls`/`#lpBuffSpin` markup + CSS, `lpStageTap`/`_lpCtlShow`/`_lpCtlIdle`/`_lpCtlSync`/`_lpCtlTick`/`lpTogglePlay`/`lpSeekBy`/`lpToggleMute`/`lpToggleFullscreen`/`_lpSeekBarInit`, keyboard handler, `.lp-idle`/`.lp-buffering`/`.lp-scrub` states). **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/FRONTEND.md](docs/FRONTEND.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.24.0] — 2026-06-11
- **On-device streaming now survives unstable connections (tunnels, Wi-Fi↔LTE swaps) the way YouTube does.** Two root causes fixed: **(1)** the forward buffer was capped at 60 s — and effectively less, since hls.js's default 60 MB `maxBufferSize` byte cap holds only ~60 s of a high-bitrate original rung — so any outage longer than the buffer cut playback out; the player now buffers ~3 minutes ahead (`maxBufferLength: 180`, `maxBufferSize: 240 MB`, with `backBufferLength: 90` freeing memory behind the playhead so mobile devices can afford it). **(2)** a fatal hls.js `NETWORK_ERROR` showed an error alert and stopped loading **forever** — even though the `<video>` element would happily keep playing its buffer and the connection usually comes right back; now playback keeps draining the buffer while a background reconnect loop retries `hls.startLoad()` with backoff (2 s → 15 s cap, indefinitely), recovery detected on `FRAG_LOADED`, with the browser `online` event short-circuiting the backoff (retries deliberately don't gate on `navigator.onLine` — a phone in a tunnel often reports online with no usable data). The Safari-native path gets the same policy: instead of an alert, a video `error` reloads the still-set `src` and seeks back to where playback died (only the permanent `MEDIA_ERR_SRC_NOT_SUPPORTED` still alerts). A "Reconnecting…" overlay appears only once the buffer actually runs dry — outages the buffer covers are invisible. **UI:** [static/index.html](static/index.html) (`_lpNetLost`/`_lpNetRetryNow`/`_lpNetReset`/`_lpNetOnline`, `lp.netDown`, hls.js buffer config, `waiting`/`playing`/`error` listeners). **Docs:** [docs/STREAMING.md](docs/STREAMING.md) (§ Unstable connections).

## [5.23.0] — 2026-06-11
- **Smart Skip fingerprinting is dramatically faster, matches intros it used to truncate or miss, and its admin progress bar no longer "restarts".** Three root causes fixed: **(1) Matcher speed + accuracy** — the episode-pair matcher was an O(N²) pure-Python loop (~10-60 s *per pair*) that also demanded a strictly consecutive run of matching frames; since each chromaprint frame spans ~2.4 s of audio, one second of episode-specific audio inside the theme (a title-card voiceover) corrupts ~20 consecutive frames, so real intros got truncated at the first blip or missed entirely. The matcher is now numpy-vectorized (XOR + popcount LUT, ~50-100× faster; pure-Python fallback if numpy is absent) and **bridges mismatch gaps** up to ~4 s (`MATCH_GAP_FRAMES`, `MATCH_MIN_RATIO`) so full intro spans are detected, while a new **stationary-audio guard** (`MIN_FRAME_DELTA_BITS`, `_informative_mask`) stops silence/drones/tones — whose near-identical chromaprint frames bogus-match for tens of seconds — from producing false intro windows. `ANALYZER_VERSION` bumped to 3 → existing libraries re-analyze with the better algorithm. Episodes are also fingerprinted 2-at-a-time (`FP_CONCURRENCY`). **(2) Restarting passes** — analysis was kicked off per prepped file, and every pass re-fingerprints the whole series, so during a season prep the analyzer genuinely restarted from episode 1 over and over (quadratic work). It now **coalesces**: scheduling defers while any prep job for the series is queued/encoding (`_series_prep_active`), with a 15 s watcher (`_watch_prep_then_analyze`) backstopping prep exits that don't fire the post-prep hook — one full pass after the last file lands. **(3) Progress reporting** — the bar was drawn from per-stage `current/total`, which resets at every stage boundary and whose matching-stage `total` is a growing estimate (backward jumps). The analyzer now emits a **monotonic all-stages `progress` fraction** consumed by the Smart Skip badge and Activity tab. Also fixed: **manual skip entries are no longer overwritten** by a series re-run (the docs always promised this; the persistence loop now enforces it). **Backend:** [analyzer.py](analyzer.py) (`_find_longest_match_np`/`_best_gap_run`/`_find_longest_match_py`, gap/ratio constants, parallel fingerprinting, `progress` emit, version 3), [main.py](main.py) (`_series_prep_active`, `_watch_prep_then_analyze`, defer guard in `_schedule_series_analysis_if_eligible`, manual-entry guard in `_run_series_analysis`, Activity snapshot uses `job.progress`). **UI:** [static/admin.html](static/admin.html) (`_statusBadge` uses `job.progress`). **Deps:** [requirements.txt](requirements.txt) (+numpy). **Docs:** [docs/ANALYZER.md](docs/ANALYZER.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.22.0] — 2026-06-09
- **The host-busy banner and the global "Prepping N files" pill are now both collapsible — each starts as a small chevron tab so they no longer clip/block the navbar and corner.** The "Host is busy/overloaded" banner (full-width sticky strip at `z-70`) and the fixed top-right prep pill overlapped the navbar and each other when active. Both now render **collapsed by default**: the banner shrinks to a small tone-coloured chevron tab pinned top-right (rest of the strip transparent + click-through); the prep pill shrinks to just its pulsing dot + chevron. Clicking the chevron expands the full content (and flips the chevron up); clicking again re-collapses. Banner state (`perfBannerExpanded`) is re-applied across the per-SSE-tick `className` rewrites; pill state (`prepBarExpanded`) survives `_renderGlobalPrep`'s re-renders. **UI:** [static/index.html](static/index.html) (`#perfBannerToggle`/`togglePerfBanner`/`renderPerfBanner`/`.perf-collapsed`; `#globalPrepToggle`/`togglePrepBar`/`.prep-collapsed`).

## [5.21.0] — 2026-06-09
- **Background work now drains itself: idle auto-fingerprint + auto-validate, plus an "Outstanding Work" backlog view on the Activity tab.** The Activity tab only ever showed work running *right now* — so with most shows never fingerprinted and almost none validated, it sat empty, because those tasks were only ever triggered manually / on-add. Two changes fix that: **(1)** a new **`background_maintenance_loop`** runs Smart Skip fingerprinting and source-file validation automatically **while the box is idle** (no playback / recent use) at below-normal priority — one series / one file at a time, pausing the instant the box is used. Validation verdicts are **persisted per file** (`library.json → files[].validation`), so the validator skips already-checked files and **resumes after a restart/auto-update instead of starting over** (directly addressing the "restarts interrupt progress that doesn't restart" problem). **(2)** the Activity tab gained an **Outstanding Work** card showing the backlog counts (un-prepped / un-fingerprinted / never-validated) and *why each isn't running* (e.g. "Auto-fingerprint is ON — runs while idle" vs "Auto-validate is OFF"). New admin **Automatic Maintenance** card (System tab) toggles the two workers (default ON). **Backend:** [main.py](main.py) (`background_maintenance_loop`, `_run_auto_validation`, `_auto_maint_cfg`, persisted-verdict helpers `_file_sig`/`_needs_validation`/`_persist_validations`, backlog helpers, `GET`/`POST /api/admin/auto-maintenance`, `AppState.auto_validate*`, manual scans now persist verdicts too, `_activity_snapshot.outstanding`). **UI:** [static/admin.html](static/admin.html) (Outstanding Work card, Automatic Maintenance toggles). **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md), [docs/BACKEND.md](docs/BACKEND.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md).

## [5.20.0] — 2026-06-09
- **New admin "Activity" tab shows every background job, *why* it's running, and whether it survives a restart.** Server restarts and auto-updates were silently discarding in-flight work (HLS stream prep, AI subtitles, file validation/repair, Smart Skip analysis, on-demand transcode sessions) — none of it checkpoints, so a progress bar that "reset" was actually a fresh start under a restarted process, with no indication why. The new tab (now the admin landing tab) lists each active job with a plain-English reason and a **Lost on restart** vs **Survives restart** badge, plus a summary strip (active count, fragile count, host idle/busy, Auto-Prep mode, prep-paused / force-prep-stopped / VPN-down gates) and a heads-up banner when restarting now would interrupt non-resumable work. Auto-refreshes every 4 s while open. Torrent downloads are flagged as surviving (qBittorrent persists them out-of-process); everything in-memory is flagged fragile, with notes on what re-queues automatically (bulk auto-prep) vs. what must be re-triggered (Force Prep, file validation/repair). **Backend:** [main.py](main.py) (`_activity_title`, `_activity_snapshot`, `GET /api/admin/activity`). **UI:** [static/admin.html](static/admin.html) (Activity tab + `loadActivity`/`startActivityPolling`). **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md).

## [5.19.0] — 2026-06-09
- **Admin can now choose which network adapter is the server's primary address — every other adapter still answers but redirects to the chosen one.** A new **Network Adapter** card (System tab) lists the host's physical LAN adapters; picking one stores its interface *name* (so it survives DHCP IP changes) and makes that adapter's current IP the single canonical address. uvicorn stays bound to `0.0.0.0`, so the dashboard keeps answering on every adapter — but a client that connects on a non-preferred adapter's IP is 307-redirected to the primary (e.g. Wi-Fi `192.168.0.104` → Ethernet `192.168.0.106`). The preferred adapter is also what `remote.local` (mDNS) and the printed URL resolve to. **Preference + graceful fallback:** if the chosen adapter is offline, the server falls back to the route-table heuristic, advertises that instead, and logs the issue (once, not on every keepalive poll); the admin card surfaces the fallback inline. **New shared module** [netadapters.py](netadapters.py) holds the adapter enumeration + preferred-adapter resolution (the logic lifted out of `run.py`'s `get_local_ip`), imported by both the launcher and the dashboard. **Backend:** [main.py](main.py) (`AppState.preferred_adapter`, `_network_cfg`, `network_adapter_redirect` middleware, `_network_status`, `NetworkReq`, `GET`/`POST /api/admin/network`, lifespan seed). **Launcher:** [run.py](run.py) (`get_local_ip` now delegates to `netadapters` + reads the preferred adapter + logs fallback on change). **UI:** [static/admin.html](static/admin.html) (Network Adapter card + `loadNetwork`/`saveNetwork`). **Docs:** [docs/RUNTIME.md](docs/RUNTIME.md), [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.18.0] — 2026-06-09
- **UI responsiveness pass — every control that waits on the server now shows an immediate pending state (spinner + dim + disable) and clears it when done.** Previously many actions fired a request and gave no feedback until the response landed — the prime example being the Library **Refresh** button, which looked inert whether it had refreshed or was still waiting. This standardises the existing `withInflight`/`_markLoading` spinner across the dashboard and ports the same helper into the admin panel (which had none). **Dashboard ([static/index.html](static/index.html)):** Refresh (`loadLibrary`), search, add/remove storage path, mark-watched, recheck, delete item, queue/cancel play-when-ready, download/profile/change-PIN submits, the per-file & per-season download/idle/skip schedule segments, and the stream-prep schedule buttons all spin while in flight (and drop double-taps). **Admin ([static/admin.html](static/admin.html)):** ported the `withInflight`/`_markLoading` helper + spinner CSS, then wired login, every Save (subtitles, STT, qBit limits, categories, TMDb, env keys, indexer config, scheduled restart, auto-prep, auto-update, skip-data, cache auto-purge), every Refresh/Load (indexers, logs, components, changelog, offline cache, analyzer log), component install, force-prep / file-validate / file-repair start+stop, cache purge/delete, branch/commit switch + hard-reset, shutdown/reboot, and PIN save. Controls that already showed strong feedback (upload progress bars, in-region "Loading…/Searching…" text) were left as-is. **Docs:** [docs/FRONTEND.md](docs/FRONTEND.md).

## [5.17.0] — 2026-06-08
- **Holding the fullscreen "To Device" tile while it's mid-prep now starts on-device playback immediately via the JIT path, instead of doing nothing.** When an un-prepped library file is playing on the TV, holding the tile starts a full prep and the tile flips to "Prepping 42%". Previously a second hold during that state was a no-op — you had to wait out the whole encode before you could move playback to your device. Now holding the "Prepping…" tile hands off to this device **right away** using on-demand (just-in-time) streaming (`_lpLoadIndex`'s existing un-prepped fallback), while the full ABR bundle keeps building in the background for a later, richer play. The tile now advertises this ("Hold to stream here now"). **UI:** [static/index.html](static/index.html) (`fcDeviceTileHold` prepping branch, `handoffToDevice(btn, allowOnDemand)`, `_renderFcHandoff` prepping note). **Docs:** [docs/STREAMING.md](docs/STREAMING.md).

## [5.16.1] — 2026-06-08
- **File repair no longer strips embedded subtitles during the lossy re-encode pass.** The remux repair leg already preserved every stream (`-map 0 -c copy`), but the opt-in **re-encode** fallback mapped only video + audio (`-map 0:v:0? -map 0:a?`), so repairing a genuinely-corrupt file by re-encoding silently dropped all baked-in subtitle tracks. The re-encode now also carries embedded subtitles (`-map 0:s?`) and, in MKV, attachments such as ASS fonts (`-map 0:t?`): subs are copied as-is for MKV (image subs like PGS/VOBSUB included) and transcoded to `mov_text` for MP4/MOV (where copying subrip fails). Sidecar subtitles — including online-downloaded and deep-discovered (`Subs/`-folder) `.srt`s — were already preserved, since repair replaces the source in place (`os.replace`) keeping the same path/stem they're keyed on. **Backend:** [main.py](main.py) (`_repair_one_file` re-encode attempt). **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.16.0] — 2026-06-08
- **Automatic stream prep can now validate & repair each source file as it preps it — GPU-accelerated, admin-controlled, off by default.** A new **Validate & Repair on Prep** card (System tab) adds a three-way mode so the unattended overnight/idle prep run can also heal corrupt media without anyone remembering to run the manual File Validator: **Don't Validate** (default, current behaviour), **Repair Before Prep** (deep-decode the source first and lossless **remux-repair** it in place if damaged, then prep the healed file), and **Repair After Prep** (build the HLS bundle first, then deep-decode the source — a repair purges the just-built bundle so it re-preps from the healed source next cycle). It reuses the existing per-file `_validate_one_file`/`_repair_one_file` (remux-only, no lossy re-encode) and runs **only for bulk & idle preps** — interactive play-on-device preps are never delayed. The validator/repair `ffmpeg` now **decodes on the GPU when NVENC is present** (transparent `-hwaccel cuda`, with a CPU fallback), and the optional repair re-encode uses `h264_nvenc`; this applies to the manual File Validator too. The scan/repair runs inside the prep concurrency slot (one ffmpeg at a time) and its live process is the prep job's, so the existing **Pause / Stop Now / activity-kick** controls terminate it for free. **Backend:** [main.py](main.py) (`PrepValidateReq`, `_prep_validate_cfg`, `GET`/`POST /api/admin/prep-validate`, `_decode_hwaccel_args`, `_prep_validate_repair`, before/after hooks in `_run_offline_job`, `set_proc`/`is_stopped` params on `_validate_one_file`/`_repair_one_file`). **UI:** [static/admin.html](static/admin.html) (Validate & Repair on Prep card + `loadPrepValidate`/`setPrepValidate`/`_renderPrepValidate`). **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.15.0] — 2026-06-07
- **New admin File Validator card (System tab) — decode your source files with ffmpeg to find damaged/corrupt media, separate from the torrent recheck.** The qBit recheck only proves the bytes on disk match the torrent's hashes, so it happily seeds a perfect copy of a file that was *already* damaged at the source (bad encode, truncated rip, bit-rot before it was added). The validator actually **decodes the media**: a **Deep** scan (default) runs each library video through `ffmpeg -v error -xerror -i … -map 0:v? -map 0:a? -f null -` and flags any file that produces a decode error (catches mid-file corruption a header probe misses); a **Quick** scan just ffprobes the container (opens / has a video stream / has a duration). Scope is the whole library or one item. ffmpeg runs **below normal priority** and one file at a time so a scan can't starve playback; **Stop** halts the loop between files and terminates the in-flight decode. Damaged + missing-on-disk files are listed with the ffmpeg error tail. Works on every OS (plain decode — not HLS, so unlike prep it is **not** macOS-gated). **Backend:** [main.py](main.py) (`ValidateFilesReq`, `AppState.file_validation*`, `_validate_one_file`, `_run_file_validation`, `_file_validation_status`, `GET`/`POST /api/admin/validate-files`, `POST /api/admin/validate-files/stop`). **UI:** [static/admin.html](static/admin.html) (File Validator card + `loadFileValidation`/`startFileValidation`/`stopFileValidation` + 2 s poll). **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md).
- **File Validator can now attempt to repair the damaged files it finds.** Once a scan turns up damaged files, a **Repair Damaged** control appears. Each file is fixed in two staged, validated attempts: a lossless **remux** (`-c copy` with `-err_detect ignore_err` + regenerated timestamps — fixes broken index/moov/PTS and recoverable stream errors), then — if you tick **Allow lossy re-encode** — a libx264 **re-encode** that drops/conceals genuinely corrupt frames. The candidate is run back through the same deep decode and the original is **replaced only if it now decodes clean**, after which its stale on-device HLS bundle is purged so playback re-preps from the repaired source. Repaired/failed files are listed; **Stop** halts between files. **Caveat surfaced in the UI:** rewriting a file stops a torrent-backed copy from seeding (its bytes no longer match the pieces) — repair targets uploaded / non-torrent content; re-download is the better fix for torrent files. **Backend:** [main.py](main.py) (`RepairFilesReq`, `AppState.file_repair*`, `_repair_one_file`, `_run_file_repair`, `_file_repair_status`, shared `_run_ffmpeg_capture`/`_ffmpeg_decode_scan`/`_safe_unlink`, `GET`/`POST /api/admin/repair-files`, `POST /api/admin/repair-files/stop`). **UI:** [static/admin.html](static/admin.html) (repair row + `startFileRepair`/`stopFileRepair`/`_renderFileRepair` + 2 s poll). **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md).

## [5.14.0] — 2026-06-07
- **The episode-picker scheduling bar gains a stream-prep schedule (Now / Idle / Never), hides the download buttons once everything's downloaded, and adds a Recheck-hashes button.** The bar in the episode page that controlled *what downloads and when* (`⬇ Now / 🌙 Idle / ⊘ Skip`) was reworked into a three-row control:
  - **Stream-prep schedule (`⚡ Now / 🌙 Idle / ⊘ Never`)** — the HLS-prep sibling of the download schedule, applied to the visible season's files. **Now** preps them immediately (a scoped `/prep-all`), **Idle** lets Automatic Stream Prep build the bundle while the box is idle (the implicit default), **Never** opts the files out of *all* auto-prep — the idle/always `auto_prep_loop` and the play-driven warm-prep chain both skip "never" files, while any already-built bundle is kept (non-destructive). The active mode is highlighted, and a persisted per-file mode lives in `library.json → item["prep"]["files"]`.
  - **Download buttons hidden when fully downloaded** — the `⬇ Now / 🌙 Idle / ⊘ Skip` download row only appears while at least one episode still needs fetching; once every file in the item is on disk, scheduling what to download is moot, so the row is hidden (the prep + recheck rows stay).
  - **Recheck hashes** — a new button runs a qBittorrent piece-level **force-recheck** of the item's torrent over the **checked** episodes, waits for it to finish, and for any file that was complete but comes back damaged it re-fetches the bad pieces and **invalidates that file's cached HLS bundle** so it re-preps from the repaired source.
  **Backend:** [main.py](main.py) (`_PREP_MODES`/`_prep_cfg`/`_effective_prep_mode`, `prep_mode` in `/files`, `POST /api/library/{id}/prep-schedule`, `POST /api/library/{id}/recheck`, `qbit_recheck`, "never" gate in `_enqueue_library_prep` + `_play_prep_chain`). **UI:** [static/index.html](static/index.html) (`renderEpList` bar, `_epPrepBtn`, `epSchedPrep`, `epRecheckSelected`, `_visibleSeasonFiles`/`_visibleSeasonPrepMode`). **Docs:** [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/API.md](docs/API.md), [docs/STREAMING.md](docs/STREAMING.md), [docs/FRONTEND.md](docs/FRONTEND.md).

## [5.13.0] — 2026-06-07
- **Scrub bar is now locked by default — a guided hold-then-release gesture unlocks it — so accidental taps can't fling playback, and the mobile scrub timestamp no longer gets stuck over the bar.** The seek bar (both the footer bar and the fullscreen-controls bar) defaults to a **LOCKED** state: the track is greyed out (`grayscale` + dimmed) with a centred **HOLD TO UNLOCK** padlock badge, and any tap/click is ignored. Unlocking is a deliberate **three-phase gesture, all phases still locked** so neither the hold nor the release that ends it can be misread as a seek: (1) a **0.5s press-and-hold** (badge "HOLD TO UNLOCK", indigo fill grows; a too-short release aborts); (2) the badge then turns green and **pulses "RELEASE"** and just **waits for the user to lift — no timer runs**, so gripping the whole time can never lead to an accidental seek; (3) **only once they release** does a final **0.5s settle** (green fill re-sweep, "UNLOCKING…") run before the bar becomes seekable. Once unlocked it seeks normally and **re-locks 5s** after the last interaction (and immediately whenever playback stops, so every new stream starts protected). The handlers were rewritten from the old mouse+touch pair (`onclick`/`ontouch*` → `handleSeekBarClick`) to unified **Pointer Events** (`handleSeekPointerDown`/`Up`/`Leave`), which also fixes a mobile bug: on `touchend` the old code read `e.touches[0].clientX` from an empty (but truthy) `TouchList`, throwing a `TypeError` before `handleSeekBarLeave()` could run — so the scrub timestamp tooltip never hid and hovered over the bar. Pointer events always carry `clientX`, and the tooltip also gained a 2s safety auto-hide. **UI:** [static/index.html](static/index.html) (`.seekBar`/`.seekLockBadge`/`.seekUnlockProgress` + `holding`/`ready`/`settling` CSS, both seek-bar markup blocks, `handleSeekPointerDown`/`Up`/`Leave`, `_doSeek`/`lockSeekBar`/`unlockSeekBar`/`_cancelArming`, `updateState` re-lock). **Docs:** [docs/FRONTEND.md](docs/FRONTEND.md).

## [5.12.0] — 2026-06-07
- **Dashboard now uses the full width of the screen with responsive multi-column grids (Metro polish).** The whole app shell (navbar, main content, player footer) was pinned to a centered `max-w-5xl` (1024px) column, leaving large dead margins on wide desktops/TVs — our Windows-primary target. Raised the shell cap to `max-w-screen-2xl` (1536px) and converted the three list surfaces to responsive grids that add columns as the viewport grows, while staying single-column (mobile-first) on phones: **Search results** → 1 / 2 / 3 columns (`sm` / `xl`), **Library cards** → 1 / 2 columns (`lg`), **Episode list** → 1 / 2 columns (`xl`). Search-result Save/Play buttons were also brought back to the Metro convention (bold uppercase). No behaviour change — purely layout/space-usage and styling. **UI:** [static/index.html](static/index.html) (`#resultsGrid` + `doSearch` card buttons, `renderLibrary` group wrappers, `renderEpList` unwatched/watched wrappers, shell `max-w` cap). **Docs:** [docs/FRONTEND.md](docs/FRONTEND.md).
- **Library cards: single-item series now span full width (no stranded empty column), and the card button row is clean on mobile.** A series with only one item was leaving the second grid column empty, so the list looked ragged — `renderLibrary` now only goes 2-up when a series actually has multiple items (`gridCls`). The per-card action row was reworked: primary buttons (`.lib-card-actions > button`) are forced to a uniform 44px height on a single line — no more 2-line "☰ / Episodes" wrap or mismatched button heights — and the icon-only buttons (download / prep / hide / delete) collect into one cluster (`iconBtns`) that follows immediately (the earlier `ml-auto` was absorbing the row's slack and leaving a sparse gap). The symbol glyphs in the primary buttons (`↩` Resume, `▶` Play/Restart/When-Ready, `⏸` Idle, `⏳` Queued) carry a `︎` text-presentation selector so iOS renders them as flat monochrome Metro glyphs instead of rounded colour emoji. **UI:** [static/index.html](static/index.html) (`renderLibrary`, `.lib-card-actions` CSS).
- **Mobile landscape is usable again — episode picker, player footer, and fullscreen controls.** Phones in landscape are only ~320-430px tall, and the portrait-sized chrome ate the whole screen. Three breakages, all fixed by a new `@media (orientation:landscape) and (max-height:500px)` block (tablets ≈744px+ tall are unaffected): (1) the **episode page** hero (poster + overview + chips + season tabs + action bar) left **zero** height for the episode list — it now becomes a **two-pane split**: the hero (series art + description) is a scrollable left column at ~¼ width and the season tabs + episode list + actions (wrapped in a new `#epRightPane`) fill the right ¾; (2) the **player footer** row heights are trimmed so the library stays browsable; (3) the **fullscreen player controls** stacked up to six `flex-1` tile rows that got crushed below their content and overlapped into an unreadable pile — the tile grid (`#fcTileGrid`) now gives each row its natural min-height and scrolls instead. **UI:** [static/index.html](static/index.html) (landscape `<style>` block, `#epRightPane`/`#epHeroContent`/`#fcTileGrid` hooks).

## [5.11.2] — 2026-06-07
- **Fix: server notice banners no longer block the on-device player's Stop / To TV buttons on mobile.** The server-attention and host-performance banners float at `z-70` — above the on-device player (`z-50`) and its top control bar — so whenever one showed (most commonly the host-busy perf banner during stream prep), it sat over the **Stop** and **To TV** buttons and swallowed every tap aimed at them on mobile. The banners are purely informational, so they're now `pointer-events:none` (set by ID so it survives the className rewrites in `renderServerAttention`/`renderPerfBanner`); only the Admin Panel link re-enables clicks. The banners and the error/warning toast were also made translucent (`/95`→`/70`, toast `/80`→`/70`) so they sit lightly over the video instead of fully obscuring it. **UI:** [static/index.html](static/index.html) (banner `pointer-events` CSS, `ALERT_STYLES`, `renderServerAttention`/`renderPerfBanner` tones).

## [5.11.1] — 2026-06-07
- **Fix: Smart Skip fingerprinting no longer freezes the dashboard.** While a fingerprint pass reached its matching stage, the UI would go unresponsive for a minute or two — inputs taking ages to register — even though the host itself (RDP, CPU, RAM) looked perfectly healthy. Cause: the intro/credits matcher (`analyzer._find_longest_match`) is ~10-20 s of tight **pure-Python** CPU work per episode pair, and although it was already offloaded with `asyncio.to_thread`, a *thread* doesn't release the GIL for pure-Python work — the GIL convoy effect starved the server's event loop. The matcher now runs in a separate one-worker **process** (`ProcessPoolExecutor`, dropped to BELOW_NORMAL priority) so the work is on a fully independent GIL and the dashboard stays snappy; it falls back to the old thread path if a host can't create a process pool. Same results, same speed — just off the event loop. (In a local A/B the worst event-loop stall dropped from ~25 ms to ~9 ms on an *idle* box; the win is far larger under real server load.) **Analyzer:** [analyzer.py](analyzer.py) (`_new_match_executor`, `_match_worker_init`, `_run_match`, `_build_clusters_async`/`analyze_series` wiring). **Docs:** [docs/ANALYZER.md](docs/ANALYZER.md) (§ Concurrency), [docs/GOTCHAS.md](docs/GOTCHAS.md) (`to_thread` doesn't help pure-Python CPU).

## [5.11.0] — 2026-06-07
- **Smart Skip audio fingerprinting now runs as part of stream prep instead of the moment a download finishes — and a failed file no longer keeps retrying forever.** Previously intro/credits fingerprinting fired on every completed download (`library_download_monitor`'s ready-flip), and a file that failed to fingerprint re-ran on *every* sibling that later became ready — churning the analyzer pointlessly. Now fingerprinting is triggered once a file's HLS bundle is built (`_ensure_analysis_for` at the end of `_run_offline_job`, the post-prep sibling of the AI-subtitle hook), so it only runs on content you actually prep. The trigger is **fire-and-forget** — it just schedules a per-series pass at lowered OS priority and never blocks or fails the prep — and a running-guard stops a bulk `Prep for Streaming` of a big series from stacking duplicate passes. **Failures are now sticky:** a file that fails fingerprinting is not auto-retried; it re-runs only when the analyzer version is bumped or when an admin clicks **Analyze** in the Smart Skip tab. (Trade-off: content that is never stream-prepped no longer gets intro/credits skip data; auto-prep being on by default covers the normal case. On macOS, where stream prep is disabled by the TCC block, fingerprinting no longer runs at all — the analyzer's ffmpeg/fpcalc children were already blocked there anyway.) **Backend:** [main.py](main.py) (`_ensure_analysis_for`, post-prep hook in `_run_offline_job`, running-guard + no-auto-retry in `_schedule_series_analysis_if_eligible` / `_needs_reanalysis`, removed the `library_download_monitor` trigger). **Docs:** [docs/ANALYZER.md](docs/ANALYZER.md), [docs/STREAMING.md](docs/STREAMING.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.10.0] — 2026-06-07
- **Admins can now choose how far the VPN kill switch reaches.** A new **VPN Kill Switch** card on the admin System tab toggles between two modes: **Block UI** (the existing behaviour — a Mullvad drop locks the whole dashboard behind the full-screen "VPN DISCONNECTED" overlay) and **qBit Only** (only qBittorrent is killed and the torrent stream/download endpoints stay blocked, but the rest of the UI keeps working so a viewer can still watch already-prepped on-device content). **Regardless of the choice, qBittorrent is always killed the instant the VPN drops** — that invariant is enforced unconditionally by `vpn_guard` (in-process) and `watchdog.py` (process level); the new setting governs only the UI lockout. Persists under `library.json → settings.vpn_killswitch.block_ui` (default `true`), mirrored into `state.vpn_block_ui` and broadcast in the `state` / `vpn_status` SSE events so the overlay appears/clears live. **Backend:** [main.py](main.py) (`_vpn_killswitch_cfg`, `AppState.vpn_block_ui`, `GET`/`POST /api/admin/vpn-killswitch`, `vpn_guard` broadcast, `state_snapshot`). **UI:** [static/index.html](static/index.html) (`renderVpn` block-UI gate), [static/admin.html](static/admin.html) (VPN Kill Switch card, `loadVpnKillswitch`/`toggleVpnKillswitch`). **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/DAEMON_WATCHDOG.md](docs/DAEMON_WATCHDOG.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.9.0] — 2026-06-07
- **Admins in developer mode can now pin the dashboard to an exact commit.** The Updates tab's developer-mode area gains a **Pin To Commit** field (shown only when *Show all branches* is ticked): enter a 7–40 hex SHA (e.g. `f8e920a40fd8`) and click **Pin Commit** to hard-checkout that exact build as a **detached HEAD** — useful for reproducing a regression on a specific commit. While pinned, **the auto-updater is disabled** (there's no branch to track, so it would silently yank you off the pinned build); a banner on the tab explains this and the status loop stands down. To re-enable auto-update, pick a branch and click **Switch Branch** (or **Apply Now**) to return to a branch. The commit-pin is developer-only and gated server-side — the SHA is structurally validated (hex, 7–40 chars) so it can't smuggle in a refspec or git option injection. **Backend:** [updater.py](updater.py) (`switch_commit`, `is_detached_head`, `_looks_like_commit`), [main.py](main.py) (`POST /api/admin/updater/switch-commit`, `detached_head` in `GET /api/admin/updater`, detached-HEAD guard in `updater_loop`). **UI:** [static/admin.html](static/admin.html) (`switchCommitNow`, `_renderCommitPin`, detached-HEAD notice). **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md).

## [5.8.0] — 2026-06-07
- **Server notices (errors, warnings, server reboot, network/host warnings) now stay visible on every screen — including the fullscreen player controls — without blocking the UI.** Previously the top banners (reboot/update, host-busy) and the error/warning toast were hidden behind the opaque fullscreen-controls overlay and could be covered by modals. Now: (1) the banners + toast sit above modals so a notice is never lost behind a dialog; and (2) while the fullscreen VLC controls are open, those top elements are swapped for a **dedicated notice strip** (`#fcNotice`) overlaid on the title row. The strip is **absolutely positioned so it never reflows the control tiles below** — users rely on muscle memory for Play/Seek/Volume, so the buttons must not shift — and it's `pointer-events:none` so it can never block a control. The strip mirrors whatever the live banners/toast are saying (reboot/update first, then host performance/network, then the latest alert), tone-coloured to match. **UI:** [static/index.html](static/index.html) (`renderFcNotice`, `_fcNoticeTone`, `body.fc-open` handling in `openFullscreenControls`/`closeFullscreenControls`, raised z-index on `#globalToast`/`#serverAttentionBanner`/`#perfBanner`). **Docs:** [docs/FRONTEND.md](docs/FRONTEND.md).

## [5.7.0] — 2026-06-07
- **The admin Updates tab now shows the changelog and warns about risky branches.** Three additions to the auto-updater panel: (1) a **Changelog** card renders `CHANGELOG.md` (newest first, lightly formatted) so an admin can see exactly what each release changed before — or after — applying an update; (2) selecting **alpha** now surfaces a severe inline warning that the branch may break StreamLink and require a manual reinstall, while **beta** shows a milder "issues may be present" note; and (3) ticking **Show all branches (developer mode)** now warns that not all branches are functional and switching to one may cause data loss and require a full reinstall. Warnings update live as the branch picker / dev-mode toggle change. **Backend:** [main.py](main.py) (`GET /api/admin/updater/changelog`). **UI:** [static/admin.html](static/admin.html) (`loadChangelog`, `_renderChangelogMd`, `_renderBranchWarning`). **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md).

## [5.6.0] — 2026-06-07
- **Skipping to the next episode now marks the one you left as watched no matter where you skipped from.** Previously the deferred "mark watched" only armed when you jumped within the **last quarter** of the episode; skip earlier and it never tripped. Now any skip to the next episode — via the **Next** button, the **Skip Credits → Next** offer, or auto-skip-credits — arms the watched timer. The **~60 s grace period is unchanged**: if you skipped by mistake (or were dodging real content), just return to that episode within the window and your real progress is left untouched (the timer cancels the moment you're back on that file). One timer at a time — a newer skip supersedes an older pending one. **Backend:** [main.py](main.py) (`_arm_credit_skip_watch` — dropped the `CREDIT_SKIP_WATCH_MIN_PCT` last-quarter gate). **Docs:** [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/ANALYZER.md](docs/ANALYZER.md), [docs/API.md](docs/API.md).

## [5.5.1] — 2026-06-07
- **The nightly scheduled reboot no longer interrupts in-progress stream prep.** The reboot's idle guard already treated an active download as "in use" (so a download would defer the reboot), but **idle stream-prep / AI-subtitle (STT) jobs were not checked** — and those run precisely when the box is otherwise idle, which is exactly when the reboot wants to fire. Because HLS prep can't checkpoint, a reboot mid-encode threw the work away and it restarted from scratch on the next idle stretch. The reboot now also defers while any HLS-prep or STT job is actively encoding (any queue) or a user/admin-priority prep is queued, re-checking after `idle_minutes` just like the "machine in use" case. Soft-paused files still finishing their current encode are caught; jobs parked at the pause gate (`paused`) correctly don't hold off the reboot. **Backend:** [main.py](main.py) (`_prep_in_progress`, `scheduled_reboot_loop`). **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/BACKEND.md](docs/BACKEND.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.5.0] — 2026-06-07
- **Auto-updates now archive the old version's logs and start the new one with a clean slate.** Previously `streamlink_app.log` / `hls.log` just kept growing across updates, mixing lines from many versions into one file. When an update is applied, the updater drops a marker so that — on the post-update restart, **before any log handler opens a file** (avoiding the Windows "can't touch an open log" trap) — every current log file in `logs/` is zipped into **`logs_old_<timestamp>.zip`** and the originals are cleared. The zip is best-effort per file: an open file is still safely captured (read is always allowed), and a file another live process holds open (e.g. `streamlink_service.log` on Windows) is left in place rather than failing the rotation. **Logs now also carry their version going forward:** every process start logs a `StreamLink v<x.y.z> starting — branch=… commit=…` banner, so a fresh (or freshly-rotated) log clearly identifies which build produced the lines beneath it. (Also brings the stale `UI_VERSION` constant back in sync with the badge.) **Backend:** [main.py](main.py) (`_archive_old_logs`, `_ROTATE_MARKER`, `_init_logging`, `_log_version_banner`, marker write in `_run_apply`). **Docs:** [docs/GOTCHAS.md](docs/GOTCHAS.md), [docs/ADMIN.md](docs/ADMIN.md).

## [5.4.1] — 2026-06-07
- **Fix: the admin "Download All (.zip)" log bundle now extracts on Windows, and the service log downloads reliably.** The bundle was streamed through an OS pipe, which is non-seekable — that forced `zipfile` to emit **data descriptors** (general-purpose bit 3) with zero CRC/sizes in every local file header. macOS and 7-Zip read the central directory so they coped, but **Windows Explorer's built-in extractor reads the local headers and rejected the archive as "invalid"** (our primary target couldn't open its own log bundle). The bundle is now built into a *seekable* temp file so `zipfile` back-patches real local headers, and each member is read through its own handle so a log another process is actively appending to (notably `streamlink_service.log`, held open by the service-wrapper process) is still captured. Separately, the per-file download now serves a stable **snapshot copy** of the requested log instead of streaming the live file, which is why `streamlink_service.log` — the one log written by a different process and growing continuously — previously failed to download; if the copy can't be read it now returns a clear 500 instead of silently failing. Both responses clean up their temp file via a background task. **Backend:** [main.py](main.py) (`admin_download_logs_bundle`, `admin_download_log`). **Docs:** [docs/API.md](docs/API.md), [docs/ADMIN.md](docs/ADMIN.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.4.0] — 2026-06-07
- **The dashboard now recovers its live connection reliably after a phone locks or you switch apps.** Previously the realtime (SSE) link could die while the device was suspended and never come back — the UI sat frozen showing stale download/playback state until a manual refresh. Two failure modes are now handled: (1) when the browser fully *closes* the event stream (it gives up after backgrounding), the client rebuilds it itself with exponential backoff instead of waiting on the browser's auto-reconnect (which doesn't fire for a closed stream); and (2) a *half-open* connection (the socket silently died while suspended but still reports OPEN, so no error ever fires) is now caught by a liveness watchdog — the server emits a `ping` event at least every 20 s, and if the client hears nothing for ~50 s it tears the socket down and reconnects. The connection is also re-checked the instant the page returns to the foreground (`visibilitychange` / `pageshow` / `focus`) and when the network comes back (`online`), which is the high-value path on mobile. Reconnects always close the prior stream first so stale/duplicate server-side queues don't pile up. **Backend:** [main.py](main.py) (`/api/events` heartbeat is now a named `ping` event instead of a bare comment line). **UI:** [static/index.html](static/index.html) (`connectSSE` reconnect supervision, `_ensureSSEConnected`, liveness watchdog, foreground/online re-check). **Docs:** [docs/FRONTEND.md](docs/FRONTEND.md), [docs/API.md](docs/API.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.3.1] — 2026-06-07
- **Fix: the fullscreen ±10s / ±30s seek icons drew their inner triangle outside the circle.** The forward (+10/+30) tiles rendered the play-arrow well *above* the surrounding circle, and the rewind (−10/−30) arrow sat inside the circle but noticeably offset to the left — both had been wrong since the icons were added. The inner triangle subpaths are now centered on the circle's center (12,12) and are proper mirror images of each other. **UI:** [static/index.html](static/index.html).

## [5.3.0] — 2026-06-07
- **Skipping to the next episode near the end now marks the one you left as watched.** When you jump to the next episode from within the **last quarter** of the current one — whether via the **Next** button, the **Skip Credits → Next** offer, or auto-skip-credits — you've almost certainly finished it and were just dodging the credits/ending, but the normal "watched" threshold (92%) wouldn't have tripped yet. StreamLink now arms a short **grace timer** (~60 s): if you don't come back to that episode, it's marked watched. If Smart Skip guessed the credits **early** and you were actually skipping real content, just return to the episode within the window and your real progress is left untouched (the timer cancels the moment you're back on that file). One timer at a time — a newer skip supersedes an older pending one. **Backend:** [main.py](main.py) (`_arm_credit_skip_watch` / `_credit_skip_watch_grace` / `_mark_file_watched_internal`, `state.pending_watch`, armed in `/api/vlc/next`, `/api/skip-now` (credits), and the auto-skip-credits countdown; cancelled on return in `vlc_progress_tracker`). **Docs:** [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/ANALYZER.md](docs/ANALYZER.md), [docs/API.md](docs/API.md).

## [5.2.0] — 2026-06-06
- **Consolidated automatic stream-prep into one clear control.** The admin System tab's separate **Overnight Stream Prep** (fixed nightly window) and **Idle Auto-Prep** cards are replaced by a single **Automatic Stream Prep** card with three plain-language modes: **Always** (prep whenever there's anything to prep, regardless of activity), **When Idle** (prep only after the box has been idle, then stop on activity — your choice of a **hard** stop that discards the in-flight file or a **soft** stop that lets it finish), and **Never** (manual only — only playing on the TV and the Force button prep). The fixed nightly time-window is gone; the modes are purely activity-based. **Auto-Prep on Play** and **Force Stream Prep** are unchanged. Existing schedules migrate automatically (any old overnight/idle setting → *When Idle*). **Backend:** [main.py](main.py) (`settings.auto_prep` + `_auto_prep_cfg`, rewritten `auto_prep_loop`, `GET/POST /api/admin/auto-prep` replacing the four overnight/idle endpoints). **UI:** [static/admin.html](static/admin.html), [static/index.html](static/index.html). **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/BACKEND.md](docs/BACKEND.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [5.1.1] — 2026-06-06
- **Fix: the admin Offline Cache tab no longer re-walks the whole cache on every open.** On a large ABR cache the recursive size walk took long enough that opening the tab felt broken. The server now caches the inventory snapshot and serves it instantly; the tab shows an **"As of …"** stamp so you know how fresh the numbers are, and the **Refresh** button forces a fresh on-disk re-check (it disables + shows *Checking…* while it works). Deletes and the auto-purge loop invalidate the snapshot so purged bundles never linger in the view. Concurrent opens (or an open racing auto-purge) share one walk via a lock. **Backend:** [main.py](main.py) (`_build_offline_cache_inventory` now snapshots; `?refresh=1` on `GET /api/admin/offline-cache`). **UI:** [static/admin.html](static/admin.html). **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md).

## [5.1.0] — 2026-06-06
- **Clip buttons are now hold-to-activate.** The *Clip Last 30s* and *Clip last…* buttons — on both the fullscreen VLC controls and the on-device player — now require a press-and-hold (same gesture as Stop/Hand-off), with the familiar left-to-right fill showing hold progress. A stray tap no longer kicks off a clip. **UI:** [static/index.html](static/index.html).

## [5.0.1] — 2026-06-06
- **Fix: VLC auto-picked AI subtitles over a real one, and ignored your saved/preferred pick.** When a file had a genuine embedded subtitle whose language VLC couldn't read from a tag (e.g. one titled *"Text with various tags"*) plus an AI-generated sidecar, the AI sub was chosen on every play — and re-selecting your sub on the next episode or on restart didn't stick. Root cause: VLC's *own* sidecar autodetection loaded the AI `.srt` (parsing `eng` from its filename) **before** `_load_all_local_subs` ran, so that pre-loaded track arrived **untagged as AI** and read as a "real English" track. The selection policy then ranked it above the untagged genuine sub (and the misclassification also defeated the lone-real-sub fallback that would otherwise carry your choice across episodes). VLC's sidecar autodetect is now disabled (`--no-sub-autodetect-file`) at all three launch sites so StreamLink is the only thing that loads sidecars — and tags AI subs correctly. Resumed files now load their sidecars up front too, so the subtitle menu stays complete and a saved sidecar pick still resolves. **Docs:** [docs/GOTCHAS.md](docs/GOTCHAS.md), [docs/STT.md](docs/STT.md).

## [5.0.0] — 2026-06-03
- **New: on-demand (just-in-time) streaming — play a non-prepped file on your device *instantly*.** Previously, playing an episode that hadn't been stream-prepped meant waiting for the **entire** file to encode first ("Building stream… 42%") — minutes on a long episode. Now playback starts almost immediately: the server hands the player a *virtual* HLS playlist (computed from the file's duration, no encoding) and transcodes each 6-second segment **just-in-time** as the player requests it — the Jellyfin/Plex model.
  - **Seek anywhere.** Jump to an un-generated spot and the browser shows a brief **Loading…** state while the server generates exactly that part, then resumes there. Seeking back into an already-generated region is instant.
  - **Switchable audio.** On-demand serves one audio track at a time; switching picks a new track and re-buffers briefly. The full ABR quality menu and seamless multi-audio remain benefits of the fully-prepped bundle.
  - **Best of both:** the first play is on-demand *and* the server kicks off the normal full bundle prep in the background (low-priority), so the **next** play of that file uses the rich multi-audio/ABR bundle automatically.
  - Technical: mpegts segments (self-contained, no fmp4 init-segment mismatch across seeks), video always transcodes with forced keyframes every 6 s so segment *N* maps exactly to `[N*6,(N+1)*6)`, per-(source+audio) transcode sessions reaped after 90 s idle. New `POST /api/library/{id}/stream-ondemand`, `GET /api/library/ondemand/{key}/{master.m3u8|media.m3u8|seg_N.ts}`, `POST /api/library/ondemand/{key}/close`. 503 on macOS hosts (no HLS). **Docs:** [docs/STREAMING.md](docs/STREAMING.md) § On-Demand, [docs/API.md](docs/API.md), [docs/GOTCHAS.md](docs/GOTCHAS.md), [docs/BACKEND.md](docs/BACKEND.md).

## [4.27.0] — 2026-06-03
- **New: subtitles get smarter about late-downloaded files, lone options, and your past picks.** Three related changes to how a subtitle track is chosen and remembered:
  - **Upgrade to a real sub when it arrives.** Release `.srt` subtitles routinely finish downloading *after* the video (sequential/streaming download), and discovery happens in waves — so by the time you press play the only usable sub is often the AI-generated one, which then gets applied. The system now keeps watching after playback starts: once a real **preferred-language** sub turns up, it switches to it automatically and shows a brief toast (*"Switched to downloaded English subtitles"*). Runs on **both** players — the TV/VLC path via a server-side watcher (`subtitle_upgrade_loop`), and the on-device player via a 15 s poller (`GET /api/library/{id}/subs`). A manual subtitle pick is never overridden. Admin-toggleable (**on** by default).
  - **A lone subtitle is assumed correct.** When exactly one real subtitle is found and its language can't be read from the filename, it's now treated as the preferred language instead of being skipped in favour of AI. Admin-toggleable (**on** by default).
  - **Per-series subtitle memory.** When you pick a subtitle, that choice (language + whether it was a real, AI, or *off* track) is remembered **per profile, per series** — and re-applied on the next episode, even weeks later. Matching prefers the same kind and falls back across kinds when the exact one isn't present yet (so a remembered "real English" applies the AI track for now and auto-upgrades when the real one lands).
  - **Fix:** the on-device player **never persisted sidecar/AI subtitle picks at all** — `_lpSaveLocalTracks` saved every non-bundle selection as `-1` (off), so a chosen `.srt`/AI sub was forgotten the moment you left the episode. Picks are now saved as a resolvable descriptor (`subtitle_sel`) that survives replays *and* drives the per-series memory above.
  - New `settings.subtitles.upgrade_late_subs` + `single_option` (both default on, in Admin → System → Subtitles); profile `series_subtitle_prefs`; `state` exposes both flags. **Docs:** [docs/STT.md](docs/STT.md), [docs/STREAMING.md](docs/STREAMING.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/API.md](docs/API.md), [docs/ADMIN.md](docs/ADMIN.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [4.26.1] — 2026-06-03
- **Fix: server going "incredibly laggy" after a download (with CPU/RAM and the machine itself perfectly fine).** `get_library()` / `put_library()` ran their disk read + `json.loads` + per-item migration (and the `json.dumps`/write) **synchronously on the asyncio event loop**. Hot loops call these constantly during/after a download — `vlc_progress_tracker` every 2 s, `library_download_monitor` every 5 s, plus every API request — and `library.json` grows after each download (the analyzer writes per-file audio-fingerprint `skip_data`). So every few seconds the whole event loop stalled re-parsing/serializing the entire library, which is invisible to CPU% and to the OS (RDP stayed responsive). The collateral was a flood of `httpx.ReadError`s from `https_proxy.py`: the HTTPS proxy's upstream read timed out while the app's loop was blocked. Fix: both helpers now run the blocking I/O via `asyncio.to_thread` while still holding `_lib_lock`, so the loop stays free.
- **Hardening: swept the rest of the codebase for synchronous filesystem work running directly on the event loop** so a big media/cache directory can't reproduce the same loop-stall lag. Now off-loaded to threads: local-subtitle discovery during playback (`_discover_local_subs` in `_load_all_local_subs` — a recursive `rglob` over `Subs/` folders, run on every play), cache-purge artifact deletion (`_delete_cache_artifacts` — recursive size walk + `shutil.rmtree`) in `cache_autopurge_loop` and the three admin offline-cache delete endpoints, the per-poll bundle-size + sidecar-sub walks in the offline/STT job-status endpoints and `offline-prepare`, and `shutil.disk_usage` in `/api/disk-space`. All are plain `await asyncio.to_thread(...)` wraps of existing pure-FS helpers — no behaviour change.
- **Also:** brought the stale `UI_VERSION` constant (was `4.13.0`) back in sync with the page badge so the client-side auto-reload version check works again.

## [4.26.0] — 2026-06-03
- **New: "Clip" — save & share the last 30 seconds (or a custom length) of whatever's playing.** A new clip control appears in two places: the **fullscreen VLC controls** (clips what's on the TV) and the **on-device player**, where it sits directly under the subtitle selector. Each spot leads with a prominent **Clip Last 30s** button and offers a quieter **Clip last…** button for a custom length (1–300 s).
  - The server cuts a standalone, universally-compatible **MP4 (H.264 + AAC, +faststart)** from the original source ending at the live playback position — re-encoded so it plays/AirDrops anywhere. NVENC when available, else libx264. On mobile the finished clip is handed to the OS **share sheet** (`navigator.share`); on desktop it downloads.
  - **Prepped-only:** clipping requires the file to already be stream-prepped (the HLS bundle on disk). The fullscreen tiles grey out until the episode is prepped; on-device the file is prepped by definition. Backend returns 409 with a hint otherwise.
  - Clips are ephemeral — written under `.clips/<token>/` and auto-purged after 2 hours. New `POST /api/library/{id}/clip` + `GET /api/library/clip/{token}/{filename}`. 503 on macOS hosts (no HLS).
- **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/API.md](docs/API.md), [docs/FRONTEND.md](docs/FRONTEND.md).

## [4.25.0] — 2026-06-02
- **Improved: offline HLS prep can now run the *entire* transcode on the GPU (decode + scale + encode resident in VRAM), not just the encode.** v4.23.0 moved decode onto NVDEC (`-hwaccel cuda`) but frames still downloaded to system memory and every ABR down-rung was scaled on the CPU — so on a busy ladder the CPU still hovered ~50%. For an **all-encode** ladder (source can't stream-copy) on a `scale_cuda`-capable build with an NVDEC-safe source, `_build_hls_ffmpeg_args` now emits `-hwaccel cuda -hwaccel_output_format cuda -extra_hw_frames 8` and replaces software `scale=-2:H` with `scale_cuda=-2:H:format=yuv420p`, so frames never leave the GPU — eliminating both the per-frame GPU↔CPU copy and the CPU scaling. This targets the worst CPU offender (e.g. h265 season packs that re-encode all three rungs).
  - **Why all-encode only:** mixing a `-c:v copy` rung with `scale_cuda` rungs under `-hwaccel_output_format cuda` **deadlocks ffmpeg** — the copy stream races ahead while the muxer / NVDEC surface pool backs up, wedging at low CPU+GPU with no progress and no exit. The all-GPU path is therefore gated to `not copy_original`; copyable H.264 keeps the proven transparent `-hwaccel cuda` path (cheap copy original + CPU-scaled down-rungs).
  - **Triple-gated:** all-GPU requires `_has_cuda_scale()` (build has the filter), `_source_nvdec_safe()` (h264/hevc/mpeg2/vc1/vp9, 4:2:0 8/10-bit — pinning frames to VRAM removes ffmpeg's SW-decode fallback), and `not copy_original` (no deadlock-prone copy rung). New `_has_cuda_scale()` probe, cached; cleared on ffmpeg install.
  - **Self-recovering from hangs:** a `GPU_STALL_TIMEOUT_SECS=90` watchdog kills the encode if `out_time` stops advancing (a residual cuda deadlock), and `_run_offline_job` then **retries once** on the transparent path — so the GPU pipeline can never leave a file unpreppable or hung. `-extra_hw_frames 8` enlarges the surface pool the parallel `scale_cuda` branches draw from to avoid the stall in the first place.
- **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [4.24.0] — 2026-06-02
- **New: admin "Force Stream Prep" — an on-demand prep that viewers and host activity can't stop.** Overnight / Idle prep are deliberately interruptible (the non-admin Pause control and the activity-kill both halt bulk prep). Force-prep is the opposite: a new **Force Stream Prep** card on Admin → System preps the whole library (or one selected item) for on-device streaming *right now*, and it **runs to completion regardless of the viewer Pause control or live host activity**. Only the admin can stop it.
  - These jobs use a dedicated `"admin"` prep queue. Like interactive (play-on-device) prep they ignore the bulk pause gate (`state.prep_paused`) and the activity-kill (`_pause_prep` / `_activity_kick` only touch `bulk`), and they preempt any in-flight bulk encode. Unlike interactive prep, the admin can halt them.
  - **Soft stop** ("Stop (finish file)") lets the in-flight encode finish — so it's cached — then cancels every queued file. **Hard stop** ("Stop Now") terminates the running ffmpeg immediately (the partial bundle is dropped) and cancels the rest. A stopped batch is *not* auto-resumed; starting a new force-prep clears the stop.
  - Bulk (overnight / idle / manual) prep now defers to admin force-prep the same way it already defers to interactive prep (`_priority_hls_pending` counts both non-bulk queues). Disabled on macOS hosts (no HLS).
  - `GET`/`POST /api/admin/force-prep` (`{item_id?}`) and `POST /api/admin/force-prep/stop` (`{hard}`).
- **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md).

## [4.23.0] — 2026-06-02
- **Improved: offline HLS prep now decodes on the GPU (NVDEC) when NVENC is in use, instead of pegging the CPU.** Previously the NVENC path offloaded only the *encode* to the GPU — the source was software-decoded and every ABR down-rung software-scaled on the CPU. On a box with an NVIDIA GPU that left the CPU at 80-90% while the GPU idled near 50% (it was only doing the small 720p/480p encodes). `_build_hls_ffmpeg_args` now inserts `-hwaccel cuda` before `-i` whenever NVENC is selected *and* a rung actually decodes (not a lone stream-copy), routing decode through NVDEC — the single heaviest CPU cost.
  - Deliberately the **transparent** form (no `-hwaccel_output_format cuda`): frames auto-download to system memory to feed the existing `scale=-2:H` filter, and ffmpeg **silently falls back to software decode** for any source codec NVDEC can't handle — so this can never hard-fail a job the pure-CPU path would have completed. The libx264 (no-GPU) path is unchanged.
- **Docs:** [docs/STREAMING.md](docs/STREAMING.md).

## [4.22.0] — 2026-06-02
- **Improved: the Profile Settings window now opens instantly with per-section loading indicators instead of waiting for every fetch to return.** Previously `openProfileSettings()` `await`ed seven settings requests sequentially and only revealed the modal once all of them resolved — so on a slow link the settings button felt unresponsive (nothing happened until everything was ready). The modal now appears immediately; each control is dimmed + pulses (`.ps-loading`) with its value label showing "…", and fills in independently as its own request lands. All loaders fire concurrently, so total load time is bounded by the slowest request rather than their sum.
  - No API or persistence changes — purely a frontend responsiveness improvement in `static/index.html`.
- **Docs:** [docs/FRONTEND.md](docs/FRONTEND.md).

## [4.21.0] — 2026-06-02
- **New: configurable VLC starting volume.** VLC previously always opened at exactly half the max-volume cap (hard-coded `cap // 2`). A new **VLC Starting Volume** slider in the dashboard settings panel sets the launch volume as a **% of the max-volume cap** (0–100, step 5), defaulting to **50%** — preserving the old half-max behaviour. Applied when VLC is launched at startup. Persisted under `library.json → settings.vlc_start_volume`.
  - `GET`/`POST /api/settings/vlc-start-volume`. Startup now computes `round(cap * pct / 100)` instead of `cap // 2`.
- **Docs:** [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md).

## [4.20.0] — 2026-06-02
- **New: auto-purge orphan offline-cache files once the cache outgrows a size cap (admin setting).** The `.offline_cache/` directory had no automatic eviction — orphan bundles (re-encoded/removed sources, or files left by a deleted library item) accumulated until an operator manually hit "Purge All Orphans". A new **Auto-Purge Orphans** card on the Admin → Offline Cache tab adds an enable toggle + a **Purge When Cache Reaches (GB)** threshold (1–10000, default 50). Persisted under `library.json → settings.cache_autopurge`.
  - A new `cache_autopurge_loop` background task re-checks every 5 min: when enabled and the total cache size is at/above the cap, it deletes every *orphan* bundle (the same set the manual purge clears). **Bundles backing current library files are never touched**, so this can't evict something a viewer might still want, and active prep jobs are skipped (the existing `_offline_cache_path_active` guard).
  - The size walk reuses `_build_offline_cache_inventory` (offloaded to a worker thread), so the event loop never stalls; the walk only runs while the feature is enabled. The card shows a "last run" line with what was freed.
  - **Windows/Linux/macOS:** pure `pathlib` + `shutil.rmtree`, identical across platforms.
  - `GET`/`POST /api/admin/cache-autopurge`.
- **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md).

## [4.19.0] — 2026-06-02
- **New: on-device (in-browser) playback now surfaces aggressively-discovered sidecar subs too — including those in `Subs/` folders — independent of HLS prep, with on-demand conversion.** The on-device subtitle list came from `_list_sidecar_subs`, which only looked for exact-stem `.srt`/`.vtt` files *next to* the video, so subs tucked in a `Subs/`/`Subtitles/` folder never appeared. It now uses the same `_discover_local_subs` sweep as the TV path, so every sidecar the box can find is offered in the subtitle dropdown. These are served straight from disk and don't depend on the HLS bundle being prepped.
  - **On-demand conversion ("prep when clicked").** The `/api/library/{id}/subtitle` endpoint now serves subs from subfolders (validated via a `path` that must resolve *inside* the item's media tree — no arbitrary file reads) and converts `.ass`/`.ssa` → WebVTT via ffmpeg on demand (`.srt` inline, `.vtt` passthrough as before). So formats the browser can't render directly now work on-device.
  - **Loading indicator.** Selecting an on-disk sidecar (whose WebVTT the server may be generating on first request) flashes a "Loading subtitles…" hint that clears when the `<track>` finishes loading, or shows an error if it can't — so the pick never silently does nothing.
  - Image-based `.sub` (VobSub) is intentionally **not** offered on-device (it needs OCR/burn-in); VLC still loads it natively on the TV path.
- **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/API.md](docs/API.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [4.18.0] — 2026-06-01
- **New: subtitles are now found aggressively and *all* of them load into VLC, with the default language auto-selected.** Releases stash sidecar subs in places VLC never auto-loads — most commonly a `Subs/` folder (flat for a single movie, or per-episode subfolders for season packs) — so subs were frequently "missing" even when present on disk. The subtitle-default policy now runs a new `_discover_local_subs(video)` sweep that covers the real layouts: subs next to the file (`Movie.srt`, `Movie.eng.srt`), and `Subs/` / `Subtitles/` / `Sub/` / `Subtitle/` folders **beside the video and one level up** (for video-in-its-own-subfolder releases), recursing into per-episode subfolders. Every match is loaded into VLC via `addsubtitle` so they're all selectable from the subtitle menu, then the track matching `settings.subtitles.default_language` is selected (embedded tracks rank ahead of sidecars; "Any" selects the first available).
  - Languages are parsed from filenames including full names — `2_English.srt`, `3_Brazilian.Portuguese.srt`, `Movie.eng.srt` — via the new `_parse_sub_lang`, since VLC rarely tags loose sidecars. Recognised extensions: `.srt .vtt .ass .ssa .sub`.
  - Season-pack safety: the lone-video "take every loose sub" fallback is restricted to the video's *own* directory, so a shared `Subs/` folder one level up can't leak a neighbouring episode's subs.
  - AI sidecars (`<stem>.<lang>.ai[.<model>].srt`) are picked up by the same discovery, so the old separate AI-sidecar policy step is gone (no behaviour lost).
  - **Windows/Linux/macOS:** discovery is pure `pathlib` (case-insensitive de-dupe, backslash paths handed to `addsubtitle` as `str(path)` exactly as the existing download path does), so it works identically across platforms.
- **Docs:** [docs/GOTCHAS.md](docs/GOTCHAS.md) (two subtitle gotchas updated/added).

## [4.17.4] — 2026-06-01
- **Fix: on-device Prev episode dot showed "not prepped" even when it was.** The readiness dot reads the in-memory `prepFileState`, which only knew about files prepped during the current session — an earlier episode already in the server's `.offline_cache` had no entry and so showed gray. `lpPlay` now hydrates prep-readiness for the whole item from `/api/library/{id}/prep-status` (which reports `cached` for on-disk bundles) before the player loads, so the Prev/Next dots are accurate from the start. The hydrate is awaited before the next-episode warm-prep so it can't clobber that "prepping" mark.

## [4.17.3] — 2026-06-01
- **Fix: on-device Prev / Next episode buttons still didn't appear** (4.17.1 didn't fully fix it). The single-file→full-series expansion read `item.files` from the cached `/api/library` list, but that list only carries `first_file`, not the full `files` array — so the lookup was always empty and the playlist stayed a single file (no neighbours → buttons hidden). `lpPlay` now fetches `/api/library/{id}/files` (season/episode-sorted) to expand a single-file play to the whole series, positioned at the chosen file. Best-effort: falls back to the single file on any error. This is why the buttons worked on the TV (VLC `prev`/`next` already fall back to the item's full file list server-side) but not on-device.

## [4.17.2] — 2026-06-01
- **Fix: wrong item playing on VLC after Stop / Next (a stale episode, or the background video).** VLC's HTTP `in_play` **appends** to its playlist and never clears it, so over a session VLC's list grew to `[bg, epA, epB, …]` — and every leftover entry was a live auto-advance target. That caused two glitches: (1) hitting **Stop** sometimes played an *episode* instead of the background video (replaying the bg entry already in the list ended and VLC auto-advanced into a stale episode), and (2) skipping to the **next episode** sometimes played the *background video* instead (a leftover bg entry won an end-of-file auto-advance during the transition). Now VLC's playlist is **emptied (`pl_empty`) immediately before every fresh `in_play`**, so it always mirrors the intended playlist and the only auto-advance target is the real next episode.
- **Backend:** new `vlc_clear_playlist()` helper, called from `_library_play_launch`, `_vlc_relaunch_playlist` (prev/next), `vlc_next_file` (natural auto-advance), the stream-now play, `_play_background_video`, and `_stop_cleanup`. The VLC-process-restart paths (retry, night-mode relaunch) are unaffected — they start with an empty playlist.
- **Docs:** [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [4.17.1] — 2026-06-01
- **Fix: Prev / Next episode buttons weren't showing in the on-device player.** They only appear when the playlist has a neighbouring episode, but the common play paths (per-episode **Play**, **Resume**, single-file **On Device**) hand the player a one-file playlist — so there was nothing to navigate to and both buttons stayed hidden. `lpPlay` now expands a single-file play to the item's **full ordered episode list** (season/episode-sorted), positioned at the chosen file, so Prev/Next (and their prep-readiness dots) appear across the whole series. Intentional multi-file queues (selected episodes / **Play All** / handoff tail) are kept verbatim; falls back to the single file if the item isn't in the library cache.

## [4.17.0] — 2026-06-01
- **New: Prev / Next episode buttons in the on-device player, with auto-prep of the next episode.** The fullscreen on-device player header now has **Prev** and **Next** episode buttons (hold-to-activate, same gesture as the TV controls and Stop). A small square dot on each shows whether that neighbouring episode is **ready to stream** (green), **preparing** (amber), or **not prepped yet** (gray) — but the **hold always works regardless of state**; if the target isn't prepped, the player just shows the usual "Building stream…" overlay while it preps on demand.
  - While you watch an episode on-device, the **next episode is now warmed automatically** (interactive HLS prep) so auto-advance — or a Next hold — resumes within a network round-trip instead of a cold encode. This mirrors the VLC "auto-prep on play" chain but is driven client-side from the on-device player, since on-device playback doesn't go through VLC's `play_library_item`.
  - The warm-prep is **interactive** (bypasses the bulk pause gate, preempts bulk work) because you're actively watching the series. Fire-and-forget: a failure just leaves the next episode un-prepped, and the Next hold / auto-advance preps it on demand.
  - Buttons live in the player's `.lp-chrome`, so they're hidden in tiny/corner mode (maximize first) — same as the To TV / Stop controls. **Not on macOS hosts** (no HLS), like every other on-device feature.
- **Frontend:** `lpNavEp` / `lpPrevEp` / `lpNextEp`, `_lpRenderNavButtons` (the prep-readiness dot), `_lpWarmNextEp`, `#lpPrevEpBtn` / `#lpNextEpBtn` in the local-player header, both hooked from `_lpLoadIndex`.
- **Docs:** [docs/STREAMING.md](docs/STREAMING.md).

## [4.16.0] — 2026-05-31
- **New: auto-prep for on-device when you play on the TV.** Playing any library episode on VLC now immediately HLS-preps **that episode** for on-device streaming, then works through the rest of the playlist one episode at a time. If you resume an episode with **under 5 minutes left**, that episode is skipped and the **next** one is prepped instead (prepping the one you're about to finish would be wasted). The chain prepares each episode fully before starting the next, so the episode you're most likely to reach next is always ready first.
  - These prep jobs are **interactive**: they run **regardless of the Overnight / Idle Auto-Prep settings and regardless of whether someone is using the box** — the bulk pause gate and the activity-kill never touch them. They do preempt in-flight *bulk* prep so the watched series is prioritised.
  - Starting a new play cancels the previous chain, so only the series you're currently watching is being prepped ahead.
  - **Admin → System → Auto-Prep on Play** (new card): a single Enable/Disable toggle. **On by default.**
  - **Not on macOS hosts** (no HLS) — same as every other on-device feature.
- **Backend:** `_play_prep_cfg` / `PlayPrepReq`, `_maybe_start_play_prep` + `_play_prep_chain` + `_start_interactive_prep_job` (`PLAY_PREP_TAIL_SECS = 300`), `state.play_prep_task`, hook in `play_library_item`, and `GET`/`POST /api/admin/play-prep`.
- **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/ADMIN.md](docs/ADMIN.md).

## [4.15.1] — 2026-05-31
- **Fix: interactive prep now jumps the queue ahead of bulk prep.** Holding **Prep for Device** (or play-on-device) while overnight / idle / manual `/prep-all` work was running would still queue *behind* it — the single encode slot was busy, so your file waited (potentially minutes) for the in-flight bulk file to finish. Now an interactive prep **preempts** the running bulk encode (terminates it, re-queues that file to resume afterwards) and bulk jobs **defer** to any pending interactive prep, so the user-initiated file starts within a tick. The booted bulk file restarts from scratch (HLS can't checkpoint mid-file) once interactive prep clears; the global pause gate and overnight/idle scheduling are untouched.
- **Backend:** `_preempt_running_bulk` (called from `offline_prepare` for every `bulk:false` request), `_interactive_hls_pending` (the precedence gate in `_run_offline_job` — bulk parks before acquiring the slot and yields it back if interactive arrives while queued), `_requeue_offline_job`, and a `_preempted` re-queue branch in `_run_offline_job`.
- **Docs:** [docs/STREAMING.md](docs/STREAMING.md).

## [4.15.0] — 2026-05-31
- **New: prep-to-device straight from the fullscreen player.** When a library file playing on the TV isn't yet prepped for on-device streaming, the fullscreen **To Device** tile no longer just greys out — it asks **"Prep for Device?"**. **Hold** it and the HLS encode starts immediately **while VLC keeps playing** (the TV is *not* stopped). The tile then shows live progress (**"Prepping 42%"** with a fill bar), and once the bundle is ready it flips to **"Play To Device"** — hold again to resume on this device, time-synced, stopping the TV. Previously you had to leave the player, open the episode picker, and Prep the row by hand before any handoff was possible.
  - The hold gesture is the trigger; the once-per-session CPU-lag warning still appears the first time. Prep runs as an **interactive** job (bypasses the global prep-pause gate and the idle-prep activity kill) so it genuinely starts while you watch.
  - A prep kicked off elsewhere (episode picker / Prep-for-Streaming) is reflected on the tile as an indeterminate **"Prepping…"**, flipping to **"Play To Device"** when it finishes.
  - macOS hosts (no HLS) keep the old greyed **"Not prepped"** tile; the footer **Device** button is unchanged.
- **Frontend:** new `_renderFcHandoff` (tile state machine, called from `renderPlayer` + the prep poll), `fcDeviceTileHold` (hold dispatcher), `prepCurrentForDevice` (interactive prep + progress polling), `_finishFcPrep`. New `app._fcPrepFile` / `app._fcPrepPct` state and `#fcHandoffLabel` / `#fcHandoffBar` elements.
- **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/FRONTEND.md](docs/FRONTEND.md).

## [4.14.0] — 2026-05-31
- **New: unified subtitle policy + explicit VLC enforcement.** Fixes three long-standing subtitle problems: online search burying English, no control over which language/track is auto-selected, and VLC turning subtitles **on** even when the UI said off.
  - **Admin → System → Subtitles** (new card): **Default Subtitle Language** (one language drives online search, automatic track selection, *and* AI generation — defaults to English when unconfigured), **Subtitles On By Default** (off out-of-the-box), and **Auto-Search Online** (on out-of-the-box). The AI card's old "Preferred Subtitle Language" picker moved here (now unified).
  - **Per-profile override:** Profile Settings gains a **Subtitles** selector — Default / On / Off — so each viewer can override the admin default. Stored as `profile.subtitles_on` (null = inherit).
  - **Explicit on/off:** on every play (and prev/next/re-apply) StreamLink now sends VLC an explicit `subtitle_track` — `-1` when subs should be off (VLC otherwise sneaks its auto/forced sub on), or the chosen track when on. Selection priority when on: embedded/loaded preferred-language track → online auto-search download in that language → AI sidecar → otherwise off. A saved per-file pick still wins.
  - **Smarter search:** the **Find Subtitles** modal defaults its language filter to the preferred language (with an "All languages" option), so English surfaces instead of a handful of foreign hits. `GET /api/subtitles/search` defaults a blank `lang` to the preferred language; `lang=all` searches everything.
- **Backend:** new `settings.subtitles` block via `_subs_cfg` (the unified language re-sources `_stt_cfg`); `_apply_subtitle_policy` (called from `_apply_track_prefs`); `_vlc_subtitle_tracks` (factored from `/api/vlc/tracks`); `_download_and_attach_subtitle` + `_auto_fetch_subtitle` (refactored from the download endpoint). New `GET`/`POST /api/admin/subtitles`, `POST /api/profiles/{id}/subtitles`. `state_snapshot.subtitle_default_language` (mirrored on `state`, seeded at lifespan, broadcast on save). `POST /api/admin/stt` no longer takes `default_language`.
- **Docs:** [docs/API.md](docs/API.md), [docs/ADMIN.md](docs/ADMIN.md), [docs/BACKEND.md](docs/BACKEND.md), [docs/FRONTEND.md](docs/FRONTEND.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/STT.md](docs/STT.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [4.13.0] — 2026-05-31
- **New: Night Mode intensity presets — Light / Medium / Max.** A picker in **Profile Settings → Global** (under the Night Mode toggle) lets you choose how hard the compressor flattens the loud–quiet gap: Light (gentle leveling), Medium (balanced, default), or Max (strongest — low threshold + 12:1 ratio). The picker is **settings-menu only** (not in the fullscreen UI). The chosen preset is **remembered independently of the on/off toggle** — turn night mode off and back on and it reuses the same intensity.
- **Backend:** `NIGHT_MODE_ARGS` → `NIGHT_MODE_PRESETS` (per-preset `compressor` arg sets), mirrored in `run.py`/`watchdog.py`. New `settings.vlc_night_mode_preset` + `state.vlc_night_mode_preset` (seeded at startup, in `state_snapshot`). `POST /api/settings/night-mode` now takes optional `{night_mode?, preset?}` (merged), and only relaunches VLC when it changes the *running* filter — turning on/off, or changing preset while on; a preset change while off just persists. `GET` returns `{night_mode, preset, presets[]}`.
- **Docs:** [docs/API.md](docs/API.md), [docs/BACKEND.md](docs/BACKEND.md), [docs/FRONTEND.md](docs/FRONTEND.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [4.12.0] — 2026-05-31
- **New: Night Mode for VLC (dynamic-range compression).** Evens out the gap between the quietest and loudest sounds so quiet dialogue stays clear at low room volume without explosions being jarring — the classic "midnight mode". It's VLC's `compressor` audio filter (low threshold + high ratio to tame peaks, makeup gain to lift dialogue, RMS detection + slow release so it doesn't pump on speech). Persists until you turn it off.
  - **Two entry points, deliberately subtle:** a small moon toggle in the fullscreen-controls header (opposite Close) and a checkbox in the **Global** section of Profile Settings. Toggling restarts VLC briefly (see below) and resumes at the same spot.
- **Backend:** VLC has no runtime HTTP command to add/remove an audio filter, so night mode is a launch arg. `POST /api/settings/night-mode` persists `library.json → settings.vlc_night_mode` and `_apply_night_mode` snapshots the playing file + position, relaunches VLC (`_restart_vlc_process` appends `NIGHT_MODE_ARGS` when enabled), then replays the playlist tail, seeks back, and re-applies saved track prefs. `run.py` (`start_vlc`) and `watchdog.py` (`vlc_spec`) read the same setting so boot / crash-recovery relaunches honour it. New `state_snapshot.vlc_night_mode`; `state.vlc_night_mode` seeded at lifespan startup. A no-op toggle skips the relaunch.
- **Docs:** [docs/API.md](docs/API.md), [docs/BACKEND.md](docs/BACKEND.md), [docs/FRONTEND.md](docs/FRONTEND.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [4.11.0] — 2026-05-31
- **New: admin can cap seeding and bandwidth (Admin → System → Seeding & Bandwidth).** Three global qBittorrent controls, applied live and persisted by qBit itself:
  - **Stop Seeding at Ratio** — enable + a share-ratio value. Once a torrent's uploaded ÷ downloaded hits the ratio, qBit **stops seeding it but keeps the files** (action fixed to "pause", not remove). E.g. a 1.0 ratio stops a 10 GB show after it has uploaded 10 GB.
  - **Max Upload Speed** / **Max Download Speed** — global caps in MiB/s (0 = unlimited) so torrents don't saturate the connection.
- These are **global** qBittorrent settings (apply to every torrent, stream-now and library) read/written live via the qBit Web API (`app/preferences` for the ratio, `transfer/*Limit` for speeds — bytes/sec, no KiB-vs-bytes ambiguity). qBit persists them in its own config, so they survive a qBit or host restart. The download scheduler never resumes a torrent qBit paused at its ratio (it only touches download-phase torrents), so seeding stays stopped.
- **Backend:** `qbit_get_preferences` / `qbit_set_preferences` / `qbit_get_speed_limit` / `qbit_set_speed_limit` / `qbit_global_limits`; `GET`/`POST /api/admin/qbit-limits`. **Frontend:** new System-tab card + `loadQbitLimits` / `saveQbitLimits`.
- **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md), [docs/BACKEND.md](docs/BACKEND.md).

## [4.10.0] — 2026-05-31
- **Fixed: the host stayed laggy long after idle-prep "paused" (sometimes needing a reboot).** The activity-pause only killed the in-flight HLS ffmpeg — the **whisper (AI-subtitle) transcription kept running**, churning CPU/GPU until it finished on its own (potentially very long on CPU), so the box was barely usable in the morning. Whisper (and its audio-extract ffmpeg) are now **cancellable**: an activity-pause kills the running whisper subprocess immediately, the job re-queues as paused, and the GPU→CPU fallback retry is suppressed when the kill was intentional (so it doesn't relaunch). `stt.generate` gained `on_proc` / `cancel_check`; `_pause_prep(kill=True)` now terminates running bulk STT too.
- **New: a viewer shows up → background work stops instantly.** `_activity_kick` pauses+kills bulk prep/STT the moment a viewer interacts **or merely opens the dashboard** (fired from the activity middleware *and* on SSE connect — a page load is a GET that wouldn't otherwise count), instead of waiting up to a 15 s scheduler tick. Idle prep now also treats an open dashboard as "in use" (`_machine_in_use(for_prep=True)`), so it stays paused while you're on the site and only resumes once you leave + the box goes idle. Only applies when idle-prep governs and outside the intentional overnight window; the scheduled reboot still ignores open tabs. Combined with the whisper kill, performance recovers in seconds, not minutes.
- **New: host performance warning + admin health monitor.** A `system_monitor_loop` samples **CPU / RAM / GPU (nvidia-smi) / network** every 5 s and classifies each `ok` / `degraded` / `overloaded`. The dashboard shows a non-blocking **"host is busy — performance may be reduced"** banner whenever the host is degraded/overloaded (auto-clears when it returns to normal), naming what's hot. **Admin → System → System Health** shows the live per-resource status (and whether background prep is running). Exposed via `sys_status` in every `state` SSE event + `GET /api/admin/system-resources`.
- **Docs:** [docs/STT.md](docs/STT.md), [docs/BACKEND.md](docs/BACKEND.md), [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md), [docs/FRONTEND.md](docs/FRONTEND.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [4.9.0] — 2026-05-31
- **New: download scheduling right inside the episode picker — whole torrent, per season, and per episode.** The episode page now has a download-control bar (shown for any torrent-backed item):
  - **This season / All files**: ⬇ Now · 🌙 Idle · ⊘ Skip — schedules the visible season's files (or all files on a single-season item).
  - **All** (multi-season): ⬇ Now · 🌙 Idle — schedules the **entire torrent** (resets per-file overrides so even previously-skipped files are included).
  - **Per episode**: a not-on-disk episode shows ⊘ Not downloaded (skipped) or 🌙 Idle — deferred with a **⬇ Download** button to fetch it to the host now; an actively-downloading one shows its live %.
- **Fixed: the per-episode "download" button only offered download-to-**device**, and showed even for files that aren't on disk** (so it downloaded an empty/partial file). The device-download link now appears **only for files that are actually complete**; not-yet-downloaded files get the host **⬇ Download** action instead.
- **Robustness:** `GET /files` per-file `complete`/`dl_pct` now falls back to a basename match if the qBit full-path key drifts (save-path differences), so a finished partial download no longer mislabels absent files as complete. `/files` also returns `has_torrent` (gates the controls).
- **Backend:** `download-schedule` accepts `reset_files` (whole-torrent now/idle, clearing per-file overrides). **Frontend:** `epSchedSeason` / `epSchedItem` / `epDownloadSkipped` (host fetch) + `epHasTorrent`/`epDownloadMode` from `/files`.
- **Docs:** [docs/API.md](docs/API.md), [docs/FRONTEND.md](docs/FRONTEND.md).

## [4.8.0] — 2026-05-31
- **New: full per-file download control on a finished partial download, including "download this skipped file now."** Once a partial selection's kept files finish, you keep the same Top / Now / Idle / Skip controls you had while downloading:
  - The library card shows the **Files** expander on a partial item (⊘ Partial) even after it's ready, with the full per-row schedule controls + progress.
  - The **episode picker** now has a **⬇ Download** button on every ⊘ Not-downloaded (skipped) episode; re-enabled files immediately switch to a live "⬇ Downloading…%" state.
  - Re-enabling a skipped file (or moving an idle file to now) on a finished item flips it back to **downloading** so the file actually fetches and the monitor/scheduler resume managing it — `downloading→ready` still happens automatically when the kept set is complete again.
- **Backend:** `_apply_item_schedule` reconciles qBit after a schedule change and reactivates a `ready` item when a non-skip file is no longer on disk; the `download-schedule` / `file-schedule` endpoints route through it. `get_item_files` now derives `complete`/`dl_pct` from live qBit per-file progress regardless of mode, so a file that was downloaded then later marked skip still reads as complete/playable.
- **Frontend:** the Files expander is exposed on ready partial cards (and re-expands across re-renders); episode-picker rows distinguish skipped (offer download) from actively-downloading (show %).
- **Docs:** [docs/API.md](docs/API.md), [docs/BACKEND.md](docs/BACKEND.md), [docs/FRONTEND.md](docs/FRONTEND.md).

## [4.7.1] — 2026-05-31
- **Fixed: a partial download (some files skipped) looked fully complete once it finished.** The library monitor flipped an item to "ready" purely on qBittorrent's torrent state — but with skipped (and idle-deferred) files at priority 0, qBit reports the torrent "complete" while those files are still absent. So a partial selection showed every episode as downloaded, audio fingerprinting ran against a missing set, and an idle-deferred file could be abandoned (the item went ready before it fetched).
  - **Ready is now gated on per-file completion**: an item flips ready (and only then audio-fingerprints) once **every non-skip file** is fully downloaded — including idle-deferred ones. Skipped files are excluded from fingerprinting and from the Smart Skip status.
  - **Partial state is now visible**: the library card shows a **⊘ Partial** badge when any file was skipped, and the episode picker marks skipped episodes **⊘ Not downloaded** (dimmed, not playable, prep/download hidden). `GET /files` now reports true per-file `complete`/`dl_pct` from qBit for ready items too (a skipped file is never "complete").
  - A mixed now+idle download correctly reads "Waiting for idle window" while it seeds the now-files and waits for the idle window to fetch the rest.
- **Backend:** `_all_nonskip_complete` (the ready gate) + `_analyzable_files` (skip-aware file set, used by the monitor, `_run_series_analysis`, `_schedule_series_analysis_if_eligible`, and `_item_skip_status`); `get_item_files` queries qBit for any torrent-backed item; `library` list gains `download_partial`.
- **Docs:** [docs/BACKEND.md](docs/BACKEND.md), [docs/API.md](docs/API.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [4.7.0] — 2026-05-30
- **New: download scheduling — pause, idle/night-only, and per-file/folder priority.** A new background task (`download_scheduler_loop`) is the single source of truth for each downloading item's qBittorrent file priorities + torrent pause/resume:
  - **Idle/night-only downloads.** A "Download at idle/night only" toggle in the download modal (and a per-card **⏸ Idle** / **▶ Resume** button) holds a download until the host is idle or the admin's overnight window is open, then auto-resumes there — and pauses again when activity returns. The idle window **reuses** the existing Admin → System prep schedules (Overnight Stream Prep / Idle Auto-Prep); no new admin config.
  - **Per-row + per-folder priority.** The "Files" expander is now folder-grouped with inline **Top / Now / Idle / Skip** controls on every row (and folder-level bulk actions) — no more scrolling to a single button at the bottom. So you can mark one episode/folder to download **now** while the rest wait for idle.
  - **See & play complete files mid-download.** Each file row shows its own download progress + a **✓ Complete** badge, and a complete file can be played to VLC immediately even while the rest of the torrent is still downloading.
- **Backend:** per-item `library.json → download` model (`{mode: now|idle, files: {<path>: now|high|idle|skip}}`); `_download_idle_open` (reuses `overnight_prep`/`idle_prep`, with `_machine_in_use(..., ignore_downloads=True)` so a running idle-download can't self-close its window); `_reconcile_item_downloads` maps schedule → live qBit priority + pause/resume; `qbit_pause`/`qbit_resume` (5.x `/stop`·`/start` fallback). New `POST /api/library/{id}/download-schedule` + `/file-schedule`; enriched `/files` (`mode`/`dl_pct`/`complete` + window state); `library` list gains `download_mode`; `library_progress` gains `download_mode`/`paused`; `state_snapshot` gains `download_idle_open`/`download_idle_configured`. `queue-play` + `library_download_pipeline` now route through the model (no raw `filePrio`); the old `/file-priority` endpoint is removed.
- **Docs:** [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md), [docs/BACKEND.md](docs/BACKEND.md), [docs/API.md](docs/API.md), [docs/ADMIN.md](docs/ADMIN.md), [docs/FRONTEND.md](docs/FRONTEND.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [4.6.0] — 2026-05-30
- **New: "Show all branches (developer mode)" in the auto-updater.** A checkbox under the Updates-tab branch picker repopulates it from a live `git ls-remote` of origin (canonical **main / beta / alpha** first, then every other branch alphabetically) so a developer can point the box at any feature branch — not just the curated three. With the box off, behaviour is unchanged and a stale non-canonical branch is sanitised back to `main` on read.
- **Backend:** branch gating is now centralised in `updater.branch_allowed(branch, allow_any)` and threaded through `check_update` / `switch_branch` / `apply_update` / `reset_hard` (+ the `/config`, `/apply`, `/switch-branch`, `/reset-hard` endpoints) as `allow_any`. Without dev mode only `ALLOWED_BRANCHES` pass; with it, any **structurally-valid** branch passes (a guard rejects leading `-`, `..`, `//`, whitespace, and HTML-unsafe names, so the relaxed picker can't smuggle in git option injection). New `GET /api/admin/updater/branches` lists origin's branches; the UI fetches it lazily (on toggle / first open) rather than on the 4 s status poll. Persisted as `settings.autoupdate.dev_mode`; sent inline on Switch/Apply so it works before an explicit Save.
- **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md).

## [4.5.1] — 2026-05-30
- **Fixed: the AI-subtitle whisper model reverting to `base` after an auto-update / branch switch.** When the admin had installed a larger model (e.g. `medium`) via the Components card, the auto-updater's non-interactive `setup.py` re-ran `merge_tool_paths()`, which refreshed `_WHISPER_MODEL` to the *first* `ggml-*.bin` it globbed (usually `base`) — silently clobbering the admin's choice. `merge_tool_paths` now treats `_WHISPER_MODEL` as a user choice: if the existing `.env` already points at a model file that still exists on disk, it keeps it, only falling back to a detected candidate when the configured model is gone.
- **Docs:** [docs/SETUP.md](docs/SETUP.md), [docs/STT.md](docs/STT.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [4.5.0] — 2026-05-28
- **Improved: AI subtitle timing, especially around long pauses.** Generated subs no longer linger across silence or start early. whisper now runs with **DTW token-level timestamp alignment** (`-dtw`) — it warps the decoder's cross-attention against the audio for accurate word boundaries that respect pauses — plus **word-boundary cue splitting** (`-ml 80` + `-sow`), so each cue carries its own accurate timing and a pause becomes a real gap between cues instead of one stretched block. Needs no extra download (alignment heads are built into whisper.cpp) and applies to every model size and both CPU/GPU builds. Regenerate an existing AI track to pick up the better timing.
- **Backend:** `stt._dtw_preset()` maps the configured model (`model_name()`) to its `-dtw` architecture preset via `_DTW_PRESETS`, disabling DTW for any model it can't map confidently (a mismatched preset would error the run); `STT_MAX_LEN` controls cue length. The CPU-fallback retry keeps the new flags.
- **Docs:** [docs/STT.md](docs/STT.md), [docs/GOTCHAS.md](docs/GOTCHAS.md).

## [4.4.0] — 2026-05-28
- **New: idle-triggered auto stream-prep.** In addition to the fixed nightly window, the library can now auto-prep for on-device streaming any time the host has been idle for a configurable number of minutes — no user interaction, VLC playback, active stream, or running download. The moment activity returns it pauses and **discards the in-flight encode** (HLS prep can't checkpoint mid-file, so that file simply restarts from scratch on the next idle stretch). Configured in **Admin → System → Idle Auto-Prep** (enable + idle-time minutes).
- **Unified scheduler.** The old `overnight_prep_loop` is now `auto_prep_loop`, which owns both triggers so they can't fight over the shared bulk-prep pause gate. "Should prep run now?" = overnight window open **OR** (idle-prep enabled AND idle long enough). A rising edge resumes + enqueues the whole un-prepped library; a falling edge hard-kills when activity is the cause, else honours the overnight `on_end` mode. With idle-prep on, an activity pause always wins over overnight Continue.
- **Backend:** `settings.idle_prep` (`enabled`, `idle_minutes`, clamped 1–720); `GET`/`POST /api/admin/idle-prep`; `state.auto_prep_engaged` replaces `state.overnight_active`. The idle window doubles as the activity detector via the existing `_machine_in_use`.
- **Docs:** [docs/STREAMING.md](docs/STREAMING.md), [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md), [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md).

## [4.3.0] — 2026-05-28
- **New: regenerate AI subtitles when the whisper model changed, and show the model in the track name.** Generated sidecars are now tagged with the model that produced them — `<stem>.<lang>.ai.<model>.srt` (e.g. `Movie.eng.ai.base.srt`). Subtitle track labels in the on-device player show it too: “English (AI · base)”.
- **Regenerate option:** when an existing generated sub was made with a different model than the one configured now (e.g. you upgraded base → medium, or switched to a GPU/larger model), the on-device player's subtitle button switches from **AI** to **Regen** and re-runs with the current model; legacy untagged subs count as stale. `generate()` removes the superseded files after the new ones are written, so it replaces cleanly. The VLC "Generate with AI" action regenerates automatically on a model change (and reports "already generated" when the model matches).
- **Behaviour:** only an *explicit* request regenerates — preprocess/overnight prep stays idempotent (it won't silently re-transcribe the whole library when you change models). `_maybe_start_stt_job` treats same-model subs as cached and different-model subs as regenerable; `_list_sidecar_subs` exposes `model` + `stale` per generated track.
- **Docs:** [docs/STT.md](docs/STT.md).

## [4.2.0] — 2026-05-28
- **New: GPU (CUDA) acceleration for AI subtitles.** whisper.cpp transcription can now run on an NVIDIA GPU via the cuBLAS build — dramatically faster than CPU (a 45-min episode drops from minutes to tens of seconds). The admin Optional Components card gains a build picker for whisper.cpp — **CPU**, **GPU · CUDA 12** (~440 MB), or **GPU · CUDA 11** (~60 MB) — and recommends a CUDA build when an NVIDIA GPU is detected (`nvenc` probe). Note: NVENC (video encode) and CUDA (whisper compute) are different GPU subsystems — this adds the latter.
- **Graceful CPU fallback at runtime.** A CUDA build offloads to the GPU automatically; if CUDA can't initialize (driver too old, no device), `_run_whisper` retries once with `-ng` (force CPU), so a too-new build choice degrades to CPU instead of failing. `-ng` is a no-op for the CPU build.
- **Backend:** `setup._resolve_whisper_win_url(build)` resolves the cpu/cuda12/cuda11 asset from the GitHub releases API (pinned v1.8.4 fallback); `/api/admin/components/install` accepts `build`; `/api/admin/components` reports `nvenc`.
- **Docs:** [docs/STT.md](docs/STT.md), [docs/ADMIN.md](docs/ADMIN.md), [docs/API.md](docs/API.md).

## [4.1.0] — 2026-05-28
- **New: admin Optional Components installer (System tab).** The auto-updater runs `setup.py` non-interactively and skips every `install_*` step, so portable dependencies the box didn't already have — notably whisper.cpp + its model for AI subtitles — never landed without a manual terminal `setup.py` run. New **Optional Components** card lets the admin install/update them from the web: ffmpeg, fpcalc, the whisper.cpp binary, and the whisper model (base/small/medium picker). Downloads stream on the host with live progress and write the paths into `.env`; `detect_tools()` + `merge_tool_paths()` then keep them across future auto-updates, so a one-time install persists.
- **Windows-native, no terminal.** Reuses `setup.py`'s URL-resolution / extract / detect helpers (imported into the running server — its prompts are gated under `__main__`) and streams the download via httpx for progress. ffmpeg / whisper.cpp binaries are offered only on Windows (elsewhere: OS package manager); fpcalc + the model install on any OS.
- **Backend:** `GET /api/admin/components` (status + in-flight job) and `POST /api/admin/components/install {component, model?}`; installs run in the background and clear the ffmpeg-version / NVENC / STT-availability caches so a freshly-installed binary takes effect without a restart.
- **Docs:** [docs/ADMIN.md](docs/ADMIN.md), [docs/SETUP.md](docs/SETUP.md), [docs/API.md](docs/API.md), [docs/STT.md](docs/STT.md).

## [4.0.3] — 2026-05-28
- **Fix: whisper.cpp binary download 404'd in setup.py, so AI subtitles could never become available on Windows.** The download URL pinned a release tag (`v1.7.4`) that doesn't publish the Windows binary zip. The asset *name* (`whisper-bin-x64.zip`) is stable but the tag isn't, so a hardcoded version rots. setup.py now resolves the current URL from the GitHub releases API (`_resolve_whisper_win_url`) and only falls back to a pinned known-good release (v1.8.4) if the API is unreachable. Verified the asset contains `whisper-cli.exe`. The model download (HuggingFace) was always fine.

## [4.0.2] — 2026-05-28
- **Fix: the "Generate subtitles (AI)" affordances only appeared after a full server restart + page reload once whisper.cpp was installed.** Two stale caches: (1) the server's `_stt_available()` cached the whisper probe **forever** at first call, so installing whisper after startup never registered; it now re-probes on a 60 s TTL. (2) the dashboard read `stt_available` once at page load into `sttAvailable`; it now also updates live from the SSE `state` stream (and re-renders the on-device player's track row if a play is active). Net: install whisper, and within ~a minute the Generate option shows up on its own — no restart, no reload. The option is gated solely on STT availability (never on whether the file already has subtitles), as intended.

## [4.0.1] — 2026-05-28
- **Fix: admin → Offline Cache tab froze/crashed the server (stuck on "Loading cache inventory…").** `_build_offline_cache_inventory` did its heavy filesystem walk — `_dir_size_bytes` (recursive `rglob` + `stat` over every segment in every HLS bundle) plus a `stat()` per library file — **synchronously on the asyncio event loop**. With the ABR ladder tripling the segment count per bundle (and a library spread across drives), that walk blocked the loop long enough to wedge the entire server (SSE, VLC polling, every request stalled), which presented as a crash. The request never returned, so the tab sat on "Loading…" forever.
- **Fix:** the inventory now fetches the library + snapshots the job list on the loop, then runs the blocking walk in a worker thread via `asyncio.to_thread` (`_offline_cache_inventory_sync`) — the same discipline `_run_offline_job` already uses for `_dir_size_bytes`/`rmtree`. The job snapshot avoids iterating the live `_offline_jobs` dict off-thread.
- **Docs:** [docs/GOTCHAS.md](docs/GOTCHAS.md) notes never to run a full-cache FS walk inline on the event loop.

## [4.0.0] — 2026-05-27
- **New: AI auto-generated subtitles (speech-to-text).** When a source ships no usable text subtitle — none at all, only image-based (PGS/VOBSUB) tracks, or none matching the admin's preferred language — StreamLink transcribes the audio into a sidecar `.srt` using bundled **whisper.cpp**. The sidecar is picked up by both players through existing plumbing: VLC via `addsubtitle`, the on-device HLS player via `_list_sidecar_subs` → `/api/library/{id}/subtitle`. Non-English audio additionally gets an English-translated track (whisper's translate task is English-only).
- **Both preprocess and on-demand:** generation runs automatically after HLS stream-prep and during the overnight window (off the playback path), and on demand via a **Generate with AI** action in the VLC subtitle menu and an **AI** button in the on-device player's subtitle row. Generated tracks are labelled “(AI)”.
- **Admin:** new System-tab card — enable/disable, a **preferred subtitle language** (a missing match triggers generation; "" = only sub-less files), and an English-translation toggle. `GET`/`POST /api/admin/stt`.
- **Setup:** `setup.py` downloads a portable whisper.cpp build + the multilingual `ggml-base` model into `tools/whisper/` (Windows), or via brew on macOS; paths recorded in `.env` as `_WHISPER_BIN` / `_WHISPER_MODEL`. Optional — declining only disables this feature.
- **Backend:** new `stt.py` module (audio extract → transcribe/translate at lowered OS priority, sharing the offline-prep concurrency semaphore + pause gate); `POST /api/library/{id}/generate-subtitles`, `POST /api/subtitles/generate`, `GET /api/stt-job/{id}`; `state.stt_available`.
- **Docs:** new [docs/STT.md](docs/STT.md); [docs/STREAMING.md](docs/STREAMING.md), [docs/SETUP.md](docs/SETUP.md), [docs/API.md](docs/API.md), [docs/GOTCHAS.md](docs/GOTCHAS.md), [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) updated.

## [3.13.0] — 2026-05-27
- **New: "Reset Hard" button in the admin Updates tab.** Recovery for a wedged or diverged checkout — runs `git fetch` + `git reset --hard origin/<current-branch>`, forcing the working tree back onto the remote and discarding any local commits and uncommitted edits to tracked files. Distinct from Switch Branch: it stays on the current branch and never runs setup.py or reboots. Confirm-gated.
- **Preserves local data:** no `git clean`, so untracked / gitignored files (`library.json`, `.env`, `.offline_cache/`, `.background/`) survive the reset.
- **Backend:** `updater.reset_hard()` (gated to `ALLOWED_BRANCHES` — refuses a detached HEAD or any branch outside main/beta/alpha) + `POST /api/admin/updater/reset-hard` (no body; 409 if another update operation is running). Returns `{ok, branch, commit}`.
- **Docs:** [docs/ADMIN.md § Updates](docs/ADMIN.md) and [docs/API.md](docs/API.md) document the new button and endpoint.

## [3.12.4] — 2026-05-27
- **Fix: in the fullscreen controls, hovering a mouse over the volume −/+ buttons could fire a volume step.** The buttons bind `_fcVolUp` to `onpointerleave` (so dragging off a held button still releases it), but `_fcVolUp` fired `vlcVolumeStep` whenever called with no hold active — and on desktop `pointerleave` fires on a plain hover-out with no preceding `pointerdown`. So moving the mouse across and off a vol button changed the volume. Touch was unaffected (no hover).
- **Fix:** added a `_volPressed` guard set in `_fcVolDown` (pointerdown) and required in `_fcVolUp`; a `pointerleave`/`pointerup` with no matching press is now ignored.

## [3.12.3] — 2026-05-27
- **Fix: the Auto Update admin card reset the branch picker mid-selection, making it nearly impossible to switch branches.** The card polls `/api/admin/updater` every 4 s and `loadAutoupdate()` was unconditionally writing the server's saved config back into the editable controls (branch `<select>`, interval, auto-apply checkbox, enabled toggle). So a branch you picked got snapped back to the saved value within 4 s — you had to race the poll to click Switch/Apply.
- **Fix:** added an `_auFormDirty` guard. User edits to any of those controls (and the enable toggle) set the flag; while it's set, the background poll updates only the read-only status fields (current branch/commit, last check, status message, button enable/disable) and leaves the editable controls alone. The flag is cleared after Save and Switch Branch (which reload to a known server state) and on every fresh open of the Updates tab, so the form re-syncs once your edit is committed.
- **Docs:** [docs/ADMIN.md § Updates](docs/ADMIN.md) notes the poll no longer clobbers in-progress edits.

## [3.12.2] — 2026-05-27
- **Fix (real one): host *still* crashed on Host Volume changes — pycaw/COM now runs out-of-process.** The 3.12.1 single-thread/never-uninit approach did **not** stop the crash: comtypes still took the whole server down with a native access violation (no traceback) after a few rapid OS-volume calls, even pinned to one COM-initialized thread. A native crash can only be contained by an OS process boundary, so all Windows OS-mixer ops now run in a new long-lived child process, **`winvol_helper.py`**. `main.py` spawns one instance, talks to it over stdin/stdout (one JSON line per request: `{"op":"get"}` / `{"op":"set","pct":N}`), and respawns it transparently if it dies. If COM ever blows up again it kills only the helper — the StreamLink server stays up.
- **Backend:** removed the in-process `_windows_volume_op` + single-thread executor; `set_system_volume`/`get_system_volume` now route through `_winvol_request` on Windows (serialized by an `asyncio.Lock`, 5 s timeout, kill-and-respawn on IO error/timeout). POSIX paths (`osascript`/`pactl`/`amixer`) are unchanged. `_PYCAW_LAST_ERROR` is now populated from the helper's error reply so the YouTube-volume endpoint still surfaces a useful message when pycaw is missing.
- **Docs:** [docs/GOTCHAS.md](docs/GOTCHAS.md) entry rewritten to mandate the subprocess boundary for any future OS-volume caller.

## [3.12.1] — 2026-05-27
- **Fix: host crashed after a few Host Volume changes on Windows (no log output).** Root cause was the Windows volume path doing `CoInitialize → work → CoUninitialize` on **every** call, across arbitrary `asyncio.to_thread` pool workers. Tearing the COM apartment down between calls leaves comtypes' cached interface metadata/proxies dangling; a later access (often GC of a COM pointer on a different pool thread) is a native access violation that kills the whole Python process before Python can raise or log anything — hence "first few work, then the server vanishes with nothing in the logs."
- **Fix:** all Windows volume ops now run on a single dedicated worker thread (`_win_volume_executor`, `max_workers=1`) that calls `CoInitialize` exactly once (tracked via a `threading.local` guard) and **never** `CoUninitialize`s for the life of the process. COM is initialized once, no COM pointer is created on one thread and released on another, and the apartment never tears down. `set_system_volume`/`get_system_volume` route through this executor on Windows; POSIX still uses `asyncio.to_thread` (plain `osascript`/`pactl`/`amixer` subprocesses). This also hardens the existing YouTube-on-TV volume path, which used the same per-call init/uninit.

## [3.12.0] — 2026-05-27
- **New: Host Volume slider in the global section of profile settings.** Drags drive the host's OS mixer in real time (pycaw on Windows, `osascript` on macOS, `pactl`/`amixer` on Linux) — no need to walk over to the box to turn it down. Reads the current OS volume on open; greys out (showing "N/A") if the platform helper isn't available.
- **Hardening (server):** `POST /api/settings/host-volume` serializes concurrent writes behind an `asyncio.Lock` and short-circuits if the latest target already matches the last value written. Stops a dragging slider from piling up parallel pycaw COM round-trips (CoInitialize → endpoint enum → Activate → set → CoUninitialize is heavy on Windows; 20+/s of those can wedge the audio subsystem).
- **Hardening (client):** single-flight pump — at most one POST is in flight at a time; further drags only update the pending target and the next POST goes out when the current resolves AND only if the target moved. Combined with the server-side dedupe, a fast drag now produces one OS write per distinct value rather than one per slider tick.
- **UI:** profile-settings modal is now `max-h-[90vh]` with the content area scrollable and the Done button pinned to the bottom — needed because the added Host Volume row pushed the modal off-screen on shorter viewports.
- **Backend:** `GET /api/settings/host-volume` → `{host_volume: 0-100 | null}`; `POST /api/settings/host-volume {host_volume: 0-100}` calls `set_system_volume`. Not persisted in `library.json` — the OS already owns its mixer state and an external change (TV remote, keyboard volume key) shouldn't be clobbered by a stale dashboard value on next launch.
- **Docs:** [docs/API.md](docs/API.md) Settings table updated with both endpoints.

## [3.11.0] — 2026-05-26
- **Major: HTTPS port (443) is now a reverse proxy to the canonical HTTP app, not a second FastAPI instance.** Previously both ports mounted `main:app` and relied on module-global `AppState` being shared across the two `uvicorn.Server` instances inside one process. In practice this produced intermittent state divergence — clients on `https://remote.local` and `http://<lan-ip>` would see different `playing` state until a refresh — because each ASGI mount has its own startup race, SSE buffer, and event-loop scheduling. Replaced with a strict architecture: port 80 serves `main:app` (the only `AppState` in the process), port 443 serves a tiny new `https_proxy:app` that streams every request to `127.0.0.1:80`. There is now provably one source of truth regardless of which hostname or scheme a client uses.
- **New file: `https_proxy.py`.** FastAPI catch-all that forwards method + path + query + headers + streamed request body to the upstream, then streams the response back (so SSE / range requests / large downloads aren't buffered). Strips RFC 7230 §6.1 hop-by-hop headers on both directions. Adds `X-Forwarded-Proto: https`, `X-Forwarded-Host`, and `X-Forwarded-For` so the upstream sees the original client context.
- **`admin_https_redirect` middleware in `main.py` now honors `X-Forwarded-Proto` / `X-Forwarded-Host`.** Without this, every admin hit through the proxy would redirect to `https://127.0.0.1/admin` (loop). With it, `https://remote.local/admin` flows straight through.
- **`run.py` + `daemon._WRAPPER_CONTENT` updated** to point the port-443 uvicorn at `https_proxy:app` instead of `main:app`. The HTTP server still loads `main:app` and runs the full lifespan (qBit login, background tasks). The `lifespan="off"` workaround on the HTTPS server is gone — the proxy app has its own minimal lifespan (creates/closes the upstream `httpx.AsyncClient`).
- **Cost:** an extra ~ms of localhost latency on HTTPS requests for the proxy hop. Negligible for a home server; well worth the elimination of an entire class of sync bugs.

## [3.10.4] — 2026-05-26
- **Fix: Windows service crashed on startup with `RuntimeError: Directory 'static' does not exist`.** Task Scheduler launches the wrapper with CWD = `C:\Windows\System32`, but `main.py` mounts `StaticFiles(directory="static")` with a *relative* path. Result: every `uvicorn.Server` raised on import, the supervisor restarted 5× in 1.0s, hit the fast-death circuit breaker, and the service gave up. Fixed by `os.chdir(HERE)` at the top of `streamlink_service.py` (and the matching `_WRAPPER_CONTENT` template in `daemon.py`) so all relative paths in `main.py` resolve next to the wrapper. macOS launchd and Linux systemd were unaffected because their unit files already set `WorkingDirectory={HERE}` — the bug only surfaced on Windows where `schtasks` has no equivalent.

## [3.10.3] — 2026-05-26
- **Fix: playing media state not shared between HTTP and HTTPS connections; service shuts down immediately on Windows.** Both `run.py` and `streamlink_service.py` were spawning two separate `uvicorn` subprocesses (port 80 HTTP, port 443 HTTPS) with independent `AppState`. Fixed by running both servers concurrently in the same `asyncio` event loop so they share state. Also fixed two service-specific failures: (1) uvicorn's default `dictConfig` adds `StreamHandler`s pointing at `sys.stderr`/`stdout`, which are `None` in a Task Scheduler service (no console) — suppressed via `log_config={"version":1,...}`; (2) the supervisor loop broke on `rc=0` treating a fast startup crash as a clean exit — fixed so any `asyncio.run()` return (non-interrupt) triggers a restart.

## [3.10.2] — 2026-05-26
- **Fix: auto-update was blocked on Windows by a UAC prompt during the "reinstall service" step.** `daemon.install()` on Windows requires admin and tries to UAC-elevate via `ShellExecute(..."runas"...)`; from a service-launched uvicorn there's no interactive desktop, so the elevation either fails or sits waiting for a click that never comes — breaking the "fully automatic" requirement. **Root cause:** we were doing more than necessary. The OS service entry (Task Scheduler task / launchd plist / systemd unit) points at `streamlink_service.py` by *path*, and that path is stable across versions. The thing that actually needs to change between versions is the *contents* of that wrapper script. Writing a file inside the project dir needs no elevation.
- **New flow:** Step 3 of `_run_apply` was changed from `daemon.uninstall() + daemon.install()` to `updater.refresh_service_wrapper()`, which just regenerates `streamlink_service.py` from the freshly-pulled `daemon._WRAPPER_CONTENT`. Plain file write, no admin, no UAC. The OS supervisor keeps finding the same wrapper path; when the reboot brings everything back up it runs the new wrapper code on its own. Idempotent: if the wrapper file is already byte-identical, the rewrite is skipped and the diagnostic panel logs "Wrapper already up to date" instead.
- **Backend:** `updater.reinstall_service()` removed; replaced with `updater.refresh_service_wrapper()` (no `daemon.uninstall()` / `daemon.install()` calls). Phase id `reinstalling-service` renamed to `refreshing-service`; the user-side banner phase set + the admin UI phase message updated. The response field `service_reinstalled` kept for compatibility with the existing toast branch — true means the wrapper write succeeded.
- **When the admin still has to run `python run.py --install` manually:** only when `daemon.py` itself changes its OS-service registration logic (a new plist key, different `schtasks` args, etc.). The wrapper regeneration covers content-only changes, which is the vast majority of updates. If a future release needs a re-registration we'll call that out in the release notes.
- **Docs:** `docs/ADMIN.md` Step 3 description, `docs/API.md` apply-endpoint shape, `docs/GOTCHAS.md` Windows-elevation entry all updated to describe wrapper refresh instead of reinstall.

## [3.10.1] — 2026-05-26
- **Fix: `setup.py` exited rc=1 from the auto-updater on Windows.** Root cause: Python 3.13 on Windows defaults `sys.stdout` to the host's legacy ANSI code page (cp1252 in en-US) when stdout is a pipe, and setup.py's first banner contains the Unicode box-drawing glyphs `┌─┐` — those are unencodable in cp1252, so the very first `print()` raised `UnicodeEncodeError` and the whole process bailed before doing anything. The actual update worked (git pull succeeded) but `_run_apply` correctly refused to proceed past a failed setup.
  - **setup.py** now reconfigures `sys.stdout` + `sys.stderr` to UTF-8 with `errors="replace"` at startup (idempotent; no-op when already UTF-8 — i.e. on a real terminal or POSIX). Any future glyphs degrade to `?` instead of crashing the script.
  - **updater.run_setup** also passes `PYTHONIOENCODING=utf-8` in the subprocess env as belt-and-braces — fixes the same crash even on a setup.py that predates the in-script reconfigure.
- **Make non-interactive re-runs of `setup.py` robust to transient pip / network failures.** New `STREAMLINK_AUTOUPDATE=1` env var (set by `updater.run_setup`) tells setup.py:
  - `pip install` failures are logged as warnings and continue — they don't kill the update. A transient PyPI hiccup shouldn't burn an apply; the existing venv contents are good enough to come back up, and the next apply will catch up.
  - Skip OS-app installs (`install_core_deps`, `install_jackett_service`, `install_smart_skip_deps`) — winget/brew need an interactive desktop and can hang or fail from a service account. Those run only during the initial interactive setup.
  - Skip `offer_service_install()` — the updater calls `daemon.uninstall()` + `daemon.install()` itself right after setup, so re-running it here would risk a Windows UAC prompt blowing up the run (no interactive desktop).
  - Force `reuse_env=True` when an existing `.env` is found — never prompt for re-entry of values during an automated update.
- **Better error surfacing in the admin UI.** When setup.py fails, the apply phase message now appends the last non-empty line of setup.py's output (typically the exception class + message) so the admin sees the actual cause without having to expand the collapsed diagnostic panel. Previously you'd see `setup.py exited rc=1` and have to click to dig.

## [3.10.0] — 2026-05-26
- **Smart Skip now records *why* fingerprinting failed and surfaces it to everyone.** Previously, if `analyze_series` couldn't extract a fingerprint for a file (corrupted audio, unsupported codec, missing ffmpeg/fpcalc, file moved off disk, no peers, etc.) the file was silently dropped from the result and users got no Skip Intro/Skip Credits offer with zero indication why. Now every file the analyzer touches gets a `skip_data` entry — successes carry the usual intro/credits, failures carry `analysis.source = "failed"` with an `error_code` and human-readable `error`. The user-facing library list shows a "⚠ Intro/credits skip not available" chip on items where every file failed (and a softer "Skip partial" chip when only some failed). The admin Smart Skip tab's editor shows a red error block with the full message above each failed file's time inputs, and a new **Fingerprint Log** panel under the item list streams the most recent 200 events (timestamp + error code + filename + message) — refreshes automatically when any in-flight analysis hits a terminal state.
- **Failed files retry on their own.** `_schedule_series_analysis_if_eligible` now treats `analysis.source == "failed"` as eligible for re-analysis, so when a new sibling episode in the series flips to ready, the larger fingerprint pool gets another shot at the previously-failed file — no admin click needed. Manual overrides (`source == "manual"`) are still never overwritten.
- **Backend:** new `analyzer.ERR_*` constants for the seven failure modes (`no_binary`, `file_missing`, `no_duration`, `fp_empty`, `too_short`, `no_skip_points`, `exception`); `analyze_series` always returns one entry per input file. `state.analyzer_log` is a 200-deep `deque` ring buffer fed by a new `_log_analyzer_event` helper. `_run_series_analysis` persists every entry (including failures) into `library.json` then logs one event per failed file. New `GET /api/admin/analyzer-log?limit=N` returns the buffer; `/api/library` adds `skip_status ∈ {ok, partial, failed, pending, none}`; `/api/admin/library` adds `files_failed` and `skip_status`; `/api/admin/library/{id}/skip-data` adds `error_code` and `error` per file. The series-level `analysis_jobs[key].status` now distinguishes "all failed" from "partial success" in its message.
- **Docs:** [docs/ANALYZER.md](docs/ANALYZER.md) gets a new "Failure tracking" section with the full error-code table and downstream consumer list; [docs/LIBRARY_DATA.md](docs/LIBRARY_DATA.md) updates the `skip_data` schema; [docs/API.md](docs/API.md) lists the new endpoint and the augmented field set on the existing ones.

## [3.9.1] — 2026-05-26
- **Apply Now is now full-cleanup: git pull → setup.py → uninstall + reinstall the OS service → reboot the host.** Replaces the previous "SIGTERM uvicorn, hope the supervisor restarts" sequence — the service wrapper (`streamlink_service.py`) is regenerated from the freshly-pulled `daemon.py` before the reboot, and a full host reboot gives the new code a clean process tree on the way back up. Live phase IDs are now `applying → setup → reinstalling-service → rebooting`; the user-side banner tints red while the host is in `rebooting` so non-admin viewers see the disruption coming.
- **Switch back to an older branch from Apply Now in one click.** Apply Now now sends the branch picker's *current* value (not the saved config), so `alpha → main` is one confirm-gated click; the confirmation explicitly names the direction ("Switching working tree 'alpha' → 'main'"). `git switch -C origin/<branch>` + `git reset --hard` is symmetric in both directions. `library.json`, `.env`, `.offline_cache/`, `.background/` are all gitignored and survive the switch.
- **Backend:** new `updater.reinstall_service()` runs `daemon.uninstall()` + `daemon.install()` on a worker thread, captures their stdout into the diagnostic buffer, and returns `{ok, output, error}`. `_run_apply` rewired to: git → setup → reinstall (best-effort: a failed reinstall is logged but the reboot still fires — better to come back up with a stale service than be stuck pre-reboot) → `_reboot_machine(delay=1.5)`. `_kill_self_for_restart` removed (reboot supersedes it). Request model field renamed `restart` → `reboot`.
- **Docs:** GOTCHAS notes that the auto-apply path *requires* host auto-login (the reboot ends at a login screen otherwise — the user-level service never starts). ADMIN.md + API.md updated for the new apply shape (`reboot` flag, `service_reinstalled` field, new phases).

## [3.9.0] — 2026-05-26
- **New: built-in auto-updater.** The admin panel now has an **Updates** tab that pulls new commits from GitHub, re-runs `setup.py` non-interactively, then exits so the system service supervisor relaunches the dashboard on the new code. Auto-apply waits until the host is idle so an update never interrupts active playback or downloads. The branch picker is constrained to **main / beta / alpha** and stored at `library.json → settings.autoupdate`. Manual **Check Now**, **Apply Now**, and **Switch Branch** buttons let the admin drive the same flow on demand.
- **Backend:** new `updater.py` module wraps `git fetch / switch / reset --hard` and the `setup.py` subprocess. `_run_check` / `_run_apply` / `_kill_self_for_restart` in `main.py` glue it to FastAPI; `updater_loop` is the background poller (1-min tick, runs every `interval_hours`). `_machine_in_use(300)` gates the auto-apply step. New endpoints: `GET /api/admin/updater`, `POST /api/admin/updater/config`, `POST /api/admin/updater/check`, `POST /api/admin/updater/apply`, `POST /api/admin/updater/switch-branch`. `state_snapshot()` exposes `updater_phase` / `updater_busy` / `updater_message` so the UI can render progress live.
- **New: post-update env-key gating.** If a new release introduces a feature that needs an API key, the dashboard shows a non-blocking banner ("Server needs admin attention: …") and the admin fills the key from the new **Required API Keys** card on the Updates tab. The server keeps running; only the feature that needs the key is degraded. The current registry covers `ADMIN_PASSWORD` (required), `INDEXER_API_KEY` (required), `JACKETT_PASSWORD` (optional, indexer-management only), `TMDB_API_KEY` (optional, episode metadata). New `ENV_KEY_FEATURES` constant + `_missing_env_keys` / `_write_env_keys` / `_reload_settings` helpers; new endpoints `GET`/`POST /api/admin/env-keys`. Writes are atomic merges into `.env` (existing comments + ordering preserved); the running `settings` object is re-instantiated in-process so changes take effect without a restart for keys that don't need one.
- **Frontend:** Updates tab in `static/admin.html` (branch picker + interval + auto-apply toggle + status panel + env-key form); `renderServerAttention()` in `static/index.html` shows the banner driven by the SSE `state` event's new `missing_env_keys` / `updater_phase` fields. Sticky banner sits below the navbar in amber for env-key issues and indigo while an apply is in progress.
- **Docs:** `docs/ADMIN.md` updated with the Updates tab; `docs/API.md` covers the 6 new endpoints; `docs/GOTCHAS.md` notes the "needs service install for auto-restart" footgun.

## [3.8.1] — 2026-05-26
- **Fix: the global stream-prep pill leaked titles of admin-only items to profiles that couldn't otherwise see them.** Anyone polling `/api/offline-active` got the real item title in the pill detail line (e.g. *"Avatar S2E5 · 4m 12s left"*), even when their profile was blocked from the item by the Content Lock. Now the server checks the requesting profile against each active job; if any active item is admin-only and the requester isn't admin/elevated, **every** title in the response is replaced with the generic phrase *"Library content"* and `item_id` is blanked. All-or-nothing redaction is deliberate — selectively hiding only the restricted ones would itself reveal which job is being hidden. All callers continue to see identical counts, progress, and ETA, so the prep pill looks the same to everyone except for the title text.
- **Backend:** `/api/offline-active` now accepts `?profile_id=` and uses `_check_admin` + the existing elevated-profiles set to decide whether to redact. **Frontend:** `pollGlobalPrep` passes the active profile; the per-card reawaken loop naturally skips redacted entries because their `item_id` is blank.

## [3.8.0] — 2026-05-25
- **New setting: "YouTube Starting Volume".** Configures the host OS volume that's pre-set **before** the YouTube kiosk loads, so the first audio frame plays at a known level instead of whatever the OS happened to be at (often max). Lives in the profile-settings panel next to "System Volume After YouTube" (default **30 %**). Stored at `library.json → settings.youtube_start_volume`.
- **Order matters and is enforced.** `/api/youtube` now: (1) snapshots the current OS volume for the Stop restore, (2) **sets the OS mixer to the configured start volume**, (3) takes over now-playing state, (4) `pl_stop`s VLC, (5) broadcasts `yt_command:load`, (6) launches the kiosk. The pre-set happens before the load broadcast and before Chrome paints, so the IFrame player can never out-run it. `state.vlc_volume` is seeded from the pre-set so the dashboard slider lines up with reality the moment YouTube takes over.
- **Backend:** new `_youtube_start_volume`, `YouTubeStartVolumeReq`, `GET`/`POST /api/settings/youtube-start-volume`. **Frontend:** new red slider in the profile-settings panel with matching load/save plumbing (`saveYoutubeStartVolume`).

## [3.7.4] — 2026-05-25
- **Fix: "COM method call without VTable" spam in the logs after each volume change** (`Exception ignored in <function _compointer_base.__del__>`). Order-of-teardown bug: the `finally` block in `_windows_volume_op` calls `CoUninitialize` *before* Python destroys the function's frame, so the local COM pointers' `__del__` calls `Release()` against a torn-down apartment. Volume changes were still landing — just every successful call left three ignored exceptions in the logs. Fix: do all COM work inside an inner closure (`_do_com_work`); when it returns, its frame is destroyed first, the COM pointers `Release()` while the apartment is still alive, *then* the outer `finally` runs `CoUninitialize`. `op(vol)` returns a plain `bool`/`int` so no COM refs leak out across the boundary.

## [3.7.3] — 2026-05-25
- **Fix: `'AudioDevice' object has no attribute 'Activate'`** — even with `comtypes` installed, Windows volume control still failed because **`pycaw.AudioUtilities.GetSpeakers()` is API-unstable across pycaw versions**. Some releases return the raw `IMMDevice` COM pointer (has `.Activate()`), others return a Python `AudioDevice` wrapper that doesn't. `_windows_volume_op` now **bypasses `AudioUtilities.GetSpeakers()` entirely**, going through `CoCreateInstance(CLSID_MMDeviceEnumerator) → GetDefaultAudioEndpoint(eRender, eMultimedia) → Activate(IAudioEndpointVolume)` directly. This reuses pycaw's COM **interface definitions** (`pycaw.api.endpointvolume.IAudioEndpointVolume`, `pycaw.api.mmdeviceapi.IMMDeviceEnumerator`) — which have been stable — but not its convenience wrapper. Works against any pycaw release we ship.
- Also falls back to a hard-coded `CLSID_MMDeviceEnumerator` GUID (`BCDE0395-E52F-467C-8E3D-C4579291692E`) + numeric `EDataFlow`/`ERole` constants if pycaw ever moves them, so the path keeps working through future renames.

## [3.7.2] — 2026-05-25
- **Fix: Windows volume control still failed — `No module named 'comtypes'`.** `pycaw` declares `comtypes` as a transitive dep, but on real hosts (the user's box, in particular) pycaw can end up installed without comtypes — older standalone installs, `pip install --no-deps`, partial venvs, etc. Now `comtypes>=1.2.0` is pinned **explicitly** in `requirements.txt` under the same `sys_platform == "win32"` marker (belt-and-braces), so a clean `pip install -r requirements.txt` always lands both.
- **Better diagnostic when an import is missing.** The error message now names the *actual* missing module (via `ImportError.name`) — so the dashboard toast says *"Windows volume control dep `comtypes` is not installed"* instead of the generic *"pycaw is not installed"*, telling the operator exactly which `pip install` to run.

## [3.7.1] — 2026-05-25
- **Fix: YouTube volume controls did nothing on Windows.** Two failure modes, both silent:
  - **COM was not initialized on the thread that called pycaw.** Volume operations run via `asyncio.to_thread`, which dispatches to Python's default `ThreadPoolExecutor` — those worker threads don't have COM initialized, so `AudioUtilities.GetSpeakers()` raises *"CoInitialize has not been called"*, the helper's `except` swallowed it as a warning, and the dashboard slider just no-oped. Volume helpers now call `comtypes.CoInitialize()` / `CoUninitialize()` around each operation (`_windows_volume_op` wrapper), so they work regardless of which pool worker happens to run them.
  - **`pycaw` may not be installed at all** if the user upgraded without re-running `pip install -r requirements.txt`. The `ImportError` got the same generic "volume failed" treatment. Now the import failure is detected explicitly and surfaces a clear message — *"pycaw is not installed on the server. Run `pip install -r requirements.txt` (or re-run setup.py) and restart."* — both in `logs/streamlink_app.log` and as a one-shot toast in the dashboard.
- **Failures now surface to the dashboard instead of silently no-opping.** `POST /api/youtube/control` returns **503** with `{ok:false, error}` when the volume call failed, and `ytControl` (JS) shows the server's error message via a one-shot session-scoped alert so the user isn't left guessing why the slider doesn't work.

## [3.7.0] — 2026-05-25
- **New: Clear All button on the admin Server Logs card.** Next to **Refresh** / **Download All (.zip)**, a confirm-gated **Clear All** wipes every file in `logs/` — handy after pulling diagnostics off a remote box, so the next bundle isn't 100 MB of stale history.
- **Backend:** `DELETE /api/admin/logs`. The live rotating handlers (`streamlink_app.log`, `hls.log`) are **truncated in-place** via `handler.acquire()` + `handler.stream.truncate(0)`, not deleted. Deleting was the obvious approach but wrong on both platforms we care about: on **Windows** you can't `unlink` a file the running process has open for writing, and on **POSIX** a delete leaves logging's FD valid but disconnected from any on-disk file — subsequent writes silently vanish until the next restart. Rotated siblings (`.1`/`.2`/`.3`) and the `streamlink.err` written by launchd/systemd/the Windows service are unlinked; if `unlink` fails (service still holds an exclusive Windows handle), we fall back to a write-mode truncate so the user still sees the file empty. Returns `{ok, cleared:[name], errors:[{file,error}]}`.

## [3.6.1] — 2026-05-25
- **Fix: YouTube kiosk launched on Windows but `/tv` never checked in.** The logs showed Edge launching every play attempt followed by the 12 s heartbeat watchdog firing — the page wasn't loading. Cause: the kiosk URL was `http://localhost/tv?v=…`, and Windows' hosts file resolves localhost to **both `::1` and `127.0.0.1`** with IPv6 first. uvicorn binds `0.0.0.0:80` (IPv4 only), so Chromium hit ECONNREFUSED on `::1` and either showed an error page or stalled past the watchdog window. The kiosk URL now pins **`http://127.0.0.1/tv?v=…`**, sidestepping the dual-stack mess entirely. Added Edge first-run/signin/welcome-modal suppression flags (`--disable-fre`, `--disable-features=msImplicitSignin,SigninInterceptBubbleV2,…`, `--noerrdialogs`) so a fresh `--user-data-dir` profile can't get blocked on a welcome screen either.
- **Fix: YouTube played at system max volume; dashboard slider was a no-op.** The IFrame player's `setVolume` only scales the audio *before* the OS mixer — the actual loudness is whatever the host's system volume is set to. The dashboard volume slider now drives the **OS system volume** during YouTube (via `pycaw` on Windows, `osascript` on macOS, `pactl`/`amixer` on Linux), and the IFrame is locked at 100 % unmuted so the OS mixer is the sole amp. While YouTube is on, the slider's range flips to **0–100** (OS scale) instead of VLC's 0–200.
- **New setting: "System Volume After YouTube".** The host's OS volume is **restored to a configured target** when YouTube stops — so headphones at 100 % don't blow eardrums and a movie session doesn't leave the room loud the next time anyone touches VLC. Lives next to "Max Volume" in the profile-settings panel (default 70). The restore only runs after the kiosk process is confirmed gone (polled with a 4 s deadline) so we never twist the volume knob underneath the still-playing video.
- **Backend:** `_set_system_volume_sync` / `_get_system_volume_sync` (pycaw on Windows, osascript on macOS, pactl/amixer on Linux), `_system_volume_default`, `state.system_volume_before_yt` snapshot, `POST /api/youtube/control` now handles `volume_set` / `volume_step` server-side (no longer broadcast as `yt_command`), `/api/stop` restores the OS volume after kiosk teardown, new `GET`/`POST /api/settings/system-volume-default`, new `library.json → settings.system_volume_default` (0-100, default 70), `pycaw` dep added to `requirements.txt` under a `sys_platform == "win32"` marker.

## [3.6.0] — 2026-05-25
- **New: download the server's log files from the admin panel.** A **Server Logs** card now sits in the System tab and lists every file under `logs/` (`streamlink_app.log`, `hls.log`, `streamlink.err`, plus the rotated `.1`/`.2`/`.3` siblings) with size + last-modified, newest first. Each row has a per-file **Download** link, plus **Download All (.zip)** at the top that streams a deflate-compressed bundle named `streamlink-logs-YYYYMMDD-HHMMSS.zip`. Lets operators pull diagnostics off a remote box (Windows in particular) without needing SSH or Remote Desktop.
- **Backend:** `GET /api/admin/logs` (listing), `GET /api/admin/logs/{name}` (single file, attachment disposition), `GET /api/admin/logs/_bundle` (streaming ZIP via the same `os.pipe()` pattern used by `/api/library/{id}/zip`). All three require admin auth, and `_safe_log_path` rejects any name containing a slash, `..`, an absolute path, or a resolved location outside `LOG_DIR` so the file-by-name endpoint can't be coaxed into reading anything outside `logs/`. The per-file route uses the token via `?admin_token=` query param so plain `<a download>` anchors work (anchor downloads can't set headers).

## [3.5.2] — 2026-05-25
- **Fix: on Windows the YouTube kiosk launched but stayed behind VLC** — the idle background video kept the screen and the kiosk window only flashed in the taskbar until you clicked it. Two causes, both Windows-specific:
  - **Focus-stealing prevention.** The server isn't the foreground process, so the browser window it spawns is denied focus (Windows flashes its taskbar button instead). Now `_bring_tv_to_front` finds the kiosk window **by its title** (`_TV_WINDOW_MARKER` = the `static/tv.html` `<title>`, since Chrome is multi-process so PID matching is unreliable) and forces it forward with the same cocktail used for VLC (zero foreground-lock timeout + synthetic ALT + `AttachThreadInput` + `SetForegroundWindow` + clear taskbar flash), retrying for ~10 s while the window is still being created.
  - **VLC stayed on top.** `/api/youtube` only `pl_stop`ped VLC; its window remained. Now it also **minimizes VLC** (`vlc_minimize`, via `_bring_tv_to_front`). Safe because the return-to-background path (`_play_background_video → vlc_focus_and_fullscreen`) `SW_RESTORE`s it.
  - **The background focus loop no longer fights the kiosk.** `vlc_focus_and_fullscreen` now bails the moment `youtube_active` is set — previously a still-running loop would keep minimizing the kiosk and re-focusing VLC for up to ~24 s after a background session.

## [3.5.1] — 2026-05-25
- **Fix: YouTube-on-TV never started on Windows** (it worked on macOS). The video failed to launch and you'd see the idle background video stop, VLC flash its loading animation, then the background video restart — the tell-tale sign that the kiosk launch failed: `_launch_tv_browser` returned False, so `/api/youtube` hit its 500 path, reset `youtube_active`, and ~3 s later `background_video_loop` reclaimed VLC.
  - **Root cause: browser discovery was too narrow on Windows.** It only checked three hard-coded Program Files paths and silently returned None when Chrome/Edge lived elsewhere — most commonly a **per-user Chrome install under `%LOCALAPPDATA%`**, or Edge at a path the list didn't cover.
  - **Fix:** `_find_chrome` now casts a wide net on Windows — the **`App Paths` registry** (HKCU + HKLM, for `chrome.exe` / `msedge.exe` / `brave.exe` / `chromium.exe`), per-user `%LOCALAPPDATA%`, both `Program Files` trees, and PATH — covering Chrome, **Edge** (preinstalled on Win10/11, so this essentially always resolves), Brave and Chromium. Discovery and launch now **log** what they found / why they failed (`logs/streamlink_app.log`) instead of failing silently.
  - **Plus a health-check:** after launching the kiosk, if the `/tv` page doesn't heartbeat within 12 s (it normally checks in within ~1 s), the server now reports a clear on-screen error — "YouTube didn't start on the TV — the browser failed to open" — instead of silently dropping back to the background video. Catches locked-profile / instant-exit / session-0-service cases too.
  - **CLAUDE.md:** added a top-of-file directive that **Windows is the primary target platform** (then Linux, then macOS) — a feature that works on macOS but not Windows is a bug, not partial success.

## [3.5.0] — 2026-05-25
- **New: play any YouTube link on the TV, controlled from the dashboard.** A red **Play on TV** input now sits at the top of the Search tab — paste a YouTube link (watch, `youtu.be`, Shorts, live, embed, or a bare id) and it opens fullscreen on the TV (the host display) and starts playing.
  - **Plays in a browser, not VLC.** VLC 3.0's bundled `youtube.lua` is broken (it "plays" the watch page for a few seconds, never resolves a title/length, then stops), and yt-dlp-into-VLC adds a fragile dependency and caps at 720p without an audio-slave hack. Instead the video plays in a fullscreen **Chrome kiosk** on the host via the **YouTube IFrame Player API** (`static/tv.html`), so quality/ads/captions all "just work".
  - **Driven by the existing controls.** The footer + fullscreen play/pause, ±10/30 s, scrubber, and volume all route to the TV's YouTube player when it's the active playback (`app.youtube_active` → `POST /api/youtube/control` → SSE `yt_command` → IFrame API). The `/tv` page reports position/duration/title/volume back every second (`POST /api/youtube/tv-state`), mirrored onto the reused display fields so the footer scrubber renders YouTube exactly like a VLC play. Save-to-library, handoff, episode-nav and track menus are hidden in YouTube mode.
  - **Backend:** `POST /api/youtube`, `POST /api/youtube/control`, `POST /api/youtube/tv-state`, `GET /tv`; `_extract_youtube_id`, `_find_chrome` (Chrome → Chromium → Edge, or `_CHROME_BIN`), `_launch_tv_browser` (`--kiosk --app` + `--autoplay-policy=no-user-gesture-required` + an isolated `--user-data-dir`), `_kill_tv_browser` (kills only the kiosk, matched by profile dir). A play hot-swaps the video over SSE if the `/tv` page is already open (heartbeat <6 s), else launches the kiosk; the fresh page autoplays from `?v=<id>`. Stop broadcasts `yt_command:close` and hard-kills the kiosk. `stat_broadcaster` + `background_video_loop` are gated while `youtube_active` so they don't clobber the YouTube state or start the idle background video over it. Not VPN-gated (ordinary HTTPS, not P2P).
  - **Docs:** new [docs/YOUTUBE.md](docs/YOUTUBE.md); API/GOTCHAS/ARCHITECTURE/PLAN updated. New Milestone 21.

## [3.4.5] — 2026-05-25
- **Every VLC call now reuses one keep-alive connection — the actual fix for the ~10 s play/resume stall.** 3.4.3/3.4.4 assumed only the `in_play` *reply* was slow, but the real problem was that **every** VLC HTTP call (status polls included) paid a per-call TCP setup against VLC's tiny, effectively single-threaded HTTP server. Three background loops (`stat_broadcaster`, `vlc_progress_tracker`, `background_video_loop`) each opened a *fresh* `httpx.AsyncClient()`/socket every 2–3 s, plus the multi-command play burst — constantly churning connections, so each call took seconds and the poll-based UI flip/seek from 3.4.4 was still gated on slow polls.
  - New shared `_vlc_http()` persistent `httpx.AsyncClient` (`base_url` + client-level BasicAuth, `connect=2 s`/`read=5 s` timeouts, a 4-connection keep-alive pool with 30 s expiry). All four VLC callers — `vlc()`, `vlc_status()`, `vlc_playlist_uri()`, `_play_background_video()` — route through it; the per-call `async with httpx.AsyncClient()` blocks are gone. Created in `lifespan` and closed on shutdown (mirrors the `qbit` client), lazily rebuilt if needed. A warm connection from the polling loops is almost always already pooled when a play starts, so commands and status reads stay sub-second.

## [3.4.4] — 2026-05-25
- **Play/resume no longer sits on "buffering" for ~10 s while VLC is already playing.** The real culprit (3.4.3 only sped up the seek): the UI flip to "playing" *and* the resume seek were both gated on `await vlc("in_play")` **returning** — and VLC's HTTP reply to `in_play` lags several seconds behind actual playback (which starts in <1 s). Worse, the two `timeout=5.0` calls in that path (volume pre-roll + in_play) added up to the "at least 10 s" you saw.
  - `_library_play_launch` and `_vlc_relaunch_playlist` (prev/next) now fire `in_play` **detached** and detect the real start by polling the lighter `status.json` endpoint (`_vlc_wait_until_ready`, now file-aware so it can't latch onto the *previously* playing file). The "playing" flip and the resume seek both happen the instant VLC is genuinely on the new file — typically well under a second — instead of when `in_play` finally replies.
  - The remaining-episodes enqueue still waits for the `in_play` call to land first (correct playlist order), but that wait is now invisible: you're already watching. The detached `in_play` task is cancelled if the play is superseded, so a slow reply can't resurrect a stale file.

## [3.4.3] — 2026-05-25
- **Resume on VLC now jumps to your spot the instant VLC has the file open — not after a fixed 3 s wait.** Starting a library item with a saved position used to fire the resume seek on a blind `asyncio.sleep(3)` after `in_play`, so on a local file (where VLC opens in well under a second) you sat at 0:00 watching the start for ~3 s before it jumped, and the seek could be dropped entirely if VLC happened to still be opening. `_library_play_launch` now polls VLC via the new `_vlc_wait_until_ready()` helper (state is `playing`/`paused` **and** `length` is known — i.e. the demuxer is up and a seek will actually take) and seeks immediately, with one guarded re-issue if VLC ignored the first seek right at open. The same wait now gates the `resume_mode="prompt"` offer so it appears as soon as playback is live. Resume is now as snappy as poking VLC from a phone remote.
- **Fixed `/api/version` reporting a stale `3.3.0`**, which left the badge (3.4.2) newer than the server's value so the client auto-reload never triggered. `UI_VERSION` is back in sync with the badge.

## [3.4.2] — 2026-05-25
- **StreamLink's core path is now the box's top priority — controls/UI/VLC-control stay responsive under heavy prep.** Lowering ffmpeg (3.4.1) wasn't enough: the server process itself sat at normal priority, so on a saturated box it got starved while separate apps (e.g. a VLC remote talking straight to VLC) stayed snappy — the on-screen volume button lagged.
  - **The server raises its own priority at startup** (`_raise_own_priority`, called first thing in `lifespan`, so both the HTTP and HTTPS uvicorn processes do it): `HIGH_PRIORITY_CLASS` on Windows (no admin needed; REALTIME deliberately avoided), a negative `nice` on POSIX (needs root/CAP_SYS_NICE — the installed service; degrades gracefully otherwise). The server is I/O-bound, so high priority just lets its short request bursts preempt the encoder without hogging the box.
  - **All background CPU work stays below it.** Prep ffmpeg already ran BELOW_NORMAL/`nice` (3.4.1); the Smart-Skip **analyzer** subprocesses (ffmpeg decode, fpcalc, ffprobe) now do too (`analyzer._lp` + `_LOWPRIO_KW`) — without this they'd inherit the server's HIGH priority and re-introduce lag during an analysis. Net effect: server ≫ VLC/qBit (normal) ≫ prep/analyzer (below normal).

## [3.4.1] — 2026-05-25
- **Keep the dashboard responsive while stream-prep runs.** Prep is CPU-heavy, so beyond the warning/pause/overnight controls (3.4.0) the work now actively yields to the rest of the app rather than just relying on the user to pause it.
  - **ffmpeg runs at lowered OS priority on every platform.** Windows already used `BELOW_NORMAL_PRIORITY_CLASS`; POSIX hosts now prepend `nice -n 10` (`_ffmpeg_nice_prefix`, fork-safe vs `preexec_fn`, no-ops if `nice` is missing). The bulk encode yields CPU to the web server's event loop, VLC, and qBit, so the site stays usable.
  - **No synchronous bursts on the event loop.** The recursive `.part`/old-bundle removals and the bundle size calc in `_run_offline_job` are offloaded with `asyncio.to_thread`, and `/prep-all` + the overnight `_enqueue_library_prep` now `await asyncio.sleep(0)` between files (the per-file `_maybe_start_prep_job` is synchronous), so kicking off prep on a large pack/library no longer briefly stalls other requests. All heavy prep work was already off-loop (ffmpeg/ffprobe as subprocesses); this closes the remaining small windows.

## [3.4.0] — 2026-05-25
- **Stream-prep is now interruptible and can run itself overnight.** Preparing files for on-device streaming pegs the host CPU, so the dashboard now warns about it, lets anyone pause/resume the work, and can do the whole library automatically while nobody's watching.
  - **Lag warning (`static/index.html`):** the first time you tap **Prep for Streaming** (per-item) or a per-row **Prep** in a session, a modal explains that the server will get laggy and can become unusable at times, and points at the pause control. Acknowledged once per session. The interactive "play on this device" path is unaffected (no warning — you want it now).
  - **Pause / Resume from the non-admin UI:** the top-right prep indicator (`#globalPrepBar`) gained **Pause** and **Resume** buttons. Pause asks how to handle the file being encoded right now — **Finish current file, then stop** (no wasted work) or **Stop now** (terminates the in-flight ffmpeg for instant relief; that file restarts from scratch on resume, since HLS prep can't checkpoint mid-file). New endpoints `POST /api/offline-prep/pause {kill}` and `POST /api/offline-prep/resume`.
  - **Overnight auto-prep (admin → System tab):** a new **Overnight Stream Prep** panel with an enable toggle, start/end times (windows may cross midnight), timezone, and an **on-end** choice — **Pause until next window** or **Continue until finished**. During the window the server queues every un-prepped library file; at the end it either holds (letting the in-flight file finish) or runs to completion. Backed by the `overnight_prep_loop` background task and `GET/POST /api/admin/overnight-prep`; config persists under `library.json → settings.overnight_prep`.
  - **Backend model (`main.py`):** bulk/per-item/overnight prep jobs are tagged `queue:"bulk"` and honor a global pause gate in `_run_offline_job`; a paused bulk job releases the single concurrency slot (so an on-demand play-on-device prep can still run) and is re-spawned on resume. `/api/offline-active`, `/prep-status` and `state_snapshot` now expose paused state for the UI.

## [3.3.0] — 2026-05-25
- **Stream-to-device HLS now ships an adaptive-bitrate ladder + an in-player quality menu.** Each bundle previously carried a single video rendition at source resolution, so a weak LAN/Wi-Fi link had no fallback and there was no way to pick a quality. Bundles now emit **Original + 720p + 480p** (each rung capped at the source height — a ≤480p source still gets one rendition, a 720p source gets Original + 480p), all sharing one audio group so switching quality never re-fetches audio.
  - **Player (`static/index.html`):** the on-device player gains a **Res** dropdown (next to Aud/Sub) populated from hls.js `levels` — `Auto` plus each resolution. `Auto` keeps hls.js adapting to bandwidth; a manual pick sets `hls.currentLevel`. Quality is session-only (not persisted). Safari native HLS auto-adapts among the variants but exposes no reliable manual-level API, so its Res row stays hidden (auto-only).
  - **Backend (`main.py`):** `_build_hls_ffmpeg_args` maps the source video once per ladder rung and sets per-output codec/scale (`scale=-2:<h>`, with `maxrate`/`bufsize` VBV caps for realistic master-playlist BANDWIDTH). The original rung still stream-copies when the source is browser-safe H.264 — even when NVENC is present — and only the scaled down-rungs transcode (NVENC on GPU when available, else libx264). `meta.json` and the `offline-prepare` response gain a `videos[]` array.
  - **`OFFLINE_CACHE_VERSION` → `v7-hls-abr`** so existing single-rendition `v6-hls` bundles rebuild on next prep/play; old dirs surface as orphans in Admin → Offline Cache for purge.
  - **Trade-offs:** down-rungs always transcode (slower prep; much faster with NVENC) and bundle storage grows ~1.6–1.9×.

## [3.2.2] — 2026-05-24
- **Bug fix (Windows): `fragLoadError` on `init_video.mp4 → HTTP 404` — the v3.2.1 fix was incomplete.** The new diagnostic alert pinpointed it: the fmp4 init segment 404s on a Windows host. Pinning `-hls_fmp4_init_filename "init_%v.mp4"` fixed the *name* but not the *location*. ffmpeg derives the init segment's output directory by parsing the **playlist** path; on Windows that path has backslashes, the parse fails, and the init file gets written to the server's working directory instead of the bundle `.part/` dir — so it never makes it into the cache and 404s at playback. (A full path on the init arg isn't an option — ffmpeg prepends the playlist dir to it and the encode dies with "Failed to open segment".)
  - **Fix:** every ffmpeg **output** is now a bare filename (init / segments / playlists / subs) and the subprocess runs with `cwd=<bundle .part dir>`, so they all land in the bundle on every OS regardless of path separators. The `-i` source stays absolute. `_build_hls_ffmpeg_args` no longer takes `out_dir`.
  - **`OFFLINE_CACHE_VERSION` → `v6-hls`** so the (still-broken) `v5-hls` bundles from the previous attempt rebuild on next prep/play.

## [3.2.1] — 2026-05-24
- **Bug fix: HLS playback never started — manifest + subtitle/audio dropdowns loaded, then a fatal `fragLoadError` and a black player.** The bundle's manifest and per-rendition playlists fetched fine (so `MANIFEST_PARSED` fired and the dropdowns populated), but the very first media fetch failed. Root cause: `_build_hls_ffmpeg_args` templated every bundle filename *except* the fmp4 init segment, leaving `-hls_fmp4_init_filename` at ffmpeg's default. ffmpeg's own `%v` expansion for the init segment doesn't always match the `#EXT-X-MAP:URI=` it writes into each variant playlist, so the player requested an init segment that didn't exist on disk → 404 → hls.js retried, gave up, and emitted a fatal `fragLoadError` before a single frame decoded.
  - **Fix:** pin `-hls_fmp4_init_filename "init_%v.mp4"` so the init files (`init_video.mp4`, `init_audio_0.mp4`, …) follow the same `name:`-tag scheme as the segments (`seg_video_…`, `seg_audio_0_…`) and the `EXT-X-MAP` URI matches the file on disk on every ffmpeg ≥ 4.3.
  - **`OFFLINE_CACHE_VERSION` → `v5-hls`** so existing (broken) bundles rebuild on next prep/play; old `v4-hls` dirs surface as orphans in Admin → Offline Cache for purge.
  - **Player diagnostics:** the hls.js `ERROR` handler now logs the failing URL + HTTP status to the console and includes the segment name + code in the on-screen alert, so any residual "won't play" is self-diagnosing instead of a bare `fragLoadError`.

## [3.2.0] — 2026-05-20
- **Fix: HLS stream-to-device never worked when the source had subtitles (i.e. essentially always).** The new `logs/hls.log` revealed the real failure: `ffmpeg ... Could not write header ... No streams to mux were specified ... Conversion failed!`. Reproduced locally on the same ffmpeg 8.1.1 — ffmpeg's HLS muxer **cannot** package multi-track WebVTT into a manifest. A single inline subtitle works; two or more (declared as their own `s:N,sgroup:…` variants, as we did) fail unconditionally, for both `fmp4` and `mpegts` segment types. Since virtually every release MKV has many subtitle tracks, HLS prep aborted on every real file.
  - **`_build_hls_ffmpeg_args` rearchitected:** the HLS bundle is now **video + audio only** (fmp4, unchanged and working). Each text subtitle is emitted as a standalone `sub_<i>.vtt` **sidecar** in the same single ffmpeg pass (`-map 0:s:N -c:s webvtt -f webvtt sub_N.vtt`). `meta.json` subtitle entries now carry `file: "sub_<i>.vtt"` instead of an in-manifest `playlist`.
  - **Player (`static/index.html`):** bundle subtitles are wired as `<track>` children of `<video>` exactly like on-disk sidecar subs, unifying the two code paths. `_lpApplySubIdx` now toggles `track.mode` by dropdown key across both the hls.js and Safari-native paths (the old `hls.subtitleTrack` route is gone). New `lp.subTracks` records every created `<track>` in dropdown order.
  - **`OFFLINE_CACHE_VERSION` → `v4-hls`** so any previously-(failed/partial) bundles rebuild on next prep; old `v3-hls` dirs surface as orphans in Admin → Offline Cache.
- **Stream-to-device marked unavailable on macOS hosts.** ffmpeg/ffprobe run as children of the (non-GUI) server process, and macOS TCC blocks them from reading media in the user's protected `~/Downloads` / `~/Desktop` / `~/Documents` — every prep aborts at the probe step with `Operation not permitted` (VLC/qBit work because they're separate, individually-granted apps). The prep endpoints now return a clear 503 (`offline_prepare`) / error (`_maybe_start_prep_job`, `_run_offline_job`) on Darwin, `state_snapshot` exposes `hls_available`, and the UI hides every Prep / On-Device affordance (and routes the play chooser straight to VLC) via a `no-hls` body class.

## [3.1.0] — 2026-05-20
- **Minor feature: real logging for the backend, focused on the HLS offline-prep pipeline.** `main.py` had no `logging` setup at all — background-task failures (most painfully an HLS conversion that died seconds after starting) were swallowed into a 500-char `job["error"]` tail with nothing written to disk, leaving operators no way to find out *why*.
  - **New logger config (`_init_logging`)** wires up a `streamlink` logger with two rotating file handlers — `logs/streamlink_app.log` (everything) and `logs/hls.log` (HLS only, via the `streamlink.hls` child logger) — at 2 MB × 3 backups each, plus a stderr handler capped at `WARNING` so a bulk `/prep-all` doesn't bury the interactive console while the full INFO trail still lands on disk. Idempotent across reloads; `propagate=False` avoids double-logging through uvicorn's root.
  - **`_run_offline_job` instrumented end to end:** logs job START (src/out), the exact ffmpeg command line (shell-quoted), encoder choice + track counts, and DONE (elapsed + bundle size). Every abort path — ffmpeg missing, ffmpeg too old, no video stream — now logs at ERROR. On a non-zero ffmpeg exit it logs the **return code, elapsed time, full command, and the last 300 lines of ffmpeg stderr** to `logs/hls.log` (the UI keeps its short tail). The outer handler uses `hls_log.exception` so crashes carry a traceback.
  - **Latent deadlock fixed as part of the stderr capture:** ffmpeg's stderr is now drained concurrently into a bounded buffer instead of being read only after `proc.wait()`. A chatty ffmpeg could previously fill the OS pipe buffer and block on write while we waited on it — `proc.wait()` would hang forever. Draining as output arrives both prevents that and gives us the complete error to log.

## [3.0.0] — 2026-05-18
- **Major feature: multi-track Stream-to-Device via HLS.** The local-device player now exposes every audio track and every text subtitle from the source MKV, with runtime switching that works across every modern browser. End-to-end rewrite of the offline-prep pipeline.
  - **Backend:** `_run_offline_job` rewritten to emit per-source HLS bundles (`<sha>/master.m3u8` + per-rendition `.m3u8` + fmp4 segments + `meta.json`) instead of single MP4s. One ffmpeg invocation with `-var_stream_map` maps the video, every audio track (each transcoded to AAC stereo at 160 kbps), and every text subtitle (transcoded to WebVTT). Image-based subs (PGS / VOBSUB / DVB) are filtered out by `_ffprobe_full` and reported in `meta.json:skipped_image_subs`. Video stream-copies when the source is already H.264 yuv420p with a non-Hi10P profile; otherwise transcodes via libx264 (or h264_nvenc when available). ffmpeg ≥ 4.3 is now required and enforced via `_ffmpeg_version()` cache — older builds fail-fast with a clear error before launching the encode.
  - **Cache layout:** `OFFLINE_CACHE_VERSION` bumped to `v3-hls`. Cache key is the same `sha256(VERSION|path|mtime|size)[:24]` but now keys a directory (`<sha>/`) instead of a file (`<sha>.mp4`). The old single-MP4 caches map to different keys under v3 and surface as `kind: "legacy"` orphans in Admin → Offline Cache for one-click purge. Per-source storage: bundle ≈ source size + AAC stereo per audio track (a 4 GB BD remux with 3 audios ≈ 4.7 GB vs. 4 GB before).
  - **Endpoints:** `/api/library/offline-cache/{name}` replaced with `/api/library/offline-cache/{cache_key}/{filename}` — strict regex validation on both segments (`[a-f0-9]{24}` + `[A-Za-z0-9._-]+`) prevents path traversal. `/offline-prepare` now returns `master_url` + `audios[]` + `subtitles[]` + `saved_tracks{}` instead of `video_url`. New `POST /api/library/{id}/local-tracks` persists the in-browser player's audio/subtitle picks per-profile per-file as `local_audio_idx` / `local_subtitle_idx` — kept separate from VLC's `audio_track` / `subtitle_track` (ES IDs) which use a different addressing scheme. `update_progress` and `mark_watched` preserve all four track-pref keys across writes.
  - **Admin Offline Cache tab** now walks directories: `_dir_size_bytes` recurses into each `<sha>/` for accurate per-item totals; `_delete_cache_artifacts` removes the bundle dir + any `.part/` staging dir + any legacy MP4 in one operation; the orphan list now includes a `legacy` kind for pre-v3 single-MP4 caches.
  - **Frontend:** ships `static/vendor/hls.min.js` (hls.js v1.5.17, ~100 KB gzipped) bundled locally — lazy-loaded on first local-player open via `_ensureHlsLib()` so the dashboard's initial paint isn't delayed. `_lpLoadIndex` rewritten to branch on `Hls.isSupported()`: hls.js for Chrome / Firefox / Edge / Android Chrome (MSE-based), `<video>.src = master_url` for Safari (native HLS). Audio/sub dropdowns populated from the bundle's `meta.json`; sidecar `.srt`/`.vtt` files coexist with bundle subs as additional dropdown options (using a `"sidecar:N"` index sentinel). `lpSetAudio(idx)` and `lpSetSubtitle(idx)` switch tracks at runtime via `hls.audioTrack` / `hls.subtitleTrack` on hls.js or `AudioTrackList.enabled` / `TextTrackList.mode` on Safari. Each switch POSTs to `/local-tracks` so the pick comes back on next play. Local-player markup gains an `lpAudioRow` next to `lpSubRow`; the row container is hidden when there's exactly one audio track and zero subtitles.
  - **UX trade-offs documented in [docs/GOTCHAS.md](docs/GOTCHAS.md):** segment-boundary seek granularity (~6 s, mostly invisible), ASS/SSA styling loss in WebVTT conversion (libass.js render-on-canvas is the deferred fix), image-based subs skipped entirely (PGS / VOBSUB / DVB — use VLC for those), audio always downmixed to AAC stereo (5.1 preservation needs the VLC path), local-player and VLC track prefs intentionally independent.
  - **Migration:** existing `.offline_cache/*.mp4` files orphan immediately; users need to re-prep their library after upgrade. Admin → Offline Cache → Purge All clears them in one click.

## [2.11.0] — 2026-05-24
- **Minor feature: re-running setup pre-fills your current `.env` values (and doubles as a config health check).** Previously the "Existing .env detected" prompt was all-or-nothing — reuse everything untouched, or re-prompt from scratch with the factory defaults, forcing you to retype every setting (Jackett URL, qBit/VLC passwords, etc.) just to change one. Now choosing to re-prompt pre-fills each field with the value currently in `.env`, so you can press Enter to keep any setting and only type over the ones you want to change. Walking through the prompts now also surfaces every active value, so it works as a quick way to audit the config before, say, re-running setup to apply the Jackett auto-recovery grant.
  - **`gather_config(existing)`** ([setup.py](setup.py)) takes the parsed existing `.env` and defaults each prompt to the stored value, falling back to the factory default on a fresh install (unchanged first-run behaviour).
  - **Secrets are never echoed.** Stored passwords (`JACKETT_PASSWORD`, `QBIT_PASSWORD`, `VLC_PASSWORD`, `ADMIN_PASSWORD`) show a masked `•••••• (Enter = keep)` hint instead of the plaintext value, so re-running setup can't leak them to the terminal/scrollback; pressing Enter still keeps the stored value, and a currently-blank secret shows `(currently blank)`. `ask()` gained a `show_default` override to support this.

## [2.10.1] — 2026-05-23
- **Bug fix: `remote.local` stops resolving after a reboot when run as a system service.** Root cause: the installed service registered mDNS exactly once at startup, but at boot it launches before Wi-Fi has associated and the interface has a LAN IP. `get_local_ip()` returned `""`, so the registration was silently skipped — `remote.local` then never resolved until the next manual relaunch, even though uvicorn (bound to `0.0.0.0`) became reachable by IP the moment the network came up. That's why it worked on first interactive install (network already up) but not after a reboot.
  - **`start_mdns_resilient()` (in `run.py`).** Registers mDNS from a background daemon thread that polls `get_local_ip()` until a LAN IP appears, registers `remote.local`, then re-registers if the IP later changes (DHCP lease, network switch). Polls every 5 s until registered, then every 30 s to watch for changes. Returns a `.close()` handle for shutdown cleanup.
  - **Used by both launch paths.** The generated service wrapper (`daemon.py`) and interactive `run.py` both now call `start_mdns_resilient()` instead of the one-shot `start_mdns()`, keeping them behaviourally identical and fixing the same latent issue when `run.py` is launched before Wi-Fi.
  - **Re-run `python3 run.py --install` to regenerate the service wrapper** with the fix.

## [2.10.0] — 2026-05-23
- **Minor feature: machine reboot + scheduled daily restart (Jackett hard-reset).** A reboot is the only reliable cure for some wedged-Jackett states, so the admin **System** tab now exposes both a manual reboot and an automated nightly one.
  - **Reboot Machine.** New `POST /api/admin/reboot` restarts the whole host (not just the web server). `_reboot_machine()` tries platform-appropriate commands in order — macOS: System Events restart (no sudo from a launchd user agent) → `sudo -n shutdown -r now` → `shutdown -r now`; Linux: `systemctl reboot` → `sudo -n shutdown` → `shutdown`; Windows: `shutdown /r /t 0`. Fires ~0.5 s after the response flushes so the UI gets confirmation first.
  - **Scheduled Restart.** New `scheduled_reboot_loop` background task + `GET`/`POST /api/admin/scheduled-reboot`. Config (`enabled`, `time` HH:MM, `timezone`, `idle_minutes`) persists under `library.json → settings.scheduled_reboot`. At the configured local time (default 00:00 America/Los_Angeles) it checks whether the box was used in the last `idle_minutes`; if idle it reboots, otherwise it waits `idle_minutes` and re-checks, repeating until idle. A persisted `last_fired` date guards against the just-rebooted machine immediately re-arming and looping.
  - **Idle detection.** New `track_activity` middleware stamps `state.last_activity` on mutating requests + search (routine GET polling is ignored). `_machine_in_use()` also treats live VLC playback/pause of non-background content and any active stream/download as "in use", so a reboot never interrupts active watching or a running download.
  - **Auto-recovery requirement.** For the host to come back after a reboot, the user must configure OS auto-login and install the StreamLink system service (`run.py --install`). Documented in the README and the System tab.

## [2.9.0] — 2026-05-20
- **Jackett reliability: detect and recover a hung indexer instead of needing a reboot.** Root cause of "Jackett stops working after a while, only a reboot fixes it": the watchdog's liveness check was a bare TCP port-open, and a hung Jackett keeps its listener socket bound while it stops serving HTTP — so the watchdog thought it was healthy and never restarted it. Even when it did try, `sc.exe start Jackett` is a no-op (1056 ALREADY_RUNNING) on a wedged service, so only a reboot cleared it.
  - **HTTP health check.** The watchdog, `run.py` startup, and a new in-app monitor now probe `GET {INDEXER_URL}/UI/Login` (any HTTP status proves the web stack is alive) via `_http_ok()` instead of a port check. `ServiceSpec` grew a `health_check` hook; `is_alive()` uses it for Jackett (VLC/qBit unchanged), and `start()` waits on it so a restart doesn't tight-loop before Jackett finishes warming up.
  - **Force-down before restart.** A new `pre_restart` hook force-stops the wedged Jackett (`sc stop` + wait for STOPPED, hard-kill fallback; `kill_by_name` off-Windows) before relaunching, so the port frees and the restart actually takes. When no `Jackett` service exists, the watchdog now launches the tray/console exe as a user process instead of giving up.
  - **Permission grant so a non-elevated watchdog can recover the service.** A LocalSystem `Jackett` service can only be `sc` stopped/started by an admin — the reason a non-elevated StreamLink hit a UAC/access-denied wall. `setup.py`'s new `grant_jackett_service_control()` additively grants Authenticated Users `SERVICE_START`+`SERVICE_STOP` via `sc sdset` and sets `sc failure` auto-restart actions (idempotent, best-effort, one UAC prompt; prints the manual commands if elevation is declined). Re-run `setup.py` to apply. Access-denied paths now log an actionable hint.
  - **In-app catch-safe + visibility.** New `jackett_health_monitor` background task polls every 20 s, publishes `state.jackett_ok` (in `state_snapshot()`) and a `jackett_status` SSE event, and — only after ~2 min of sustained local outage (i.e. when no watchdog has recovered it) — calls the reusable `watchdog.restart_jackett()` as a backstop, so it never duels a healthy watchdog.

## [2.8.0] — 2026-05-19
- **Minor feature: bidirectional Handoff (TV ⇄ device), time-synced.**
  - **TV → device.** While a library item is playing on the TV (VLC), a new emerald **Device** button (next to Stop in the player footer, and a **To Device** tile next to Stop in the fullscreen controls) hands playback to the browser that tapped it. Both require a **0.5 s hold** to activate (same `_holdStart` / `.hold-btn` progress-fill pattern as Stop), so an accidental tap can't yank playback off the TV. It captures VLC's live position via a fresh `GET /api/vlc/tracks` read, starts the local `<video>` player on the same item — using the remaining-playlist tail so auto-advance keeps going — seeked to that exact time, and stops VLC. `/api/stop` returns 202, so the device's prep/transcode runs in parallel with the TV tearing down; the resume seek is frozen at the captured time, so the handoff lands on the same frame regardless of how long prep takes. The button shows only during library playback (`is_library_playback && library_item_id`) and is gated by `withInflight("handoff")` against double-taps. To support it, `state_snapshot()` now also publishes `library_item_id` and the full ordered `library_playlist`, and the frontend `app` mirrors both.
  - **Prep gating.** The handoff-to-device button is greyed out with a "Not prepped for on-device streaming" note (footer: tooltip; fullscreen tile: visible sub-label) when the currently-playing file has no `.offline_cache` MP4 and isn't Safari-native — handing off then would stop the TV and drop into a long transcode. Readiness is resolved from `prepFileState` (instant when known) and a per-file `GET /api/library/{id}/prep-status` check (`_handoffReadyState` / `_maybeRefreshHandoffReady`), and the button flips to active automatically once the file is prepped (via the episode picker's Prep / a card's Prep for Streaming). Tapping it while not prepped shows a toast instead of acting.
  - **Device → TV.** The on-device local player now has the mirror-image **To TV** button in its fullscreen header (next to Stop): `lpHandoffToVlc` captures the `<video>` position + remaining playlist tail, tears down the device player (flushing progress), and starts VLC playback at the exact same time via `playLibraryFiles` (`POST /api/library/{id}/play` with `seek_first_to`). Gated by `withInflight("handoff_vlc")`.

## [2.7.0] — 2026-05-19
- **Minor feature: the auto-skip countdown now runs *before* the skip point, and the intro/credits is skipped in full.** Previously the countdown started *at* the intro/credits start and skipped N seconds later, so the first 5 s of an intro (or 10 s of credits) played first. Now the countdown leads *into* the skip point — e.g. for an intro starting at 1:30 the 5-second countdown runs 1:25→1:30 and the skip fires exactly at 1:30, so none of the intro plays. Credits likewise count down over the 10 s before `credits_start` and advance to the next episode the moment credits begin. The countdown is now **position-driven** (`_run_skip_countdown` shows `ceil(target − pos)` and fires at `pos ≥ target`), so it tracks real playback: it freezes while paused and follows seeks, polling every 0.5 s so the popup clears within ~0.5 s when you scrub out of the window. The auto trigger window widened to `[skip_point − lead, …)`; the manual (auto-skip-off) Skip button is unchanged.

## [2.6.2] — 2026-05-19
- **Bug fix:** The countdown popup ("Skipping intro in 1") stayed frozen on the TV after the skip fired — and after any seek that ended the countdown. Root cause: VLC's `marq` filter reads `--marq-file` with `getdelim()`, which returns EOF on an *empty* file, so the filter **keeps the previously-rendered text** instead of blanking it (and logs a read error every refresh tick). Every "clear" wrote an empty string, which was a no-op on screen. `_marquee_write` now clears by writing a single space — a valid non-empty line that forces marq to update but renders no glyph (we draw no background box). `run.py` / `watchdog.py` likewise seed the file with a space, not "". This is what actually made 2.6.1's two-sided-window abort visible on screen.

## [2.6.1] — 2026-05-19
- **Bug fix:** The "Skipping intro in N" countdown popup no longer lingers when the viewer scrubs the progress bar past the intro. `_run_skip_countdown` previously only aborted when the position moved *below* the window floor (a backward seek); a forward seek out of the intro left the popup up and would have yanked playback back to the intro end when the count hit zero. The countdown now takes a `[floor, ceil)` window (intro `ceil = intro end`; credits unbounded) and aborts on leaving it in **either** direction, clearing the popup within ~1 s of the seek.

## [2.6.0] — 2026-05-19
- **Minor feature: on-TV auto-skip countdown popup.** When a profile has auto-skip enabled, Smart Skip no longer cuts instantly — it first shows a countdown directly on the TV (VLC) before acting: **10 s** before auto-advancing past credits, **5 s** before seeking past an intro. The popup renders bottom-right with padding, as opaque white text. Implemented via a VLC `marq` sub-source: VLC is launched (in `run.py`, `watchdog.py`, and `main.py`'s `_restart_vlc_process`) with `--sub-source=marq --marq-file=<repo>/.vlc_marquee.txt --marq-position=10 …`, and `main.py` writes "Skipping intro/credits in N" into that file each second (emptying it clears the popup). Text-only — no global freetype background box — so regular subtitles render unchanged. A dedicated countdown coroutine (`_run_skip_countdown`) polls VLC each tick so it holds while paused and aborts if the viewer seeks out of the window or switches files; Stop / Next / Prev / a new Play all cancel it and wipe the popup. New `state.skip_countdown` field is included in the SSE state snapshot.

## [2.5.0] — 2026-05-18
- **Minor feature: automatic UI cache-bust on version mismatch.** Added `GET /api/version` (returns `{"version": "…"}` with `no-cache` headers) and a `UI_VERSION` constant in `main.py` that must be kept in sync with the version badge in `index.html`. On every page load, a self-invoking script block reads the version baked into the loaded HTML (via the `[data-ui-version]` badge) and compares it against the server response. If the cached page is older than the server's version, the page reloads with a `?_v=<version>` cache-busting query parameter so the browser fetches the updated HTML. A `sessionStorage` guard prevents infinite reload loops.

## [2.4.4] — 2026-05-18
- **Bug fix:** New clients no longer see "No active stream" when they open the remote while VLC is in the middle of real playback — the dashboard now reconciles `state` with VLC on startup. Trigger was a `uvicorn main:app` restart (admin Shut Down, watchdog kick, manual relaunch) while VLC kept running: every `AppState` field reset to its dataclass default (`stream_status="idle"`, `active_title=None`), and `background_video_loop` saw VLC already `state=playing` so it stayed out of the way — leaving every SSE client stuck on "No active stream" until someone pressed Stop and started over. New `_sync_state_from_vlc()` runs in `lifespan` after the volume init: queries `status.json` + `playlist.json`, matches the currently-playing URI against the background-video path (→ sets `background_playing=True` and returns) or every library item's files (→ seeds `active_title`, `library_item_id`, `library_playlist`, `library_current_file`, `active_hash`), and falls back to using the file stem as the title for un-matched playback (external VLC plays, stream-now items whose torrent has been GC'd from `library.json`). `library_profile_id` is intentionally left unset — the original profile isn't recoverable, so `vlc_progress_tracker` skips progress saves and skip offers for the restored session, but title display and next/prev/stop work normally.

## [2.4.3] — 2026-05-18
- **Bug fix:** On server restart, the default VLC volume is now half of the configured max-volume cap instead of 100 (normal). `lifespan` reads the cap from `library.json` settings, sets `state.vlc_volume` and `state.user_volume_before_bg` to `cap // 2`, and immediately pushes that value to VLC. This prevents an audio blast when playback starts on a fresh server start — the pre-existing `vlc("in_play")` pre-roll already sends the volume before any media loads, so the correct starting level now flows through cleanly.

## [2.4.2] — 2026-05-18
- **Bug fix:** Button text is no longer selectable. Added `user-select: none; -webkit-user-select: none` to the global `button, select` rule in `index.html` and a matching `button` rule in `admin.html`.

## [2.4.1] — 2026-05-18
- **Bug fix:** Profile-picker bottom buttons (**Log in with PIN**, **Manage profiles**, **Admin**) are no longer covered by playback chrome on mobile when a video is playing. The picker is `fixed inset-0 z-40` with `bg-gray-950`, but the player footer is also `z-40` and later in the DOM, and the skip/resume offers and local-device player sit at `z-50`+, so all of them bled through over the picker's bottom row on short viewports. Added a `body:has(#profilePicker:not(.hidden))` rule that hides `footer`, `#skipOffer`, `#resumeOffer`, and `#localPlayer` while the picker is up — turns the picker into a proper lock-screen overlay regardless of what's playing in the background.

## [2.4.0] — 2026-05-18
- **Minor feature: Shut Down Server from the admin panel.** New **System** tab in `/admin` with a single red "Shut Down" button. Posts to a new `POST /api/admin/shutdown` endpoint, which enumerates every `uvicorn main:app` process via psutil and sends SIGTERM — siblings first (so the admin process can finish issuing signals), self last, with a 3 s `os._exit(0)` fallback if uvicorn ignores the signal. Once the HTTP uvicorn exits, `run.py`'s existing `finally` block tears down the HTTPS sibling and the mDNS responder. qBittorrent / Jackett / VLC are not touched — they aren't children of the FastAPI process. Useful for stopping the server cleanly from a phone without SSH-ing into the host.

## [2.3.0] — 2026-05-18
- **Minor feature: hold-to-activate on destructive / navigation controls.** The footer Stop button, the footer Prev / Next episode buttons, and the fullscreen-overlay Stop / Prev / Next tiles now require a 0.5 s press-and-hold to fire instead of a single tap. A short tap is a no-op. While held, a CSS fill grows left-to-right across the button so the user can see the timer counting, then briefly flashes once the action fires. Backed by a generic `_holdStart` / `_holdCancel` helper that wires `pointerdown` / `pointerup` / `pointerleave` / `pointercancel` — works on touch and mouse. Motivation: these three controls all kick the user out of their current playback context, and an accidental brush against the bottom strip was triggering them.
- **Retry button removed.** The amber "Retry" tile in the fullscreen controls (added in 4.4 as a workaround for VLC freezes when files weren't ready) has been removed along with its frontend `retryPlayback` handler. The `/api/retry` endpoint and `_retry_task` helper in `main.py` are kept in place for now in case they're useful via direct API; they're no longer reachable from the UI.

## [2.2.1] — 2026-05-18
- **Bug fix:** Player UI no longer gets stuck on "Loading…" / buffering after a library play, even though VLC is already playing. The 2.2.0 refactor of `/api/library/{id}/play` flipped `state.stream_status` to `"playing"` only *after* the full `in_play` + sequential `in_enqueue` loop completed — so a "continue watching" play that enqueued 50+ remaining episodes left the UI in buffering for many seconds even though VLC had been playing the first episode from the start. The state flip + broadcast now run the instant VLC accepts the first track (the only roundtrip that gates actual playback), and the remaining `in_enqueue` calls run in parallel via `asyncio.gather` so wall time is one roundtrip max regardless of playlist length. Same fix applied to `/api/vlc/prev` and `/api/vlc/next`'s `_vlc_relaunch_playlist` helper.

## [2.2.0] — 2026-05-18
- **Minor feature: slow-network playback responsiveness.** On a slow or flaky link the app used to feel dead for several seconds between "user clicks Play" and "VLC actually opens the file" — the Play response was synchronous on the VLC HTTP roundtrips, the chooser modal closed immediately, and the player UI didn't paint until the SSE `playing` event arrived. Four-part fix:
  1. **Server: non-blocking playback handoff.** `/api/library/{id}/play`, `/api/vlc/prev`, `/api/vlc/next`, `/api/stop`, and `/api/stream` now broadcast a state event (`buffering` / `idle`) and return 202 immediately. The slow VLC `in_play` + `in_enqueue` work (and the qBit deletes on stop/stream) run in background tasks so the response no longer waits on the network at all. A new `state.library_play_task` is tracked and cancelled on subsequent Play / Stop so a slow handoff can't race a newer action.
  2. **Client: optimistic buffering UI.** A new `_optimisticBuffering(label, itemId)` helper flips `app.stream_status` to `buffering`, populates `active_title` from the cached library item, and opens the mobile fullscreen overlay the instant the user clicks Play — no waiting for the server. `renderPlayer` now also auto-opens the overlay on `stream_status === "buffering"` (previously only on `playing`). For library plays the buffering badge says **"Loading…"** and the fullscreen status says **"Starting playback…"** (instead of the misleading "Buffering…" / "Connecting…" copy, which made fully-downloaded episodes look like network problems).
  3. **Client: in-flight guards on Play.** `continueLibraryItem` and `playLibraryFiles` now run under `withInflight("play_${itemId}", …)`, so an impatient double-tap while the server is mid-handoff is dropped client-side instead of racing extra `in_play` requests to VLC. They also bail with a warn-toast if `app._connected === false` (i.e. SSE has been disconnected past the 4 s grace), since the response would otherwise never get a confirming state event.
  4. **Client: visible SSE connection state.** The navbar's SSE pill (`#sseLabel`) now shows **LIVE** / **OFFLINE** on mobile too (previously `hidden sm:block`), and after a 4 s reconnect grace the user gets a "Lost connection to host — reconnecting…" toast plus a guard that blocks new Play actions until SSE re-opens.

## [2.1.8] — 2026-05-17
- **Bug fix:** Resume popup now resumes playback in the same mode that is already active (VLC if no local player is open, local device player if it is), instead of always opening the "Play where?" chooser.

## [2.1.7] — 2026-05-17
- **Bug fix:** Fullscreen-controls overlay no longer lets users highlight (text-select) any of its labels, buttons, title, or time readout when long-pressing or drag-selecting on touch / desktop. Previously only `.fc-tile` elements had `user-select:none`; now the entire `#fullscreenControls` subtree disables selection and the iOS tap-highlight flash.

## [2.1.6] — 2026-05-17
- **Bug fix:** Idle background video now reliably comes up fullscreen with the taskbar / Dock hidden and every other app minimized when StreamLink is launched as a system service at boot/login. Three-part fix:
  1. VLC is now spawned with `--fullscreen` in all three launch paths (`run.py`, `watchdog.py`, `main.py:_restart_vlc_process`) so it owns the screen from the very first frame instead of relying on a post-`in_play` HTTP toggle.
  2. `vlc_focus_and_fullscreen` no longer runs the focus + minimize-others pass once — it now loops for ~24 s on a slowing cadence (6× 0.5 s, 8× 1 s, 6× 2 s), re-asserting focus and re-minimizing every visible non-VLC window each iteration. The previous one-shot pass only caught apps that were already visible at the moment vlc_focus_and_fullscreen ran, and startup apps that launch a few seconds *after* logon would appear on top of VLC and leave the taskbar / Dock visible. The Stop-button path didn't have this problem because the desktop is already settled when Stop is pressed; the loop replicates that "stable desktop" condition for the boot path.
  3. On macOS the focus path additionally hides every other visible app via AppleScript so the player covers the screen — the counterpart to the existing Windows `_minimize_other_windows_windows` call.

## [2.1.5] — 2026-05-17
- **Bug fix:** Setup and launcher prompts no longer hang when stdin is non-interactive (system service, piped invocation, etc.). `setup.py`'s `ask`/`ask_bool` now check `sys.stdin.isatty()` upfront and auto-apply the default if there's no console — previously they relied on `input()` raising `EOFError`, which does not always fire reliably under Windows Task Scheduler. The `.env`-reuse prompt now correctly defaults to "yes" in service contexts. `run.py`'s "Continue without VPN?" prompt is similarly gated and silently proceeds when non-interactive (safe because the watchdog already gates qBittorrent on VPN status).

## [2.1.4] — 2026-05-16
- **Bug fix (Windows):** Dropped `/RL HIGHEST` from the scheduled task created by `run.py --install`. On Windows, ports below 1024 do not require admin to bind (the "privileged ports" rule is Unix-only), so the wrapper doesn't need elevation. HIGHEST was actively harmful for Standard User accounts — it made Task Scheduler try to elevate their token at trigger time, which failed silently and left the task registered but never running. Firewall rules (which DO need admin) are now added during `_windows_install` while we hold the UAC-granted admin token.
- **Setup warning:** `setup.py` now detects per-user Python installs on Windows (Microsoft Store python, `AppData\Local\Programs\Python`, etc.) and warns that the resulting venv will not be usable by any other Windows user account. This was the silent root cause of the service-doesn't-run case: when the scheduled task ran as a different user, the venv launcher couldn't read the base python in the installer's user profile and bailed with "Access is denied" before the wrapper could log anything.
- **Docs:** New GOTCHAS entries for Microsoft Store Python and the `/RL HIGHEST` / `/RU` Windows-task pitfalls so the next person doesn't have to re-debug.

## [2.1.3] — 2026-05-16
- **Bug fix (Windows):** `run.py --install` now registers the scheduled task to run as the *console* user (the one actually sitting at the keyboard), not the Admin account whose credentials were entered at the UAC prompt. Previously, installing from an elevated Admin shell while logged in as a regular user would tie the task to the Admin's logon, so the task never fired at the user's logon — the service appeared to install successfully but never started, with no logs written. Detection uses `WTSGetActiveConsoleSessionId` + `WTSQuerySessionInformationW` (works on all Windows editions), with a PowerShell `Win32_ComputerSystem.UserName` fallback. Install output now prints the detected `RunAs` user so it's clear which account the service is tied to.

## [2.1.2] — 2026-05-16
- **Bug fix:** System service (`run.py --install`) now registers mDNS, opens Windows firewall, and launches the HTTPS admin process — matching what `python run.py` does interactively. Previously the service only ran the watchdog + HTTP uvicorn, so `remote.local` did not resolve and admin/HTTPS was unavailable when launched via the installed service. The wrapper now reuses `run.py`'s `get_local_ip`/`setup_windows_firewall`/`start_mdns` helpers so the installed service and interactive launch take the same code path for these network bits. (Both `setup.py`'s install offer and `run.py --install` already delegate to `daemon.install()`, so the registered task itself is identical between them.)

## [2.1.1] — 2026-05-16
- **Bug fix:** qBittorrent no longer launches on startup when Mullvad VPN is off. `run.py` now checks VPN status before starting qBit; if VPN is down, qBit is skipped and the watchdog will start it once VPN connects.

## [2.1.0] — 2026-05-16
- **Minor feature:** Fullscreen vol − / + buttons now detect hold-down (≥400 ms) and apply a ±15 step instead of ±5; short taps retain the original ±5 behaviour. Repeats every 350 ms while held.

## [2.0.0] — baseline
- Initial versioned release (previously tracked as alpha1/alpha2).
