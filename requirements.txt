fastapi>=0.111.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
psutil>=5.9.8
pydantic-settings>=2.3.0
python-multipart>=0.0.9
zeroconf>=0.131.0
cryptography>=42.0.0
pyacoustid>=1.3.0
# YouTube-on-TV system-volume control (CoreAudio). Windows only; macOS uses
# osascript, Linux uses pactl/amixer — both built-in / standard.
# `comtypes` is declared explicitly even though pycaw lists it as a transitive
# dep — on real hosts we've seen pycaw installed via a path that skipped its
# deps (older standalone install, `--no-deps`, etc.) leaving comtypes missing
# and producing "No module named 'comtypes'" at runtime. Belt-and-braces.
pycaw>=20240210; sys_platform == "win32"
comtypes>=1.2.0; sys_platform == "win32"
